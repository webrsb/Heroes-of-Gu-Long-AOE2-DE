# 船夫搭船（DE 重做）設計

日期：2026-08-30　狀態：使用者已逐段核可、三輪 spike 皆已入檔，待書面複審。

## 一、背景

金鵬島線兩座碼頭（東 (110–114, 226–228)、西 (74–81, 233–235)）由 P8 兩艘「畫舫」（運輸船 ref 25003／25004，`船`／`船2` 每 21 秒對開）連接，深水不可步行。

**原作（劍譜5）機制**：`船塢` T3672 永久迴圈在牆位建 Gaia 乾草堆 ×3 當牆；玩家踩外線→報價→走到船夫前→`X船6` 扣費並在牆內水面建一顆該座位的地圖顯示器當「票」；`X草`／`X草2` 偵測「牆內有該座位物件」（票本身就滿足）→ `草草5`／`草草7` 拆牆，`草草6`／`草草8` 計時後關拆牆、重武裝偵測。牆靠「船塢每輪建、草草每輪拆」的時序打架維持通路；玩家走過牆登上盟友（P8）運輸船，船被 task 到對岸**陸地格**時卸貨；抵達者站在牆內同樣觸發拆牆走出；票在踩外線時被 `X船4` 清掉。頭暈（`X頭暈起`→3 分鐘後 `X頭暈` 復原）由東碼頭 `X船6` 在付費時直接啟動。

**基底（無法無天）現況**：作者插入觸發造成 ID 位移，`草草5`～`草草8` 被換成無關觸發——`X草` 現指 T3949/T3950（箭影高人 P5/P6 傷害迴圈，正規開關是 `X當僧`／`X練4`），`X草2` 現指 T3951/T3952（周賓巢穴的橋，正規路徑是狂風犬出現後 3600 秒）。全圖無任何觸發移除那兩道牆。結果：牆永不開、付費照扣、站到牆內就誤開 Boss 橋。使用者體感的「重複被扣錢」＝進不去→走動踩到外線→`X船4` 重武裝整條鏈→再扣一次。

**DE 實測（spike_ferry v1/v2，2026-08-30）**：
- `UNLOAD`（效果 17）能把盟友乘客從 P8 運輸船卸到指定岸格；原作「task 到陸地格」在 DE 對盟友乘客不卸。
- 駐軍中單位計入區域條件，但座標**凍結在登船那格**、不隨船移動（②a PASS／②b FAIL）。
- `TELEPORT_OBJECT`（效果 35）區域選取**一次只搬一隻**；`max_units_affected` 無效；同區域連下 N 個效果可各搬一隻（使用者評「過於暴力」，不採）。
- 駐軍中單位不可被傳送（既有否決事實）。
- 落點格被任何單位壓住時傳送**失敗且無事發生**（spike_ferry3 v3：同落點連傳只到 1 隻）。

## 二、目標與非目標

目標：付費後能可靠登船、渡海、下船、出港；一人一趟一票、隊伍同行；不重複扣費；頭暈只在英雄真正進入登船處時觸發；修掉基底錯線。

非目標：不改票價與等級門檻；不改畫舫班次；不拆牆（牆與 `船塢` 原樣保留）；不處理西海 (9,93) 船夫 2000 兩那條座位 1 獨有路線（另案裁決）。

## 三、機制（每座位 s × 每碼頭一套；「旗」皆為 Gaia 旗）

| 觸發 | 開／迴圈 | 條件 | 效果 |
|---|---|---|---|
| `X船6`（既有，改） | 原樣 | 原樣 | 保留扣費與台詞；**移除**建票 `CREATE_OBJECT 837`；**移除**東碼頭直接啟動 `X頭暈起`；**新增**啟動 `X船入`、`X船窗止`、`X船暈`；**新增**提示「<ORANGE>買票後三分鐘內踩牆邊旗子進入登船處，船上踩旗可回岸」——用 `SEND_CHAT` sp=s（與同支既有船夫台詞同型），**只有買票的座位看得到**；不得用 `DISPLAY_INSTRUCTIONS`（全員可見） |
| `X船入`（新） | 關／迴圈 | 座位 s 有物件在**牆外旗格** | `TELEPORT` sp=s、區域＝牆外旗格 → 牆內落點。一次一隻，窗內踩幾次傳幾隻；落點被壓住時該輪失敗，單位留在旗格，下一輪重試 |
| `X船清入`（新） | 開／迴圈 | 座位 s 有物件在**牆內落點格** | `TASK_OBJECT` sp=s、區域＝落點格 → 走到落點旁一格（讓出落點，下一隻才傳得進來） |
| `X船窗止`（新） | 關／一次 | `TIMER` 180 | 關 `X船入`、關 `X船暈` |
| `X船暈`（新） | 關／一次 | `BRING_OBJECT_TO_AREA`（unit＝座位 s 英雄 ref，區域＝牆外旗格） | 啟動既有 `X頭暈起`；關自己 |
| `X船出`（新） | 開／迴圈 | 座位 s 有物件在**牆內旗格** | `TELEPORT` sp=s、區域＝牆內旗格 → 牆外落點 |
| `X船清出`（新） | 開／迴圈 | 座位 s 有物件在**牆外落點格** | `TASK_OBJECT` sp=s、區域＝落點格 → 走到落點旁一格 |
| `船卸東`／`船卸西`（新，不分座位） | 開／迴圈 | P8 有 const 545 在停船水域 | `UNLOAD` sp=8、區域＝停船水域 → 牆內落點 |
| `船旗初始化`（新） | 開／一次 | `TIMER` 0 | `CREATE_OBJECT` sp=0 旗 ×4（兩牆外、兩牆內） |
| `X船4`（既有，改） | 原樣 | 原樣 | **中和**「移除 (108,227)／(83,234) 物件」效果——票已不存在，它唯一可能刪到的是船上駐軍中的英雄 |
| `X草`／`X草2`（既有，改） | **停用** | — | 基底已指到箭影高人迴圈與周賓橋；票拿掉後仍會被牆內乘客觸發 |

流程：買票（原鏈）→ 3 分鐘內任何單位踩牆外旗即被傳入牆內（英雄踩時開始頭暈）→ 點畫舫登船 → 船到對岸停船水域 → `船卸` 卸到牆內落點 → 踩牆內旗傳到牆外落點。後悔不搭者踩牆內旗即可出來。回程須重新買票。

頭暈判準不依賴位置：抵達者全程只碰牆內旗，牆外旗只有買票後主動走進登船處的人會踩；買票後又出來的人白暈 3 分鐘，與原作「付錢即暈」等價。

## 四、座標

| | 東碼頭（金鵬島線，收 100,000 精力） | 西碼頭（回中原線，收 2,000 兩） |
|---|---|---|
| 牆（保留） | x=111, y 226–228 | x=80, y 233–235 |
| 牆外旗格 | (112,227)（付費區內，緊貼牆） | (79,234)（付費區內，緊貼牆） |
| 牆內落點 → 清場走到 | (110,226) → (110,227) | (81,233) → (81,234) |
| 牆內旗格 | (110,228) | (81,235) |
| 牆外落點（出港）→ 清場走到 | (115,231) → (115,233) | (75,234) → (74,234) |
| 停船水域（卸貨） | (107–109, 225–229) | (82–84, 233–235) |

牆外落點皆在 `X船4` 外線之外，走回船夫會正常重新報價。

## 五、施工位置

- 全部走 `merge_spec.yaml`：`trigger_fixes`（`X船6`／`X船4` 效果編輯、`X草`／`X草2` 整支停用）與 `trigger_add`（新觸發），由既有 s37 施作；不新增 step。
- 新觸發帶座位欄位，會照 s39 慣例展開成（職業, 座位）變體；`X船暈` 用英雄 **ref**（非 const）以沿用 s39 對 `X頭暈起` 同型 sel 的重指。
- 新旗子用初始化觸發 `CREATE_OBJECT` 放置，不動單位表；旗格需先過「玩家相關區域覆蓋圖」（2026-08-30 廣場洗頻教訓）——牆外旗格落在 `X船6` 付費區內是刻意的（買票與踩旗同一區）。
- `X船6` 目前 12 支 ID：東 T3750/3755/3760/3765/3770/3775、西 T3781/3787/3793/3799/3805/3811；`X船4`：東 T3748/3753/3758/3763/3768/3773、西 T3779/3785/3791/3797/3803/3809；`X草` T3734–3744 偶、`X草2` T3735–3745 奇。
- s90 對帳：Change kind `trigger_add`／`effect`／`trigger` 既有支援。

## 六、待驗與風險

- **已驗（spike_ferry3）**：落點被壓住＝傳送失敗、無事發生 → 落點由 `X船清入`／`X船清出` 迴圈用 `TASK_OBJECT` 叫站在上面的單位走開一格；`X船入`／`X船出` 本身是迴圈，失敗那輪單位留在旗格，落點清空後自動重試。（使用者裁決，不採多落點級聯。）
- `X船清入` 只在單位站在落點格那一瞬間下令，玩家在落點格上直接點船會被蓋掉一次，走開一格後再點即可；清場走到的格子緊鄰畫舫。
- `TIMER` 於觸發被啟動時起算（原作 `1船2`／`草草8` 同用法），3 分鐘窗以此為準。
- 西停船水域含海灘格 (82,y) 只影響 `UNLOAD` 區域選船，不再用於頭暈，無誤判疑慮。
- 多人：六座位各自一套，畫舫共用；兩人同時在牆內互不干擾（各自踩旗各自傳）。

## 七、驗收（單人→劇情，必帶陽性對照）

1. 東碼頭買票 → 提示出現 → 英雄踩牆外旗 → 出現在 (110,226) 且「船暈現象」名牌亮起（對照：寵物先踩旗只傳不暈）。
2. 點畫舫登船 → 船到西岸 → 自動下船在 (81,233) 附近 → 踩 (81,235) → 出現在 (75,234)。
3. 回程買票 2,000 兩 → 同流程回東岸 → 踩 (110,228) → 出現在 (115,231)。
4. 買票後不進去、3 分 10 秒後踩牆外旗 → **不傳**（窗已關，對照：3 分內會傳）。
5. 站在牆內任一格 30 秒 → 周賓橋不變、P5/P6 站 (116,54) 不掉血（錯線已拔）。
6. 全程只被扣一次費。

## 八、裁決記錄（2026-08-30）

- 方案 B（傳進／傳出）取代 A（直接傳過海）與 C（修回拆牆狀態機）。
- 票機制移除；牆永久；改為 3 分鐘售票窗＋牆內外兩旗，不自動傳。
- 一人一趟一票、隊伍同行（同原作）；一次只傳一隻是 DE 限制，順勢成為規則。
- 頭暈判準＝英雄踩牆外旗（窗開時），不用停船水域位置偵測（無法分上船／下船）。
- 反向法（開放走入、沒票者傳出）否決：駐軍中不可傳送→上船即無法拉出；票狀態回歸。
- 落點被壓住：不用多落點級聯，改「落點格迴圈叫人走開一格」，傳送迴圈自然重試。
