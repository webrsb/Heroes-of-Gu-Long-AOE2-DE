# 復活與選職業系統 設計書

2026-08-29 定稿。標的：`out/古龍921_合併.aoe2scenario`（經合併流水線，不動任何來源檔）。
承接 spike 實證（src/合併/spikes/，commits 43eac6e…670cc6e）與使用者逐項裁決。

## 一、需求（使用者裁決彙整）

1. **遊戲內選職業**：六隻展示英雄依使用者佈局（2026-08-29 自編輯器版位萃取，見 §五座標表）
   站在出發廣場邊，**全程掛 P8**（玩家可點選查看職業數值資訊）、t=0 凍結、頭上 caption＝改名為職業名。
   點選即選定；六職業不可重複、先選先贏；任何玩家位可玩任何職業（全矩陣）；
   沒被選走的職業永遠鎖定；少於 6 人自然支援。
2. **10 條命**（建置參數 `lives`，先以 3 條出測試版）：死亡後 **10 秒**於出發點重生；升級／任務效果跨命保留；無復活代價。
3. **訊息**：非最終死亡＝「{職業名}已受傷，10秒後重生({N})」（N＝累計死亡次數）；命盡＝「{職業名}已死亡」。職業名沿用原「X死」觸發的稱謂（刀客等，實作時自 T3821–3826 訊息萃取）。
4. **死亡連動**（馬召收馬、哥8轉裝、魂移、修端木等 54 處）：**每次死亡都觸發**（裁決 1b）。非循環的連動觸發由重生鏈重新啟動，使其下次死亡可再觸發。
5. 原 12 支「X死」觸發（訊息＋生成 const837）退役，由新系統接管。const837 的防敗功能等價遷移：命盡時給該玩家一個角落觀戰單位（防全滅判負）。
6. **DE 圖形佚失 const 替換表**（2026-08-29 徹查定案。實證：1.21z 未改 dat，全部原版 TC 數值；
   DE 保留同條目但五個 const 圖形佚失。AoC 對照確認原版看得到 → 全數換皮＋數值校正回原版值）：
   | const | 原版身分/數值 | 替換 | 校正 |
   |---|---|---|---|
   | 94 拳本體×3 | BRSRK槽 HP75 攻10 甲0/0 攻速1.5 速0.88 | **1811 戰鬥祭師**（使用者選定） | HP80→75 攻11→10 甲1/1→0/0 攻速→1.5 速→0.88 |
   | 37 ×12（P8） | LANCE槽 HP110 攻7 速1.4（AoC外觀=駱駝系） | **329 駱駝騎士** | HP100→110 攻6→7 |
   | 573 ×1（P8） | RFSWD槽 HP45 **無攻擊** 和平NPC | **74 民兵**（使用者選定外觀） | HP40→45 攻4→**0** |
   | 95 ×6（P8） | GTBC2 石城門構件 HP2750 @(226,12~14) 石牆缺口 | DE 石牆城門（AoC 對位 2026-08-29 確認；拆6構件、置1城門，朝向沿 x=226 牆線） | 依原版 HP2750；稽核涵蓋該區的效果/條件並改接 |
   | 817 ×4＋生成13處 | STATUE 雕像 HP1 裝飾 | **保留原 const**（DE 有圖，2026-08-29 預覽確認） | — |
   數值校正用 DE 屬性修改效果（玩家×單位種類，t=0 初始化觸發）。
   實作稽核：替換目標 const（1811/329/74）在全圖的既有使用與區域條件交集，防替換後誤中既有過濾。

## 二、已實證機制（spike 結論，全部單人實測）

| 機制 | 結論 |
|---|---|
| 效果 ref 清單含死 ref | 活 ref 照常生效 → 升級跨命保留的基礎 |
| 效果玩家欄 = -1（帶 ref 清單） | 改名/傷害/加血/加攻/任務全部照做 → 玩家位中性化基礎 |
| OR 條件（type 29） | parser 可寫、遊戲照 OR 評估 |
| 30,002 支觸發 | 讀圖與遊玩效能無虞 |
| 藏兵艙 | `garrisoned_in_id` 預駐**隱形物件 const 1291**，一命一容器 |
| 放兵 | **先駐軍中換家 → 再移除容器**，兵以正確歸屬彈出 |
| 防歸順 | 單位一律 **P8 建檔 → t=0 轉 Gaia**（建檔 Gaia 會被自動歸順） |
| 點選選角 | OBJECT_SELECTED 對遊戲中轉 Gaia 的單位有效（單人） |
| 已否決路徑 | 卸載（跨家無效）、P8 建築（AI 亂卸）、毛毯（移除吞兵）、傳送（使用者棄用）、帳棚類 |

**未驗（實作期 spike）**：OBJECT_SELECTED 對 **P8 所屬**單位是否觸發（已證 Gaia 可、P8 未測）；
OR 與 AND 條件混排的分組語意（不過關則退回每命複製觸發——效能已證可容納）。
**未驗（多人前置項）**：OBJECT_SELECTED_MULTIPLAYER 的多人同步行為。

## 三、資料模型

- **職業 C**＝原玩家位 1–6 的角色系統（刀客…）。職業名、原本體 ref（0/1/2/502/7/45117）不變。
- **玩家位 S**＝實際玩家 1–6。
- **命 L**＝1..lives。第 1 命＝原本體單位（建置期改掛 P8、移到展示位、t=0 凍結，**ref 不變**——既有 ref
  接線對第 1 命零改動；不轉 Gaia，P8 同盟可被玩家點選看數值）。第 2..lives 命＝新增同 const 單位，
  各自駐於一個新增隱形容器（const 1291），容器全部疊在**重生區＝出發廣場方框正中央 (81,111)**
  （方框＝聖物碎片旗座四角 77.5,107.5～84.5,114.5）。
- **配對 (C,S)**＝選角建立，全域唯一 per C、唯一 per S。

## 四、接線架構（方案二：欄位中性化＋殘餘矩陣）

對本體 ref 的 1,858 處引用（六職業計）分四類處理，全部由建置步驟腳本生成：

1. **效果 ref 清單**（改名/傷害/血/攻/任務/停止/凍結/移除等 ~1,479 處）：追加第 2..lives 命 ref、玩家欄改 -1 → 六玩家位共用一份，備身在容器中同步吃升級。
2. **條件 ref 欄**（帶到區域/帶到物件/摧毀/有目標等 ~360 處）：以 OR 展開至各命 ref，維持單一觸發（OR 混排語意 spike 不過則退回每命複製）。
3. **效果 location_object_reference**（任務目的地＝本體，19 處）：無法 OR，逐命複製觸發。
4. **玩家定址觸發**（聊天收件人、經貢進貢、資源累積條件、按玩家過濾的區域條件、為玩家生成——精確清單由計畫期腳本盤點）：複製 ×6 玩家位變體，建置時全部停用，選角時批次啟動 (C,S) 變體。跨玩家觸發 16 支逐一裁決寫入 merge_spec。

## 五、選角系統

- 展示單位（＝第 1 命）位置（使用者 2026-08-29 編輯器擺位）：
  P1 刀客 (78.5,108.5)、P2 (80.5,108.5)、P3 (82.5,108.5)、P4 (83.5,109.5)、
  P5 (83.5,111.5)（換皮後補位，待確認）、P6 (83.5,113.5)。
  全程 P8＋t0 凍結＋改名為職業名（caption）。
- 單人條件用 OBJECT_SELECTED；多人版換 OBJECT_SELECTED_MULTIPLAYER（帶選取者欄），每 (C,S) 一支選角觸發。
- 選定 (C,S)：第 1 命轉給 S → 啟動 (C,S) 玩家定址家族＋復活鏈第 1 節 → 停用職業 C 其餘玩家位的選角觸發（不可重複）＋玩家位 S 對其他職業的選角觸發（一人一職）。
- 訊息：「{S 玩家}選擇了{職業名}」。

## 六、復活鏈（每職業一條，per (C,S) 變體）

死亡第 N 次（N < lives）：
1. 死亡偵測：摧毀物件(第 N 命 ref)（觸發建置時停用，前一節啟動）
2. 訊息「{職業名}已受傷，10秒後重生({N})」
3. 連動觸發重啟（裁決 1b：讓馬召等連動下次死亡可再觸發；其條件同步 OR 展開至各命）
4. 啟動 10 秒計時觸發 → 第 N+1 命駐軍中轉給 S → 移除容器 N+1 → 啟動下一節死亡偵測

死亡第 lives 次：訊息「{職業名}已死亡」＋角落觀戰單位轉給 S（防敗等價遷移）。
重生位置＝重生區 (81,111)（容器所在），所有職業共用。

## 七、建置整合

- 新增流水線步驟（依既有 auto-registry 慣例，置於 s37 之後、s40 之前）：
  - `s38_選職業`：本體改掛 P8、展示/選角/配對觸發生成、玩家定址家族 ×6 複製與啟動表
  - `s39_復活`：備身與容器單位生成、ref 追加與玩家欄中性化、條件 OR 展開、復活鏈生成
- 參數全數入 `merge_spec.yaml`（`params.revive`：lives、展示位座標表、重生區座標、觀戰單位 const/座標、
  職業名表、P5 換皮 const）。缺裁決一律 BuildError，不默默裁定。
- 使用者編輯器佈局檔（遊戲資料夾 2026-08-29 20:37 版）僅作座標萃取來源，不併入產線；
  抽查其 5799 支觸發效果數值與 repo 版零差異（本次編輯器存檔未破壞數值，鐵律照舊）。
- TDD：每步驟先寫失敗測試；s90 稽核擴充——新 ref 接線完整性（每效果清單含全命 ref）、變體家族啟動表一致性、無建檔 Gaia 單位。
- 雙軌 log 同檔名慣例、測試指引自動生成、一次建置一個 commit。

## 八、測試計畫

單人（每階段建置後）：選角→技能實測（**第 2 命發動原職業技能**＝接線核心驗證）→ 打坐/升級跨命→ 死亡連動每死觸發→ 10 秒延遲→ 命盡訊息＋觀戰單位→ 未選職業鎖定。陽性對照照鐵律。
多人（lives=3 版過後）：選角同步、不可重複搶選、多人重生歸屬。
效能：正式版全量建置後以實玩回報。

## 九、風險與退路

| 風險 | 退路 |
|---|---|
| OR 混排語意不符 | 每命複製條件觸發（30k 效能已證） |
| 多人點選不同步 | 退走位進圈選角（區域條件，AoC 傳統） |
| 玩家定址盤點漏項 | s90 稽核比對「觸發引用玩家欄」全清單，缺項 BuildError |
| 死亡瞬間連動與重生競態 | legacy_execution_order 固定順序；連動排在重生鏈之前 |
