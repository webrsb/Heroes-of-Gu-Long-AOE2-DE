# 野怪平衡曲線 s374 實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 依 spec `docs/superpowers/specs/2026-09-01-野怪平衡曲線-design.md` 的裁決表，把 40 條野怪生成觸發的加血／加攻改寫成新曲線目標值，並在 s91 加特徵斷言。

**Architecture:** 合併管線新步驟 `s374_mobbalance`（自動註冊，id 排序落在 s373 改名之後、s375 killvar 之前）；目標值全部以裁決表 `params.mob_balance` 寫死在 merge_spec.yaml（表＝spec §六，可稽核）；步驟把每個建立點的格效果改寫為 `目標 − 基礎`、全域加攻歸零折入格效果；`analysis/balance_check.py` 提供 s91 特徵斷言（實值對帳＋等級單調）。

**Tech Stack:** Python 3.14、AoE2ScenarioParser 0.8.4（鎖版）、pytest（假件工廠 `tests/conftest.py` 的 `f`）。

**背景知識（生成觸發結構，已實測 out/古龍921_合併.aoe2scenario）：**
- 每個建立點一組效果：`REMOVE(15, 格)→CREATE(11, loc)→CHANGE_HP(27, 格, qty=加血)→CHANGE_ATTACK(28, 格, aac=class/amount)`；部分觸發另有**全域 28**（area 全 -1，如 T2746 民團 +20、T2721 狂屍 +779、T3854 渦怪手下 +1069）。
- 實際血 ＝ 基礎血(dat＋P7科技) ＋ 格 27 qty；實際攻 ＝ 基礎攻 ＋ 格 28 amount ＋ 全域 28 amount。
- s30 之後 28 的編碼一律 `armour_attack_class=0, armour_attack_quantity=N, operation=ADD(2)`（見 `steps/s30_attackfix.py`）；27 的 operation 原檔為 -1，行為是加值，**不動 operation 只改 quantity**。
- 雙單位觸發（T4787 劍勇）：格 27/28 帶 `object_list_unit_id`（640/765），以「同格的 CREATE」對應 const。
- 渦鬼(珠光) T2713／(殺人) T2714、頭目（3959 首領、3997 靈象、3998 豬王、5112 豬頭、3853 渦怪）、表三賓周增援：**不入表＝不動**（特例「維持現值」的實作方式）。
- 觸發定位沿用 s373 的防呆：`trig_by_id` ＋ 觸發名比對，名不符即 `BuildError`（基底位移防護）。

---

### Task 1: 裁決表寫入 merge_spec.yaml

**Files:**
- Modify: `src/合併/merge_spec.yaml`（緊接 `mob_names:` 區塊結束之後、`killvar:` 註解之前）

- [ ] **Step 1: 插入 params.mob_balance**

在 `merge_spec.yaml` 的 mob_names 區塊後插入（縮排跟 mob_names 同層，屬 params）：

```yaml
  # 【2026-09-01 野怪平衡曲線】spec docs/superpowers/specs/2026-09-01-野怪平衡曲線-design.md。
  # 曲線：近戰基準DPS(Lv)=7.5×1.051^(Lv−1)、血(Lv)=70×1.042^(Lv−1)；折扣 m×1.0/r×0.7/f×0.85/rf×0.6；
  # 攻=round(折後DPS×開火)。hp/atk＝目標實值（含基礎）；hp: null＝表二血不動。
  # base_hp/base_atk＝該單位 dat＋P7 帝王科技實值（野怪比較.md）。步驟寫入加值＝目標−基礎。
  # 渦鬼(2713/2714)、頭目、表三不入表＝維持現值。kind: m近戰/r遠程/f快速/rf遠快（快速=移速≥1.2）。
  mob_balance:
    - {tid: 2746, name: 民團, const: 74, lv: 1, kind: m, itv: 2.0, base_hp: 60, base_atk: 17, hp: 70, atk: 17}
    - {tid: 2745, name: 山賊, const: 448, lv: 4, kind: f, itv: 1.9, base_hp: 95, base_atk: 11, hp: 95, atk: 14}
    - {tid: 2744, name: 獵人, const: 93, lv: 9, kind: m, itv: 3.0, base_hp: 45, base_atk: 11, hp: 97, atk: 33}
    - {tid: 2743, name: 青銅獅, const: 700, lv: 14, kind: f, itv: 2.0, base_hp: 100, base_atk: 16, hp: 120, atk: 24}
    - {tid: 2742, name: 獅頭族人, const: 751, lv: 18, kind: f, itv: 2.0, base_hp: 100, base_atk: 17, hp: 141, atk: 30}
    - {tid: 2741, name: 官兵, const: 692, lv: 21, kind: m, itv: 2.0, base_hp: 54, base_atk: 20, hp: 159, atk: 41}
    - {tid: 2740, name: 六扇門門生, const: 686, lv: 24, kind: r, itv: 2.0, base_hp: 55, base_atk: 11, hp: 180, atk: 33}
    - {tid: 2739, name: 紅鞋子幫眾, const: 5, lv: 26, kind: r, itv: 3.45, base_hp: 40, base_atk: 17, hp: 196, atk: 63}
    - {tid: 2738, name: 武僧, const: 281, lv: 28, kind: r, itv: 2.0, base_hp: 60, base_atk: 7, hp: 213, atk: 40}
    - {tid: 2736, name: 峨嵋弟子(男), const: 555, lv: 35, kind: m, itv: 2.0, base_hp: 70, base_atk: 20, hp: 284, atk: 81}
    - {tid: 2737, name: 峨嵋弟子(女), const: 629, lv: 35, kind: f, itv: 2.0, base_hp: 220, base_atk: 21, hp: 284, atk: 69}
    - {tid: 2735, name: 仙童, const: 41, lv: 38, kind: m, itv: 2.0, base_hp: 60, base_atk: 18, hp: 321, atk: 94}
    - {tid: 2734, name: 青衣樓殺手, const: 77, lv: 43, kind: m, itv: 2.0, base_hp: 60, base_atk: 17, hp: 394, atk: 121}
    - {tid: 2733, name: 鏢師, const: 232, lv: 45, kind: f, itv: 2.0, base_hp: 70, base_atk: 19, hp: 428, atk: 114}
    - {tid: 2732, name: 偏將, const: 73, lv: 48, kind: r, itv: 3.0, base_hp: 50, base_atk: 14, hp: 484, atk: 163}
    - {tid: 2711, name: 母夜叉, const: 430, lv: 52, kind: m, itv: 2.0, base_hp: 100, base_atk: 12, hp: 571, atk: 190}
    - {tid: 2712, name: 青武, const: 763, lv: 55, kind: rf, itv: 1.9, base_hp: 50, base_atk: 9, hp: 646, atk: 125}
    - {tid: 2715, name: 孽僧, const: 726, lv: 59, kind: m, itv: 2.0, base_hp: 75, base_atk: 27, hp: 761, atk: 269}
    - {tid: 2716, name: 獅頭魔, const: 752, lv: 60, kind: f, itv: 2.0, base_hp: 100, base_atk: 17, hp: 793, atk: 240}
    - {tid: 2717, name: 石獅, const: 168, lv: 61, kind: f, itv: 2.0, base_hp: 450, base_atk: 18, hp: 826, atk: 252}
    - {tid: 2718, name: 斬狂, const: 556, lv: 61, kind: rf, itv: 2.0, base_hp: 120, base_atk: 10, hp: 826, atk: 178}
    - {tid: 2719, name: 幽靈武士, const: 291, lv: 65, kind: m, itv: 1.9, base_hp: 70, base_atk: 18, hp: 974, atk: 344}
    - {tid: 2722, name: 血鋼爪, const: 860, lv: 65, kind: m, itv: 0.9, base_hp: 9, base_atk: 99, hp: 974, atk: 163}
    - {tid: 2720, name: 赤武, const: 771, lv: 66, kind: rf, itv: 2.9, base_hp: 90, base_atk: 19, hp: 1015, atk: 331}
    - {tid: 2721, name: 狂屍, const: 25, lv: 87, kind: m, itv: 2.0, base_hp: 110, base_atk: 25, hp: 2408, atk: 1081}
    - {tid: 2727, name: 狂夜叉(上), const: 783, lv: 101, kind: m, itv: 2.0, base_hp: 100, base_atk: 12, hp: 4284, atk: 2170}
    - {tid: 2726, name: 狂夜叉(下), const: 783, lv: 101, kind: m, itv: 2.0, base_hp: 100, base_atk: 12, hp: 4284, atk: 2170}
    - {tid: 2728, name: 炎武(左), const: 569, lv: 110, kind: f, itv: 1.9, base_hp: 180, base_atk: 18, hp: 6204, atk: 2741}
    - {tid: 2729, name: 炎武(右), const: 569, lv: 110, kind: f, itv: 1.9, base_hp: 180, base_atk: 18, hp: 6204, atk: 2741}
    - {tid: 2730, name: 靈鳳劍仙, const: 629, lv: 120, kind: f, itv: 2.0, base_hp: 220, base_atk: 21, hp: 9362, atk: 4745}
    # ---- 表二群怪：血不動(hp: null)、攻只降不升；Lv鍵依 spec §五 ----
    - {tid: 2723, name: 飛鵬幫先鋒, const: 195, lv: 90, kind: f, itv: 2.0, base_hp: 350, base_atk: 16, hp: null, atk: 1067}
    - {tid: 2724, name: 飛鵬幫戰將, const: 198, lv: 90, kind: f, itv: 2.0, base_hp: 330, base_atk: 21, hp: null, atk: 1067}
    - {tid: 2725, name: 飛鵬幫刀組, const: 474, lv: 81, kind: rf, itv: 2.0, base_hp: 80, base_atk: 11, hp: null, atk: 481}
    - {tid: 3854, name: 渦怪手下, const: 171, lv: 80, kind: m, itv: 2.0, base_hp: 440, base_atk: 17, hp: null, atk: 763}
    - {tid: 4785, name: 紅衣, const: 171, lv: 80, kind: m, itv: 2.0, base_hp: 440, base_atk: 17, hp: null, atk: 763}
    - {tid: 4786, name: 9天, const: 165, lv: 100, kind: rf, itv: 2.0, base_hp: 200, base_atk: 7, hp: null, atk: 397}
    - {tid: 4787, name: 劍勇, const: 765, lv: 90, kind: rf, itv: 1.9, base_hp: 65, base_atk: 9, hp: null, atk: 716}
    - {tid: 4787, name: 劍勇, const: 640, lv: 90, kind: f, itv: 2.0, base_hp: 200, base_atk: 26, hp: null, atk: 1067}
    - {tid: 4788, name: 遊俠, const: 773, lv: 66, kind: rf, itv: 2.9, base_hp: 90, base_atk: 19, hp: null, atk: 331}
    - {tid: 5372, name: 非魂, const: 424, lv: 90, kind: rf, itv: 1.0, base_hp: 180, base_atk: 13, hp: null, atk: 377}
```

- [ ] **Step 2: YAML 可載入驗證**

Run: `cd src/合併 && ../../.venv/bin/python -c "from core.specfile import load_spec; s=load_spec('merge_spec.yaml'); rows=s.params['mob_balance']; print(len(rows)); assert len(rows)==40"`
（在 repo 根執行則 python 路徑為 `.venv/bin/python`、cwd 為 `src/合併`。）
Expected: `40`

- [ ] **Step 3: Commit**

```bash
git add src/合併/merge_spec.yaml
git commit -m "feat(合併): 野怪平衡裁決表 params.mob_balance（40 行）"
```

---

### Task 2: s374 平衡步驟（TDD）

**Files:**
- Create: `src/合併/tests/test_s374_mobbalance.py`
- Create: `src/合併/steps/s374_mobbalance.py`

- [ ] **Step 1: 寫失敗測試**

`src/合併/tests/test_s374_mobbalance.py`：

```python
# -*- coding: utf-8 -*-
"""s374 野怪平衡：格 27 改 hp 差值、格 28 改 atk 差值、全域 28 歸零折入。"""
import pytest
from steps.base import BuildError
from steps.s374_mobbalance import apply_mob_balance

P7 = 7


def _create(f, const, x, y):
    return f._eff(11, source_player=P7, object_list_unit_id=const,
                  location_x=x, location_y=y)


def _hp(f, x, y, qty, const=-1):
    return f._eff(27, source_player=P7, object_list_unit_id=const, quantity=qty,
                  operation=-1, area_x1=x, area_y1=y, area_x2=x, area_y2=y)


def _atk(f, x, y, amount, const=-1):
    e = f._eff(28, source_player=P7, object_list_unit_id=const, quantity=amount,
               operation=2, area_x1=x, area_y1=y, area_x2=x, area_y2=y)
    e.armour_attack_class, e.armour_attack_quantity = 0, amount
    return e


def _gatk(f, amount, const=-1):
    e = f._eff(28, source_player=P7, object_list_unit_id=const, quantity=amount,
               operation=2)
    e.armour_attack_class, e.armour_attack_quantity = 0, amount
    return e


def row(**kw):
    d = dict(tid=0, name='民團', const=74, lv=1, kind='m', itv=2.0,
             base_hp=60, base_atk=17, hp=70, atk=17)
    d.update(kw)
    return d


def test_single_const_rewrites_tile_and_zeroes_global(f):
    t = f.trig(name='民團', tid=0, effects=[
        _create(f, 74, 80, 84), _hp(f, 80, 84, 30), _atk(f, 80, 84, 9),
        _create(f, 74, 77, 80), _hp(f, 77, 80, 30), _atk(f, 77, 80, 9),
        _gatk(f, 20, const=74)])
    tm = f.tm([t])
    changes = apply_mob_balance(tm, [row()])
    assert t.effects[1].quantity == 10 and t.effects[4].quantity == 10          # 70-60
    assert (t.effects[2].armour_attack_class, t.effects[2].armour_attack_quantity) == (0, 0)  # 17-17
    assert t.effects[2].operation == 2
    assert t.effects[6].armour_attack_quantity == 0                              # 全域歸零
    assert changes


def test_two_consts_scoped_by_tile(f):
    t = f.trig(name='劍勇', tid=0, effects=[
        _create(f, 765, 69, 190), _hp(f, 69, 190, 11200, const=765), _atk(f, 69, 190, 975, const=765),
        _create(f, 640, 66, 192), _hp(f, 66, 192, 9750, const=640), _atk(f, 66, 192, 4167, const=640)])
    tm = f.tm([t])
    apply_mob_balance(tm, [
        row(name='劍勇', const=765, base_hp=65, base_atk=9, hp=None, atk=716),
        row(name='劍勇', const=640, base_hp=200, base_atk=26, hp=None, atk=1067)])
    assert t.effects[1].quantity == 11200 and t.effects[4].quantity == 9750     # hp: null 不動
    assert t.effects[2].armour_attack_quantity == 707                            # 716-9
    assert t.effects[5].armour_attack_quantity == 1041                           # 1067-26


def test_name_mismatch_raises(f):
    t = f.trig(name='別的', tid=0, effects=[_create(f, 74, 1, 1), _hp(f, 1, 1, 1), _atk(f, 1, 1, 1)])
    with pytest.raises(BuildError):
        apply_mob_balance(f.tm([t]), [row()])


def test_missing_tile_attack_raises(f):
    t = f.trig(name='民團', tid=0, effects=[_create(f, 74, 1, 1), _hp(f, 1, 1, 1)])
    with pytest.raises(BuildError):
        apply_mob_balance(f.tm([t]), [row()])


def test_negative_delta_raises(f):
    t = f.trig(name='民團', tid=0, effects=[_create(f, 74, 1, 1), _hp(f, 1, 1, 1), _atk(f, 1, 1, 1)])
    with pytest.raises(BuildError):
        apply_mob_balance(f.tm([t]), [row(atk=10)])          # 10 < base_atk 17


def test_duplicate_tile_effects_first_wins_rest_zeroed(f):
    t = f.trig(name='民團', tid=0, effects=[
        _create(f, 74, 1, 1), _hp(f, 1, 1, 5), _hp(f, 1, 1, 25), _atk(f, 1, 1, 4), _atk(f, 1, 1, 5)])
    tm = f.tm([t])
    apply_mob_balance(tm, [row()])
    assert t.effects[1].quantity == 10 and t.effects[2].quantity == 0
    assert t.effects[3].armour_attack_quantity == 0                              # delta 0
    assert t.effects[4].armour_attack_quantity == 0
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `cd src/合併 && ../../.venv/bin/python -m pytest tests/test_s374_mobbalance.py -q`
Expected: FAIL（`ModuleNotFoundError: steps.s374_mobbalance`）

- [ ] **Step 3: 實作步驟**

`src/合併/steps/s374_mobbalance.py`：

```python
# -*- coding: utf-8 -*-
"""s374 野怪平衡曲線（params.mob_balance；spec docs/superpowers/specs/2026-09-01-野怪平衡曲線-design.md）。

生成觸發結構＝每建立點「移除(15)→建立(11)→改血(27,格)→改攻(28,格)」＋部分觸發的全域 28。
本步把每格 27 改成 hp−base_hp、每格 28 改成 atk−base_atk（class0/ADD，同 s30 編碼），
全域 28 一律歸零（總量已折入格效果）。hp: null＝表二血不動。差值必 ≥0（設計夾0）且 ≤int16。
渦鬼(2713/2714)、頭目、表三不入表＝維持現值。觸發名比對防基底位移（同 s373）。"""
from core.change import Change
from .base import trig_by_id, Step, BuildError

CREATE, CH_HP, CH_ATK, P7 = 11, 27, 28, 7
ADD = 2                       # Operation.ADD；假件無 datasets，寫死同 s30 實值


def _tile(e):
    return (e.area_x1, e.area_y1, e.area_x2, e.area_y2)


def _delta(target, base, tag, what):
    d = int(target) - int(base)
    if d < 0:
        raise BuildError(f'缺裁決：{tag} {what} 目標 {target} 低於基礎 {base}（夾0 應在設計表處理）')
    if d > 32767:
        raise BuildError(f'缺裁決：{tag} {what} 差值 {d} 超過 int16')
    return d


def _set_atk(e, amount):
    e.operation = ADD
    e.armour_attack_class = 0
    e.armour_attack_quantity = amount


def apply_mob_balance(tm, rows):
    changes = []
    table = {}
    for r in rows:
        key = (int(r['tid']), int(r['const']))
        if key in table:
            raise BuildError(f'缺裁決：mob_balance (tid,const)={key} 重複')
        table[key] = r
    for tid in sorted({k[0] for k in table}):
        rows_t = {c: r for (t_, c), r in table.items() if t_ == tid}
        name = next(iter(rows_t.values()))['name']
        t = trig_by_id(tm, tid)
        if t is None or (t.name or '') != name:
            raise BuildError(f'缺裁決：T{tid} 名稱「{getattr(t, "name", None)}」≠ 預期「{name}」，'
                             f'基底觸發編號可能位移')
        tag = f'T{tid}「{name}」'
        creates = [e for e in t.effects
                   if getattr(e, 'effect_type', None) == CREATE and getattr(e, 'source_player', -1) == P7
                   and getattr(e, 'location_x', -1) not in (-1, None)]
        present = {e.object_list_unit_id for e in creates}
        if present != set(rows_t):
            raise BuildError(f'缺裁決：{tag} 建立單位 const {sorted(present)} ≠ 表 {sorted(rows_t)}')
        tiles = [( (e.location_x, e.location_y), e.object_list_unit_id) for e in creates]
        if len({xy for xy, _ in tiles}) != len(tiles):
            raise BuildError(f'缺裁決：{tag} 多個建立效果共用同一格，格效果無法歸屬')
        for (x, y), const in tiles:
            r = rows_t[const]
            area = (x, y, x, y)
            scoped = lambda e, et: (getattr(e, 'effect_type', None) == et and _tile(e) == area
                                    and getattr(e, 'object_list_unit_id', -1) in (-1, const))
            hps = [e for e in t.effects if scoped(e, CH_HP)]
            atks = [e for e in t.effects if scoped(e, CH_ATK)]
            if r.get('hp') is not None:
                if not hps:
                    raise BuildError(f'缺裁決：{tag} 格({x},{y}) 無 ChangeHP 效果')
                d = _delta(r['hp'], r['base_hp'], tag, '血')
                old = hps[0].quantity
                hps[0].quantity = d
                for e in hps[1:]:
                    e.quantity = 0
                changes.append(Change('s374', 'effect', f'{tag}({x},{y})', 'hp_add',
                                      str(old), str(d), f"Lv{r['lv']} 目標血 {r['hp']}"))
            if not atks:
                raise BuildError(f'缺裁決：{tag} 格({x},{y}) 無 ChangeAttack 效果')
            d = _delta(r['atk'], r['base_atk'], tag, '攻')
            old = getattr(atks[0], 'armour_attack_quantity', atks[0].quantity)
            _set_atk(atks[0], d)
            for e in atks[1:]:
                _set_atk(e, 0)
            changes.append(Change('s374', 'effect', f'{tag}({x},{y})', 'atk_add',
                                  str(old), str(d), f"Lv{r['lv']} 目標攻 {r['atk']}（{r['kind']}）"))
        for e in t.effects:
            if getattr(e, 'effect_type', None) == CH_ATK and _tile(e) == (-1, -1, -1, -1):
                old = getattr(e, 'armour_attack_quantity', e.quantity)
                _set_atk(e, 0)
                changes.append(Change('s374', 'effect', tag, 'global_atk',
                                      str(old), '0', '全域加攻歸零，總量折入格效果'))
    return changes


class MobBalanceStep(Step):
    id = 's374'
    title = '野怪平衡曲線'
    intro = ('生成觸發加血/加攻改寫為曲線目標（DPS 7.5×1.051^Lv、血 70×1.042^Lv，'
             '遠程×0.7/快速×0.85）；渦鬼與頭目維持現值。無 mob_balance 參數時跳過。')

    def apply(self, ctx):
        rows = ctx.spec.params.get('mob_balance')
        if not rows:
            return []
        return apply_mob_balance(ctx.base.trigger_manager, rows)

    def test_guide(self, changes):
        n = sum(1 for c in changes if c.kind == 'effect')
        return (f'野怪平衡：{n} 筆加值改寫。單人驗（新手體驗）：開場站著給民團打，'
                '約 10–11 刀才倒（現況 4 刀）；Lv9 獵人單下傷害應高於 Lv4 山賊；'
                'Lv87 狂屍應明顯強於 Lv66 赤武（單下約 1,081 vs 331）。\n'
                '陽性對照：渦鬼（珠光/殺人）與首領/豬王/靈象數值不變；'
                '渦鬼殺人仍一刀 2 萬級＝沒有全表誤縮。')


STEP = MobBalanceStep()
```

- [ ] **Step 4: 跑測試確認通過**

Run: `cd src/合併 && ../../.venv/bin/python -m pytest tests/test_s374_mobbalance.py -q`
Expected: 6 passed

- [ ] **Step 5: Commit**

```bash
git add src/合併/steps/s374_mobbalance.py src/合併/tests/test_s374_mobbalance.py
git commit -m "feat(合併): s374 野怪平衡曲線落地（格27/28 改寫＋全域歸零折入）"
```

---

### Task 3: balance_check 特徵斷言（TDD）

**Files:**
- Create: `src/合併/tests/test_balance_check.py`
- Create: `src/合併/analysis/balance_check.py`

- [ ] **Step 1: 寫失敗測試**

`src/合併/tests/test_balance_check.py`：

```python
# -*- coding: utf-8 -*-
"""balance_check：建置後實值對帳＋等級單調（s91 特徵斷言，比照 ferry_check 回傳 (ok,msg) 列表）。"""
from analysis.balance_check import check_mob_balance

P7 = 7


def _create(f, const, x, y):
    return f._eff(11, source_player=P7, object_list_unit_id=const,
                  location_x=x, location_y=y)


def _hp(f, x, y, qty):
    return f._eff(27, source_player=P7, quantity=qty, operation=-1,
                  area_x1=x, area_y1=y, area_x2=x, area_y2=y)


def _atk(f, x, y, amount, cls=0):
    e = f._eff(28, source_player=P7, quantity=amount, operation=2,
               area_x1=x, area_y1=y, area_x2=x, area_y2=y)
    e.armour_attack_class, e.armour_attack_quantity = cls, amount
    return e


ROWS = [
    dict(tid=0, name='民團', const=74, lv=1, kind='m', itv=2.0,
         base_hp=60, base_atk=17, hp=70, atk=17),
    dict(tid=1, name='山賊', const=448, lv=4, kind='f', itv=1.9,
         base_hp=95, base_atk=11, hp=95, atk=14),
]


def _tm(f, hp0=10, atk0=0, hp1=0, atk1=3):
    t0 = f.trig(name='民團', tid=0, effects=[_create(f, 74, 1, 1), _hp(f, 1, 1, hp0), _atk(f, 1, 1, atk0)])
    t1 = f.trig(name='山賊', tid=1, effects=[_create(f, 448, 2, 2), _hp(f, 2, 2, hp1), _atk(f, 2, 2, atk1)])
    return f.tm([t0, t1])


def test_all_ok(f):
    results = check_mob_balance(_tm(f), ROWS)
    assert results and all(ok for ok, _ in results)


def test_tampered_hp_flagged(f):
    results = check_mob_balance(_tm(f, hp0=99), ROWS)
    assert any(not ok and '民團' in msg and '血' in msg for ok, msg in results)


def test_nonzero_attack_class_flagged(f):
    tm = _tm(f)
    tm.triggers[0].effects[2].armour_attack_class = 3
    results = check_mob_balance(tm, ROWS)
    assert any(not ok and 'class' in msg for ok, msg in results)


def test_monotonic_violation_flagged(f):
    rows = [dict(ROWS[0]), dict(ROWS[1])]
    rows[1]['atk'] = 5                       # Lv4 基準DPS 遠低於 Lv1 → 單調破壞
    t0 = f.trig(name='民團', tid=0, effects=[_create(f, 74, 1, 1), _hp(f, 1, 1, 10), _atk(f, 1, 1, 0)])
    t1 = f.trig(name='山賊', tid=1, effects=[_create(f, 448, 2, 2), _hp(f, 2, 2, 0), _atk(f, 2, 2, 0)])
    with_rows = f.tm([t0, t1])
    rows[1]['base_atk'] = 5                  # 差值 0，實值對帳過，單調檢查抓
    results = check_mob_balance(with_rows, rows)
    assert any(not ok and '單調' in msg for ok, msg in results)
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `cd src/合併 && ../../.venv/bin/python -m pytest tests/test_balance_check.py -q`
Expected: FAIL（`ModuleNotFoundError: analysis.balance_check`）

- [ ] **Step 3: 實作**

`src/合併/analysis/balance_check.py`：

```python
# -*- coding: utf-8 -*-
"""野怪平衡特徵斷言（s91 掛載，回傳 [(ok, msg)]，比照 ferry_check）。

從建置後觸發重推實值：血＝base_hp＋Σ格27、攻＝base_atk＋Σ格28＋Σ全域28（class 必為 0），
與 params.mob_balance 目標對帳；另斷言表本身對 Lv 單調（近戰基準 DPS 與血，2% 容差吸收取整）。"""
CREATE, CH_HP, CH_ATK, P7 = 11, 27, 28, 7
DISCOUNT = {'m': 1.0, 'r': 0.7, 'f': 0.85, 'rf': 0.6}


def _tile(e):
    return (e.area_x1, e.area_y1, e.area_x2, e.area_y2)


def check_mob_balance(tm, rows):
    results = []
    by_tid = {}
    for r in rows:
        by_tid.setdefault(int(r['tid']), {})[int(r['const'])] = r
    for tid, rows_t in sorted(by_tid.items()):
        t = tm.triggers_by_id.get(tid) if hasattr(tm, 'triggers_by_id') else tm.triggers[tid]
        name = next(iter(rows_t.values()))['name']
        if t is None or (t.name or '') != name:
            results.append((False, f'T{tid} 名稱不符（預期「{name}」）'))
            continue
        creates = [e for e in t.effects
                   if getattr(e, 'effect_type', None) == CREATE and getattr(e, 'source_player', -1) == P7
                   and getattr(e, 'location_x', -1) not in (-1, None)]
        for ce in creates:
            r = rows_t.get(ce.object_list_unit_id)
            if r is None:
                results.append((False, f'T{tid}「{name}」const {ce.object_list_unit_id} 不在表內'))
                continue
            area = (ce.location_x, ce.location_y, ce.location_x, ce.location_y)
            hp_add = atk_add = 0
            bad_cls = False
            for e in t.effects:
                et = getattr(e, 'effect_type', None)
                scoped = _tile(e) == area and getattr(e, 'object_list_unit_id', -1) in (-1, ce.object_list_unit_id)
                glob = _tile(e) == (-1, -1, -1, -1) and getattr(e, 'object_list_unit_id', -1) in (-1, ce.object_list_unit_id)
                if et == CH_HP and scoped:
                    hp_add += int(e.quantity or 0)
                elif et == CH_ATK and (scoped or glob):
                    if getattr(e, 'armour_attack_class', 0) not in (0, None):
                        bad_cls = True
                    atk_add += int(getattr(e, 'armour_attack_quantity', None) or 0)
            tag = f'T{tid}「{name}」格({ce.location_x},{ce.location_y})'
            if bad_cls:
                results.append((False, f'{tag} 攻擊效果 class≠0'))
            if r.get('hp') is not None:
                ok = r['base_hp'] + hp_add == r['hp']
                results.append((ok, f'{tag} 血 {r["base_hp"]}+{hp_add} {"＝" if ok else "≠"} 目標 {r["hp"]}'))
            ok = r['base_atk'] + atk_add == r['atk']
            results.append((ok, f'{tag} 攻 {r["base_atk"]}+{atk_add} {"＝" if ok else "≠"} 目標 {r["atk"]}'))
    # 表自身單調（僅表一＝hp 非 null；同 Lv 容 2% 取整誤差）
    ladder = sorted((r for r in rows if r.get('hp') is not None), key=lambda r: r['lv'])
    for a, b in zip(ladder, ladder[1:]):
        da = a['atk'] / a['itv'] / DISCOUNT[a['kind']]
        db = b['atk'] / b['itv'] / DISCOUNT[b['kind']]
        ok = db >= da * 0.98 and b['hp'] >= a['hp']
        results.append((ok, f"單調 Lv{a['lv']}{a['name']}→Lv{b['lv']}{b['name']}："
                            f"基準DPS {da:.1f}→{db:.1f}、血 {a['hp']}→{b['hp']}"))
    return results
```

- [ ] **Step 4: 跑測試確認通過**

Run: `cd src/合併 && ../../.venv/bin/python -m pytest tests/test_balance_check.py -q`
Expected: 4 passed

- [ ] **Step 5: Commit**

```bash
git add src/合併/analysis/balance_check.py src/合併/tests/test_balance_check.py
git commit -m "feat(合併): balance_check 實值對帳＋等級單調斷言"
```

---

### Task 4: s91 掛載特徵斷言

**Files:**
- Modify: `src/合併/steps/s91_invariants.py`（`features = {...}` 一帶，約 33–35 行）
- Modify: `src/合併/tests/test_s91.py`（檔尾新增一測試）

- [ ] **Step 1: 寫失敗測試**

在 `src/合併/tests/test_s91.py` 檔尾新增（沿用該檔既有的 ctx／fixture 慣例；若該檔以 `_ctx(f, ...)` 之類 helper 建 BuildContext，照抄其寫法）：

```python
def test_s91_mob_balance_feature_gate(f):
    """params.mob_balance 存在時，s91 須跑 balance_check 並把違規升為 BuildError。"""
    import pytest
    from steps.base import BuildContext, BuildError
    from steps.s91_invariants import STEP
    t = f.trig(name='民團', tid=0, effects=[
        f._eff(11, source_player=7, object_list_unit_id=74, location_x=1, location_y=1)])
    # 無 27/28 效果 → 實值 60+0 ≠ 70 → 違規
    spec = type('S', (), {'params': {'mob_balance': [dict(
        tid=0, name='民團', const=74, lv=1, kind='m', itv=2.0,
        base_hp=60, base_atk=17, hp=70, atk=17)], 'trigger_fixes': []}})()
    ctx = BuildContext(base=type('B', (), {'trigger_manager': f.tm([t])})(),
                       source=None, spec=spec, pairing=None, notes={})
    with pytest.raises(BuildError):
        STEP.apply(ctx)
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `cd src/合併 && ../../.venv/bin/python -m pytest tests/test_s91.py::test_s91_mob_balance_feature_gate -q`
Expected: FAIL（沒有 raise BuildError）

- [ ] **Step 3: 實作掛載**

`src/合併/steps/s91_invariants.py` 中 `features = {'渡船': check_ferry(tm)} if ...` 之後加：

```python
        if ctx.spec.params.get('mob_balance'):
            from analysis.balance_check import check_mob_balance
            features['野怪平衡'] = check_mob_balance(tm, ctx.spec.params['mob_balance'])
```

（與渡船共用其後的 `for tag, results in features.items()` 迴圈，違規自動升 BuildError。）

- [ ] **Step 4: 跑測試確認通過**

Run: `cd src/合併 && ../../.venv/bin/python -m pytest tests/test_s91.py -q`
Expected: 全數 passed（既有測試不得變紅）

- [ ] **Step 5: Commit**

```bash
git add src/合併/steps/s91_invariants.py src/合併/tests/test_s91.py
git commit -m "feat(合併): s91 掛載野怪平衡特徵斷言"
```

---

### Task 5: 全量建置驗證與收尾

**Files:**
- 產出: `src/合併/out/古龍921_合併.aoe2scenario`、`src/合併/logs/`

- [ ] **Step 1: 全套測試**

Run: `cd src/合併 && ../../.venv/bin/python -m pytest -q`
Expected: 全數 passed

- [ ] **Step 2: 全量建置**

Run: `cd src/合併 && ../../.venv/bin/python build.py`
Expected: `s374 野怪平衡曲線: ~300+ 筆異動`（每生成點 hp/atk 各一筆＋全域/區域歸零）、s90/s91 過、`產出：out/古龍921_合併.aoe2scenario`。
若 s374 抛「T{tid} 名稱不符」＝基底觸發位移，回頭核對 merge_spec tid；
若 s91「野怪平衡」違規＝步驟與斷言不一致，先修步驟再建。

- [ ] **Step 3: 抽查建置產物**

Run（repo 根）:
```bash
.venv/bin/python - <<'EOF'
from AoE2ScenarioParser.scenarios.aoe2_de_scenario import AoE2DEScenario
import logging; logging.disable(logging.CRITICAL)
scn = AoE2DEScenario.from_file("src/合併/out/古龍921_合併.aoe2scenario")
tm = scn.trigger_manager
t = tm.triggers[2746]           # 民團
assert t.effects[2].quantity == 10, t.effects[2].quantity
assert t.effects[16].armour_attack_quantity == 0
t = tm.triggers[2721]           # 狂屍：血 2408-110=2298、攻 1081-25=1056
assert t.effects[2].quantity == 2298
assert t.effects[3].armour_attack_quantity == 1056
t = tm.triggers[2714]           # 渦鬼(殺人) 特例不動
assert t.effects[2].quantity != 0
print("抽查 OK")
EOF
```
Expected: `抽查 OK`

- [ ] **Step 4: 部署與使用者測試指引**

Run: `cd src/合併 && ../../.venv/bin/python build.py --deploy`
然後請使用者依 `logs/測試指引.md` s374 段進遊戲驗證（民團 10–11 刀、獵人>山賊、狂屍>>赤武、渦鬼不變＝陽性對照）。

- [ ] **Step 5: Commit（logs 與遷移記錄）**

```bash
git add src/合併/logs
git commit -m "build(合併): s374 野怪平衡曲線首次全量建置"
```

視結果在 `src/新負血劍譜/古龍921_DE遷移記錄.md` 補一段施工記錄（等使用者遊戲實測回報後再記結論）。

---

## Self-Review 記錄

- Spec 覆蓋：§二曲線＋§三折扣（值已烘進裁決表）、§四特例（渦鬼不入表＋夾0 已在表面、負差值防呆）、§五範圍（40 行、表二 hp:null）、§六總表（Task 1 全量）、§七 s91 斷言＋測試指引 → 各有對應 Task。dat 移速覆核改為「kind 寫死於裁決表＋使用者遊戲內抽查」，偏離 spec §三之處已在表註記。
- 佔位掃描：無 TBD／無「補充適當處理」；所有步驟含完整程式碼與預期輸出。
- 型別一致：`apply_mob_balance(tm, rows)`、`check_mob_balance(tm, rows)`、row 欄位 `tid/name/const/lv/kind/itv/base_hp/base_atk/hp/atk` 全計畫一致。
