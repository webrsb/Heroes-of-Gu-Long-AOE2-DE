# 復活與選職業系統 實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

v2（對抗審查修正版）。修正對照：計畫審查 #1–#15、設計審查 H1–H6/M1–M6 全數落任務。

**Goal:** 在古龍921_合併加入遊戲內選職業（6×6 全矩陣）與每職業 10 條命復活系統，並修復五個 DE 圖形佚失 const。

**Architecture:** 全部改動經合併流水線。接線：可追加型效果追加備身 ref＋sp=-1；條件 OR 展開（A 組 spike 把關）；玩家定址觸發家族閉包後 ×6 矩陣；死亡連動走 §七之二 狀態機（不展開、不重啟）；藏兵艙＝1291 預駐＋先換家再移除容器。

**Tech Stack:** Python + AoE2ScenarioParser 0.8.4、pytest、merge_spec.yaml 裁決制。

**Spec:** `docs/superpowers/specs/2026-08-29-復活與選職業-design.md`（v6）

## Global Constraints（鐵律）

- 絕不覆寫來源檔；產物一律 `out/古龍921_合併.aoe2scenario`；絕不從 DE 編輯器存檔
- 只有 `core/scenario_io.py::write_out()` 呼叫 `write_to_file`；legacy_execution_order 屬 **option_manager**（write_out 已處理；spike 檔自行設定時勿寫在 trigger_manager——silent no-op）
- 測試檔必帶陽性對照；merge_spec 缺裁決＝BuildError；一次建置一個 commit；繁中
- 觸發新增一律 append；**複製一律 `copy_trigger(tid, append_after_source=False, add_suffix=False)` 或 deepcopy+append**（預設參數全表重編 id＝禁止）
- 跨步驟傳遞用 `ctx.notes`（BuildContext 無 shared 欄位）
- 新步驟走 auto-registry；實序 s10→s30→s35→s36→s37→s38→s39→s40→s50→s90

## 檔案結構

```
src/合併/
  core/revive_model.py         (新) params.revive 載入驗證
  analysis/revive_inventory.py (新) 五類分類、閉包、死亡連動分類提案、馬系統報告
  steps/s35_misc.py            (改) gate_fix；audit_const_collision（條件+效果雙側）
  steps/s36_attrfix.py         (新) 屬性校正（SET+DIVIDE 兩段式）
  steps/s38_choose.py          (新) 展示（無敵）/備身/容器/職業名
  steps/s39_revive.py          (新) 接線引擎＋矩陣＋選角＋復活鏈＋連動狀態機＋退役斷邊
  steps/s90_audit.py           (改) 七項新稽核
  tests/conftest.py            (新) 共用假件工廠（fake_tm/trig/eff/cond）
  tests/test_revive_model.py test_s35_gate.py test_s36.py test_revive_inventory.py
        test_s38.py test_s39_transform.py test_s39_chain.py test_s90_revive.py
  spikes/spike_final2.py       (新) 六組驗證檔
merge_spec.yaml                (改) const_replacements/gate_fix/attr_fixes/revive
```

## 介面總表

- `ReviveSpec`（T2）：`lives:int, respawn:(float,float)=(81.5,111.5), displays:{cid:(x,y)}, hero_refs:{cid:int}, hero_consts:{cid:int}, retired:[tid]`；`load_revive(params)->ReviveSpec`
- `Inventory`（T6）：`families, eff_list_appendable:[(tid,ei,cid)], eff_list_command:[(tid,ei,cid,etype)], cond_ref, locref, addressed:{cid:set}, matrix:{cid:set}, shared:{cid:set}, death_linked:{cid:set[tid]}, global_init:[tid], const_filtered:[(tid,何處,cid)], cross:[tid]`；`build_inventory(tm, hero_refs, hero_consts, exclude:[tid])->Inventory`
- `LifeRefs`（T8）：`{cid:[ref...]}`＋`containers:{cid:[ref]}`，經 `ctx.notes['revive']` 傳遞
- `build_matrix`（T7）→ `(variant_map:{(tid,slot):tid}, enable_lists:{(c,s):[tid]}, changes)`
- `build_revive_chains`（T9）→ `ChainOut(select/watch/timer/final)`

---

## Phase 0：六組 spike（使用者閘門）

### Task 1: spike_final2 六組驗證檔

**Files:** Create `src/合併/spikes/spike_final2.py`

- [ ] **Step 1: 屬性名映射先查**：`ObjectAttribute` 實名（HIT_POINTS=0、ATTACK=9、MOVEMENT_SPEED=5、`ATTACK_RELOAD_TIME`=10、`ARMOR`=8＋armour_attack_class 編碼——**不存在 RELOAD_TIME/MELEE_ARMOR/PIERCE_ARMOR**，用映射表不做存在性檢查）；`Operation.SET/DIVIDE` 值
- [ ] **Step 2: 寫產生器**，六組各含陽性對照：

```text
A OR混排鑑別：T_mix 條件=[area(P1兵在旗區), destroy(X), OR, destroy(Y)]
  t=10 殺X（兵不在區）→不應觸發；t=15 殺Y（兵仍不在區）→ 錯誤分組 (area∧dX)∨dY 會在此觸發＝FAIL；
  t=25 兵進區 → 應觸發＝PASS。對照：純OR、純AND 各一支。
B P8點選：P8 凍結單位 + OBJECT_SELECTED → 訊息；對照 Gaia 單位。
B2 同單位 OBJECT_SELECTED_MULTIPLAYER(source_player=1) 單人行為。
C 屬性修改：MODIFY_ATTRIBUTE SET+DIVIDE 設 P1 民兵 HP75/攻10/移速88÷100/攻速3÷2；
  對照未修改民兵，懸停比 tooltip＋走路速度目測。
D 駐軍吃效果：1291 容器駐 4 隻 Gaia 兵，t=5 分別對其改名/加攻/加血/傷害（sp=-1+ref），
  t=15 換家+移除容器彈出 → 逐隻驗名字/血量/攻擊 tooltip。對照：地面兵同效果。
E 移除vs摧毀：P1 兵 X 掛 watch [destroy(X)]→訊息A、[另一支 remove X at t=10]；
  另一隻 Y 被 kill 當對照（watch destroy(Y)→訊息B 應出現）。
  觀察：移除後訊息A 出不出＝REMOVE 是否滿足 DESTROY。
F 駐軍算不算區域物件：容器(內駐1兵)放在旗區，watch [OBJECTS_IN_AREA(P0,區)≥1] → 訊息；
  對照：地面 Gaia 兵在另一旗區同條件。
```

- [ ] **Step 3: 部署** `SPIKE六項驗證.aoe2scenario`＋指引更新
- [ ] **Step 4: Commit** `spike(復活): 六組最終驗證`
- [ ] **Step 5: 使用者閘門**——分支表：A敗→T7 use_or=False；B敗→展示 t0 轉 Gaia（先過 T6 區域稽核）；B2敗→選角改走位進圈（回 spec §五 改版）；C敗→回報重議；D敗→**停線回設計**（升級補課模型）；E敗→船塢類逐支改接；F結果→流傳2/容器共域裁決依據

## Phase 1：換皮與數值校正（可獨立交付）

### Task 2: ReviveSpec 載入與驗證

**Files:** Create `src/合併/core/revive_model.py`, Test `tests/test_revive_model.py`

- [ ] **Step 1: 失敗測試**

```python
import pytest
from core.revive_model import load_revive
from steps.base import BuildError

BASE = dict(lives=3, respawn=[81.5, 111.5],
            displays={1: [78.5, 108.5], 2: [80.5, 108.5], 3: [82.5, 108.5],
                      4: [83.5, 109.5], 5: [83.5, 111.5], 6: [83.5, 113.5]},
            hero_refs={1: 0, 2: 1, 3: 2, 4: 502, 5: 7, 6: 45117},
            retired=[1805, 1806, 1807, 1808, 1809, 1810, 3821, 3822, 3823, 3824, 3825, 3826])

def test_load_ok():
    r = load_revive(BASE)
    assert r.lives == 3 and r.displays[5] == (83.5, 111.5) and 3821 in r.retired

def test_missing_display_raises():
    bad = dict(BASE, displays={k: v for k, v in BASE['displays'].items() if k != 5})
    with pytest.raises(BuildError):
        load_revive(bad)

def test_lives_too_small_raises():
    with pytest.raises(BuildError):
        load_revive(dict(BASE, lives=1))
```

- [ ] **Step 2:** pytest FAIL → **Step 3: 實作**（dataclass；hero_consts 建置時由單位表補入）→ **Step 4:** PASS → **Step 5: Commit**

### Task 3: s35 擴充——換皮＋城門＋碰撞稽核

**Files:** Modify `steps/s35_misc.py`, `merge_spec.yaml`; Test `tests/test_s35_gate.py`

- [ ] **Step 1: merge_spec**：const_replacements 加 `{94→1811,expect_count:3}`、`{37→329,expect_count:12}`、`{573→74,expect_count:1}`；`gate_fix: {remove_const:95, expect_count:6, gate_const:<Step2查定>, player:8}`——**座標不入 spec**，由構件包絡推導（實物在 x=225.5，v1 計畫寫 226.5 是錯的）
- [ ] **Step 2: 查石門 const**：halfon 列 Gate/type80 條目，選沿 x=225 牆線（y 軸向）朝向；記錄佔格
- [ ] **Step 3: 失敗測試**

```python
import pytest
from types import SimpleNamespace as NS
from steps.s35_misc import apply_gate_fix
from steps.base import BuildError

ENTRY = dict(remove_const=95, expect_count=6, gate_const=64, player=8)

def U(ref, const, x, y):
    return NS(reference_id=ref, unit_const=const, x=x, y=y)

def make_um(units8):
    um = NS(units=[list(units8) if p == 8 else [] for p in range(9)], added=[])
    um.add_unit = lambda **kw: (um.added.append(kw), NS(reference_id=99999, **kw))[1]
    return um

def test_gate_fix_derives_center_removes_six():
    um = make_um([U(i, 95, 225.5, 11.5 if i < 3 else 14.5) for i in range(6)])
    changes = apply_gate_fix(um, ENTRY)
    assert len(um.added) == 1 and um.added[0]['unit_const'] == 64
    assert abs(um.added[0]['x'] - 225.5) < 0.6 and abs(um.added[0]['y'] - 13.0) < 1.6
    assert sum(1 for c in changes if c.kind == 'unit_remove') == 6

def test_gate_fix_wrong_count_raises():
    um = make_um([U(1, 95, 225.5, 11.5)])
    with pytest.raises(BuildError):
        apply_gate_fix(um, ENTRY)
```

- [ ] **Step 4:** FAIL → **Step 5: 實作**（unit_manager 實 API 移除；中心=構件座標包絡中點）→ PASS
- [ ] **Step 6: 碰撞稽核** `audit_const_collision(tm, um, new_consts)`：**條件 object_list ＋效果 object_list_unit_id 雙側**掃描＋區域交集。測試：合成 `REMOVE_OBJECT(olu=329, sp=7)` 與換皮單位同座標 → 告警
- [ ] **Step 7: Commit**

### Task 4: s36 屬性校正

**Files:** Create `steps/s36_attrfix.py`, Test `tests/test_s36.py`; Modify `merge_spec.yaml`

- [ ] **Step 1: merge_spec attr_fixes**（分數屬性以 `[分子, 分母]` 表達）：

```yaml
attr_fixes:
  - {unit_const: 1811, players: [1,2,3,4,5,6,7,8], reason: 拳原版94值,
     attrs: {hit_points: 75, attack_c4: 10, armor_c4: 0, armor_c3: 0,
             attack_reload_time: [3, 2], movement_speed: [88, 100]}}
  - {unit_const: 329, players: [7,8], reason: 37原版值, attrs: {hit_points: 110, attack_c4: 7}}
  - {unit_const: 74, players: [8], reason: 573原版·攻歸0·波及P8既存民兵ref24947已裁決豁免,
     attrs: {hit_points: 45, attack_c4: 0}}
  - {unit_const: <gate_const>, players: [8], reason: 城門原版血, attrs: {hit_points: 2750}}
```

- [ ] **Step 2: 失敗測試**：整數屬性一條 SET 效果；分數屬性 SET＋DIVIDE 兩條且順序相鄰；效果總數=Σ(players×(整數屬性+2×分數屬性))；attribute id 映射正確（attack_reload_time→ATTACK_RELOAD_TIME=10、armor_cN→ARMOR+class）
- [ ] **Step 3: 實作** → PASS → **Step 4: Commit**

### Task 5: Phase 1 建置與實測（使用者閘門）

- [ ] build → 全 pytest → 部署 → 使用者驗：拳/駱駝/民兵NPC/城門現形、tooltip 校正值（含攻速/移速體感）、817 仍在、37 怪攻擊正常（對照組）→ commit＋遷移記錄

## Phase 2：盤點與接線引擎

### Task 6: revive_inventory 五類盤點＋裁決表產出（使用者閘門）

**Files:** Create `analysis/revive_inventory.py`, Test `tests/test_revive_inventory.py`, `tests/conftest.py`

分類規則（spec §六）：
- eff_list_appendable：sel 含本體 ref 且 effect_type ∈{改名26,傷害24,改攻28,改血27}
- eff_list_command：sel 含本體 ref 且 effect_type ∈{STOP 29,FREEZE 22,REMOVE 15,TASK 12}
- addressed：條件玩家欄∈1..6 ∨ 效果 target_player∈1..6（**不論 sel**）∨（效果 source_player∈1..6 ∧ sel 空）
- global_init：無條件（或純 timer≤1）且玩家欄涵蓋 1..6 全包 → 強制 shared，永不進 matrix
- matrix：addressed（扣 global_init、retired）沿家族內 activate/deactivate 閉包
- death_linked：條件含 destroy(本體ref)（扣 retired）；輸出 54 行分類提案（looping→b、
  被任務鏈啟停→a＋旗標閘門、终死型→c、其餘→提案後定）
- const_filtered：條件 object_list 或效果 object_list_unit_id ∈ 本體 const 的**非家族**觸發
- retired 排除：build_inventory(exclude=retired) 於一切分類之前剔除

- [ ] **Step 1: conftest 工廠**（trig/eff/cond/fake_tm 以 SimpleNamespace，介面照 test_s37.py 慣例）
- [ ] **Step 2: 失敗測試**

```python
def test_closure_pulls_activator_into_matrix(f):
    t0 = f.trig(effects=[f.eff_rename(sel=[0])])                      # 純ref → shared
    t1 = f.trig(effects=[f.eff_chat(sp=1)])                           # addressed
    t2 = f.trig(effects=[f.eff_activate(t1.trigger_id), f.eff_rename(sel=[0])])
    inv = build_inventory(f.tm([t0, t1, t2]), {1: 0}, {1: 845}, exclude=[])
    assert t1.trigger_id in inv.matrix[1] and t2.trigger_id in inv.matrix[1]
    assert t0.trigger_id in inv.shared[1]

def test_target_player_with_sel_is_addressed(f):
    t = f.trig(effects=[f.eff_ownership(sp=8, tp=6, sel=[29332])])    # 哥8 型
    inv = build_inventory(f.tm([t]), {6: 45117}, {6: 765}, exclude=[])
    assert t.trigger_id in inv.addressed[6]

def test_global_init_forced_shared(f):
    t = f.trig(conds=[], effects=[f.eff_task(sp=p) for p in range(1, 7)], enabled=True)
    inv = build_inventory(f.tm([t]), HERO_REFS, HERO_CONSTS, exclude=[])
    assert t.trigger_id in inv.global_init and all(t.trigger_id not in inv.matrix[c] for c in range(1, 7))

def test_retired_excluded_everywhere(f):
    t = f.trig(conds=[f.cond_destroy(0)], effects=[f.eff_create(sp=1)])
    inv = build_inventory(f.tm([t]), {1: 0}, {1: 845}, exclude=[t.trigger_id])
    assert all(t.trigger_id not in s for s in
               (inv.death_linked.get(1, set()), inv.matrix.get(1, set()), inv.shared.get(1, set())))

def test_const_filtered_nonfamily(f):
    t = f.trig(effects=[f.eff_remove(olu=765, sp=6, area=(57, 187, 73, 196))])  # 船塢型
    inv = build_inventory(f.tm([t]), {6: 45117}, {6: 765}, exclude=[])
    assert (t.trigger_id, 'effect', 6) in inv.const_filtered
```

- [ ] **Step 3:** FAIL → 實作 → PASS
- [ ] **Step 4: 實跑報表** `reports/復活盤點.md`：五類數量／死亡連動 54 行分類提案表／const_filtered 全清單（船塢型標紅）／**馬系統鏈路報告**（馬召/收馬/馬2/馬3 的完整觸發網絡，含 6 處 REMOVE 本體的上下游）／X死退役斷邊清單（指向 12 支的 activate/deactivate 全列，實查含轉生 X51×6、馬2 系列）／cross 重跑數字／總量預估（公式含 OR 退路 +360×(lives−1)、啟動器切分、locref 按觸發數）——**>30,000 熔斷停線**
- [ ] **Step 5: 使用者閘門**：死亡連動 54 行裁決、const_filtered 裁決、馬系統裁決、轉生訊息世代裁決（spec 待裁決①）→ 全部寫入 merge_spec `revive.*_rulings`
- [ ] **Step 6: Commit**

### Task 7: s39 前半——接線改造引擎

**Files:** Create `steps/s39_revive.py`, Test `tests/test_s39_transform.py`

Produces：`append_life_refs(tm, inv, life_refs)`（僅 appendable；含 matrix 觸發於複製前處理）、
`expand_conditions(tm, inv, life_refs, use_or)`（death_linked 除外）、
`dup_locref(tm, inv, life_refs) -> locref_copies:{(cid,L):[tid]}`（供鏈啟停）、
`build_matrix(tm, inv, life_refs)`（複製用 deepcopy+append 或 copy_trigger 非預設參數；
玩家欄 值==cid→slot 含 **target_player**；內部 activate/deactivate 重指同 slot 變體；
enable_lists=(原 enabled==True 者)）。

- [ ] **Step 1: 失敗測試**

```python
def test_append_only_appendable_types(f):
    e_ok = f.eff_rename(sel=[0]); e_cmd = f.eff_stop(sel=[0])
    t = f.trig(effects=[e_ok, e_cmd])
    tm = f.tm([t])
    append_life_refs(tm, f.inv(eff_app=[(t.trigger_id, 0, 1)], eff_cmd=[(t.trigger_id, 1, 1, 29)]),
                     {1: [0, 900, 901]})
    assert e_ok.selected_object_ids == [0, 900, 901] and e_ok.source_player == -1
    assert e_cmd.selected_object_ids == [0]            # 指令型不追加

def test_or_expansion_sequence(f):
    c = f.cond_bring_area(unit_object=0)
    t = f.trig(conds=[c]); tm = f.tm([t])
    expand_conditions(tm, f.inv(cond=[(t.trigger_id, 0, 'unit_object', 1)]), {1: [0, 900]}, use_or=True)
    assert [x.condition_type for x in t.conditions] == [c.condition_type, 29, c.condition_type]

def test_death_linked_not_expanded(f):
    c = f.cond_destroy(0); t = f.trig(conds=[c]); tm = f.tm([t])
    inv = f.inv(cond=[(t.trigger_id, 0, 'unit_object', 1)], death_linked={1: {t.trigger_id}})
    expand_conditions(tm, inv, {1: [0, 900]}, use_or=True)
    assert len(t.conditions) == 1

def test_matrix_rewrites_sp_and_tp(f):
    t = f.trig(effects=[f.eff_chat(sp=2), f.eff_ownership(sp=8, tp=2, sel=[7])])
    tm = f.tm([t])
    vm, el, _ = build_matrix(tm, f.inv(matrix={2: {t.trigger_id}}), {2: [1]})
    v = tm.triggers_by_id[vm[(t.trigger_id, 5)]]
    assert v.effects[0].source_player == 5
    assert v.effects[1].source_player == 8 and v.effects[1].target_player == 5

def test_matrix_repoints_internal_activation(f):
    a = f.trig(effects=[f.eff_chat(sp=1)])
    b = f.trig(effects=[f.eff_activate(a.trigger_id), f.eff_chat(sp=1)])
    tm = f.tm([a, b])
    vm, el, _ = build_matrix(tm, f.inv(matrix={1: {a.trigger_id, b.trigger_id}}), {1: [0]})
    vb = tm.triggers_by_id[vm[(b.trigger_id, 4)]]
    assert vb.effects[0].trigger_id == vm[(a.trigger_id, 4)]

def test_enable_lists_only_originally_enabled(f):
    a = f.trig(enabled=True, effects=[f.eff_chat(sp=1)])
    b = f.trig(enabled=False, effects=[f.eff_chat(sp=1)])
    tm = f.tm([a, b])
    vm, el, _ = build_matrix(tm, f.inv(matrix={1: {a.trigger_id, b.trigger_id}}), {1: [0]})
    assert el[(1, 3)] == [vm[(a.trigger_id, 3)]]
```

- [ ] **Step 2:** FAIL → 實作 → PASS（全部既有測試綠）→ **Step 3: Commit**

## Phase 3：選角、復活鏈與稽核

### Task 8: s38 展示/備身/容器

**Files:** Create `steps/s38_choose.py`, Test `tests/test_s38.py`; Modify `merge_spec.yaml`（revive 入檔）

動作：本體→P8＋展示位；職業名萃取（失敗 BuildError）；備身 6×(lives−1)＋容器（1291/Gaia）疊 respawn；
t=0 觸發：改名/凍結/**無敵化**（HP 凍結迴圈或屬性法——T1-C 結果定）、備身 P8→Gaia；
展示位 vs t=0 生成物碰撞檢查（T160 const606 於 (83,109) 等——衝突即 BuildError 請裁決）；
產出 `ctx.notes['revive']`。

- [ ] 失敗測試：備身/容器數量與對應、展示座標、P5 const==94 時 BuildError（s35 順序防呆）、606 碰撞 BuildError 案例 → 實作 → PASS → Commit

### Task 9: s39 後半——選角＋復活鏈＋連動狀態機＋退役斷邊

**Files:** Modify `steps/s39_revive.py`, Test `tests/test_s39_chain.py`

per (C,S)：選角（條件型別依 T1-B/B2 定案；效果＝轉家/解凍/解無敵/啟動 enable_lists（啟動器每支≤200 效果）/啟動 watch1/互斥停用/訊息）；
watch(C,S,L)（一次性、鏈式啟動）＝訊息＋**連動狀態切換（依 T6 裁決表：a 型 activate 變體、b 型 activate）**＋啟動 timer；
timer(C,S,L)＝換家＋移除容器＋locref 拷貝啟停＋**b 型連動 deactivate**＋啟動 watch(L+1)；
final＝訊息＋revealer。
退役斷邊：retired 12 支 enabled=0＋改名「退役_」＋**全圖指向它們的 activate/deactivate 效果改接**
（轉生 X51 → 依裁決①改接訊息世代切換或移除該效果）；
a 型連動的任務閘門邊改旗標單位（create/remove 於角落格，格址由 merge_spec 配發）。

- [ ] 失敗測試（縮小模型 lives=3、2C×2S）：

```python
def test_chain_links(f):
    out = build_revive_chains(...)
    w1 = tmx.triggers_by_id[out.watch[(1, 2, 1)]]
    assert w1.enabled == 0 and w1.looping == 0
    t1 = tmx.triggers_by_id[out.timer[(1, 2, 1)]]
    assert any(e.effect_type == 18 and e.target_player == 2 for e in t1.effects)
    assert any(e.effect_type == 15 for e in t1.effects)

def test_b_type_linkage_window(f):
    # b型連動：watch activate、timer deactivate
    out = build_revive_chains(...)
    w = tmx.triggers_by_id[out.watch[(1, 2, 1)]]
    t = tmx.triggers_by_id[out.timer[(1, 2, 1)]]
    assert any(e.effect_type == 8 and e.trigger_id == B_TYPE_VARIANT for e in w.effects)
    assert any(e.effect_type == 9 and e.trigger_id == B_TYPE_VARIANT for e in t.effects)

def test_retire_cuts_edges(f):
    # 合成：X51 activate 1805 → 斷邊後該效果不再指向 1805
    build_revive_chains(...)
    x51 = tmx.triggers_by_id[1690]
    assert all(e.trigger_id != 1805 for e in x51.effects if e.effect_type in (8, 9))

def test_selection_mutual_exclusion(f):
    out = build_revive_chains(...)
    sel = tmx.triggers_by_id[out.select[(1, 2)]]
    deact = {e.trigger_id for e in sel.effects if e.effect_type == 9}
    assert out.select[(1, 3)] in deact and out.select[(2, 2)] in deact
```

- [ ] FAIL → 實作（Step 類串流程；test_guide 產單人驗收清單；**逐觸發發 Change**——s90 對帳按 trigger_add 計數）→ PASS → Commit

### Task 10: s90 七項稽核

**Files:** Modify `steps/s90_audit.py`, Test `tests/test_s90_revive.py`

- [ ] 每項一例壞資料必 BuildError：
  1. appendable eff_list 缺任一命 ref
  2. enable_lists tid 不存在或初值≠disabled
  3. 建檔 Gaia 軍事單位（1291 豁免）
  4. 換皮碰撞（條件＋效果雙側）無豁免
  5. 玩家定址漏網（shared 觸發殘留 1..6 玩家欄：**target_player 不論 sel；source_player 限 sel 空**）
  6. 未選家族的 enabled 死亡連動（展示英雄被殺誤發防線；配合展示無敵）
  7. 展示位/重生點的區域條件覆蓋清單（報表供裁決，未裁決項 BuildError）
- [ ] FAIL → 實作 → PASS → Commit

## Phase 4：建置與實測

### Task 11: lives=3 建置＋單人實測（使用者閘門）

- [ ] build（`revive.lives: 3`）→ 全 pytest → 部署
- [ ] 驗收：選角互斥 → **第 2 命找職業 NPC 升級** → 打坐跨命 → 連動狀態機（死亡視窗魂移生效、重生即停；哥8 依任務閘門）→ 10 秒延遲 → 命盡 → 未選職業鎖定 → **存檔/讀檔迴圈（死亡→存→讀→重生）** → 陽性對照
- [ ] 修復循環 → 遷移記錄＋commit

### Task 12: 多人與全量

- [ ] 實際總量若 >30,000 → 重壓測後才續
- [ ] 多人：選角同步、**同 tick 搶選**、重生歸屬、1811 無 DLC 端顯示
- [ ] `lives: 10` 全量 → 部署 → 長線回歸 → 遷移記錄結案＋commit
