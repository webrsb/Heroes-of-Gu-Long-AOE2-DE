# 合併流水線 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建置單一入口流水線 `src/合併/build.py`：從兩個 origin 基底出發，執行 盤點→搬運→修復→施工 各步驟，每步自動產異動紀錄與測試指引，可重跑重建成品。

**Architecture:** 純函式 steps（吃 scenario 物件回傳 `Change` 清單）＋唯一寫檔點 `scenario_io.py`。分析層（配對/分群/依賴度）獨立於建置層，先產盤點報告供使用者圈選，圈選寫入 `merge_spec.yaml` 後 build 才有東西搬。所有 log 固定檔名，git 承擔歷史。

**Tech Stack:** Python 3.12、AoE2ScenarioParser 0.8.4、PyYAML、pytest。

**Spec:** `docs/superpowers/specs/2026-08-29-合併流水線-design.md`

## Global Constraints

- **絕不覆寫 `src/origin/` 兩檔**；產出只寫 `src/合併/out/`（不進 git）。
- **全專案只有 `core/scenario_io.py` 的 `write_out()` 呼叫 `write_to_file`**；任何 step 不得自行寫檔。
- 寫檔前必設 `scenario.option_manager.legacy_execution_order = True`（0.8.4 實測此屬性在 option_manager 上）。
- **絕不從 DE 編輯器開啟後存檔**（會把刻意寫入的溢位值正規化）。
- caption 文字只能用 ASCII＋中日韓字元，百分比用**半形 `%`**（全形％與方塊字元會變豆腐）。
- **build 不做沉默裁決**：遇到 merge_spec 未覆蓋的衝突，raise `BuildError` 列出缺哪條裁決，絕不默默選邊（spec §7a）。
- 異動紀錄固定檔名（`logs/10_自動搬運.tsv` 等），每次建置覆寫，一輪建置一個 commit。
- 兩個 origin 檔各需 ~20 秒解析，**涉及真實劇本檔的測試一律標 `@pytest.mark.slow`**，用 module/session-scope fixture 只載一次。
- 測試指令：`cd src/合併 && python -m pytest tests/ -v`（預設跳過 slow：pytest.ini 設 `addopts = -m "not slow"`；要跑 slow 用 `-m slow`）。
- 中文檔名/路徑在 Bash heredoc 裡會被反斜線轉義弄壞——**寫檔一律用 Write/Edit 工具或 Python 腳本檔，路徑用正斜線**。

## 檔案地圖

```
src/合併/
├── build.py                 CLI：--report / --until sNN / --deploy
├── merge_spec.yaml          圈選規格（Task 19 建立 fix-only 初版）
├── pytest.ini
├── core/
│   ├── __init__.py
│   ├── change.py            Change dataclass、TSV 序列化        (Task 1)
│   ├── logging_out.py       機器 TSV＋人讀 md 產生器            (Task 2)
│   ├── testguide.py         測試指引產生器                      (Task 3)
│   ├── specfile.py          merge_spec.yaml 載入與驗證          (Task 4)
│   └── scenario_io.py       唯一寫檔點、origin 載入、deploy     (Task 6)
├── steps/
│   ├── __init__.py          STEPS 註冊表（依 id 排序）
│   ├── base.py              Step 基底、BuildContext、BuildError (Task 5)
│   ├── s10_transplant.py    自動搬運器                          (Task 13)
│   ├── s30_attackfix.py     攻擊力修復                          (Task 14)
│   ├── s40_boss_hp.py       雙0血                               (Task 15)
│   ├── s50_healthbar.py     caption 血條                        (Task 16)
│   ├── s60_misc.py          雜項替換（436→2353 型）             (Task 17)
│   └── s90_audit.py         終檢不變量                          (Task 18)
├── analysis/
│   ├── __init__.py
│   ├── fingerprint.py       觸發內容指紋                        (Task 8)
│   ├── pairing.py           兩版觸發配對                        (Task 9)
│   ├── grouping.py          啟動圖分群                          (Task 10)
│   ├── dependency.py        依賴度評估                          (Task 11)
│   └── inventory.py         盤點報告組裝                        (Task 12)
├── reports/                 盤點報告輸出
├── logs/                    異動紀錄＋測試指引輸出
├── out/                     .aoe2scenario 產出（gitignore）
└── tests/
```

s20 手寫補丁不在本計畫內：要等盤點報告＋使用者圈選後才知道有哪些糾纏組，屆時逐支補丁按 `steps/base.py` 介面新增（檔名 `s2N_patch_<組名>.py`，一組一支，接線決策寫 docstring）。

背景知識（實作者必讀的專案文件）：

- `src/新負血劍譜/古龍921_DE遷移記錄.md` §3.3e（攻擊力公式）、§5.6/§5.7（雙0血）、§5.7a（血條）
- `src/新負血劍譜/fix_attack.py`（已實測可用的攻擊力修復實作，Task 14 直接移植）
- `src/新負血劍譜/audit_buildings.py` 的 `targets()`（效果目標三模式解析，Task 11/18 移植）
- `src/新負血劍譜/verify/01s_魔王血條百分比.md`、`verify/15_血條顯示方式.md`（血條觸發的實測形狀，Task 16 對照）
- AoE2ScenarioParser 0.8.4 關鍵 API（已核對）：
  - `AoE2DEScenario.from_file(path)`、`scenario.write_to_file(path)`
  - `tm = scenario.trigger_manager`；`tm.triggers[i]`、`tm.add_trigger(name, enabled=…, looping=…)`
  - **`tm.import_triggers(triggers, deepcopy=True)`**：自動重映射搬入集合內部的 (de)activate `trigger_id`；**指向集合外的引用會被 warn 並重設為 -1** —— s10 必須先記錄跨界引用、匯入後回填
  - `scenario.unit_manager.add_unit(player, unit_const, x, y, z, rotation, reference_id=…)`；`unit_manager.units[player]` 為 9 個玩家清單（0=Gaia）
  - `scenario.option_manager.legacy_execution_order`（bool，1.55+ 才有，本檔 1.58 有）
  - 效果欄位：`effect_type`、`quantity`、`trigger_id`、`selected_object_ids`、`location_object_reference`、`object_list_unit_id`、`source_player`、`target_player`、`area_x1/y1/x2/y2`、`operation`、`message`、`armour_attack_quantity`、`armour_attack_class`
  - `from AoE2ScenarioParser.datasets.effects import EffectId`、`from AoE2ScenarioParser.datasets.conditions import ConditionId`

---

### Task 1: core/change.py — 異動資料結構

**Files:**
- Create: `src/合併/core/__init__.py`（空檔）
- Create: `src/合併/core/change.py`
- Test: `src/合併/tests/test_change.py`
- Create: `src/合併/pytest.ini`

**Interfaces:**
- Produces: `Change(step: str, kind: str, target: str, field: str, old: str, new: str, reason: str)` dataclass；`TSV_HEADER: str`；`to_tsv_row(c: Change) -> str`。kind 慣用值：`trigger_add`、`remap`、`effect`、`unit_add`、`unit_field`、`param`、`audit`。所有後續 Task 的 step 都回傳 `list[Change]`。

- [ ] **Step 1: 建 pytest.ini 與空 __init__.py**

`src/合併/pytest.ini`：

```ini
[pytest]
addopts = -m "not slow"
markers =
    slow: 需要載入真實劇本檔（每檔約 20 秒）
```

- [ ] **Step 2: 寫失敗測試**

`src/合併/tests/test_change.py`：

```python
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from core.change import Change, TSV_HEADER, to_tsv_row

def test_tsv_row_basic():
    c = Change(step='s30', kind='effect', target='T210 效果0', field='quantity',
               old='16646156', new='+65036(class0)', reason='§3.3e 還原公式')
    assert to_tsv_row(c) == 's30\teffect\tT210 效果0\tquantity\t16646156\t+65036(class0)\t§3.3e 還原公式'

def test_tsv_header_matches_field_order():
    assert TSV_HEADER == 'step\tkind\ttarget\tfield\told\tnew\treason'

def test_tsv_escapes_tabs_and_newlines():
    c = Change('s10', 'effect', 'T1', 'message', 'a\tb', 'c\nd', 'r')
    row = to_tsv_row(c)
    assert '\t' in row and row.count('\t') == 6  # 只有 6 個分隔 tab
    assert '\n' not in row
```

- [ ] **Step 3: 跑測試確認失敗**

Run: `cd src/合併 && python -m pytest tests/test_change.py -v`
Expected: FAIL（ModuleNotFoundError: core）

- [ ] **Step 4: 實作**

`src/合併/core/change.py`：

```python
# -*- coding: utf-8 -*-
"""統一異動資料結構。所有 step 的產出、log 與測試指引的唯一來源。"""
from dataclasses import dataclass

TSV_HEADER = 'step\tkind\ttarget\tfield\told\tnew\treason'

@dataclass
class Change:
    step: str    # 's10'
    kind: str    # trigger_add / remap / effect / unit_add / unit_field / param / audit
    target: str  # 'T5599「幫眾」效果3' / 'ref31245 聚魂塔'
    field: str   # 'trigger_id' / 'quantity' / '—'
    old: str
    new: str
    reason: str  # 規格出處或 merge_spec 條目

def _esc(s: str) -> str:
    return str(s).replace('\t', '␉').replace('\n', '␊')

def to_tsv_row(c: Change) -> str:
    return '\t'.join(_esc(v) for v in (c.step, c.kind, c.target, c.field, c.old, c.new, c.reason))
```

- [ ] **Step 5: 跑測試確認通過**

Run: `cd src/合併 && python -m pytest tests/test_change.py -v`
Expected: 3 PASS

- [ ] **Step 6: Commit**

```bash
git add src/合併/core src/合併/tests src/合併/pytest.ini
git commit -m "feat(合併): Change 異動資料結構與 TSV 序列化"
```

---

### Task 2: core/logging_out.py — 雙軌異動紀錄

**Files:**
- Create: `src/合併/core/logging_out.py`
- Test: `src/合併/tests/test_logging_out.py`

**Interfaces:**
- Consumes: `Change`, `TSV_HEADER`, `to_tsv_row`（Task 1）
- Produces: `write_step_logs(step_id: str, title: str, intro: str, changes: list[Change], logs_dir: Path) -> tuple[Path, Path]` — 寫 `logs/{NN}_{title}.tsv` 與 `.md`（NN = step_id 去掉 s 前綴），回傳兩路徑。**固定檔名，直接覆寫**。changes 為空也要寫檔（TSV 只有表頭、md 記「本輪無異動」），git diff 才看得出「從有變無」。

- [ ] **Step 1: 寫失敗測試**

`src/合併/tests/test_logging_out.py`：

```python
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from pathlib import Path
from core.change import Change
from core.logging_out import write_step_logs

def _c():
    return Change('s30', 'effect', 'T210 效果0', 'quantity', '16646156', '+65036', '§3.3e')

def test_writes_tsv_and_md(tmp_path):
    tsv, md = write_step_logs('s30', '攻擊力修復', '對全檔跑還原公式', [_c()], tmp_path)
    assert tsv.name == '30_攻擊力修復.tsv' and md.name == '30_攻擊力修復.md'
    lines = tsv.read_text(encoding='utf-8').splitlines()
    assert lines[0].startswith('step\t') and len(lines) == 2
    body = md.read_text(encoding='utf-8')
    assert '攻擊力修復' in body and 'effect: 1' in body and '§3.3e' in body

def test_empty_changes_still_writes(tmp_path):
    tsv, md = write_step_logs('s60', '雜項替換', '…', [], tmp_path)
    assert tsv.read_text(encoding='utf-8').strip().startswith('step\t')
    assert '本輪無異動' in md.read_text(encoding='utf-8')

def test_overwrites_same_filename(tmp_path):
    write_step_logs('s30', '攻擊力修復', 'x', [_c(), _c()], tmp_path)
    tsv, _ = write_step_logs('s30', '攻擊力修復', 'x', [_c()], tmp_path)
    assert len(tsv.read_text(encoding='utf-8').splitlines()) == 2
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `cd src/合併 && python -m pytest tests/test_logging_out.py -v`
Expected: FAIL（no module logging_out）

- [ ] **Step 3: 實作**

```python
# -*- coding: utf-8 -*-
"""雙軌異動紀錄：機器 TSV ＋ 人讀 md。固定檔名，git 承擔歷史。"""
from collections import Counter
from pathlib import Path
from .change import Change, TSV_HEADER, to_tsv_row

def write_step_logs(step_id: str, title: str, intro: str,
                    changes: list, logs_dir: Path):
    logs_dir = Path(logs_dir); logs_dir.mkdir(parents=True, exist_ok=True)
    nn = step_id.lstrip('s')
    tsv_p = logs_dir / f'{nn}_{title}.tsv'
    md_p = logs_dir / f'{nn}_{title}.md'
    tsv_p.write_text('\n'.join([TSV_HEADER] + [to_tsv_row(c) for c in changes]) + '\n',
                     encoding='utf-8')
    kinds = Counter(c.kind for c in changes)
    reasons = sorted({c.reason for c in changes})
    lines = [f'# {step_id} {title}', '', intro, '']
    if not changes:
        lines.append('**本輪無異動。**')
    else:
        lines.append(f'共 **{len(changes)}** 筆異動：')
        lines += [f'- {k}: {n}' for k, n in kinds.most_common()]
        lines += ['', '依據：'] + [f'- {r}' for r in reasons]
    md_p.write_text('\n'.join(lines) + '\n', encoding='utf-8')
    return tsv_p, md_p
```

- [ ] **Step 4: 跑測試確認通過**

Run: `cd src/合併 && python -m pytest tests/test_logging_out.py -v`
Expected: 3 PASS

- [ ] **Step 5: Commit**

```bash
git add src/合併/core/logging_out.py src/合併/tests/test_logging_out.py
git commit -m "feat(合併): 雙軌異動紀錄產生器（固定檔名）"
```

---

### Task 3: core/testguide.py — 測試指引產生器

**Files:**
- Create: `src/合併/core/testguide.py`
- Test: `src/合併/tests/test_testguide.py`

**Interfaces:**
- Consumes: `Change`（Task 1）
- Produces: `StepResult(step_id: str, title: str, changes: list[Change], guide: str | None)` dataclass；`render_guide(results: list[StepResult]) -> str`。guide 是 step 自己代入實際改動後的文字段（`None` 或空 changes ⇒ 標「本輪無異動，免測」）。輸出整份 `測試指引.md` 內容字串（呼叫方負責寫檔）。

- [ ] **Step 1: 寫失敗測試**

```python
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from core.change import Change
from core.testguide import StepResult, render_guide

def test_guide_contains_step_sections():
    r = [StepResult('s40', '雙0血', [Change('s40','unit','ref31245 聚魂塔','hp','—','雙0血','§5.7')],
                    '怎麼測：派兵攻擊聚魂塔\n預期：血量實際下降'),
         StepResult('s60', '雜項替換', [], None)]
    out = render_guide(r)
    assert '### 驗 s40 雙0血' in out
    assert '派兵攻擊聚魂塔' in out
    assert 's60' in out and '本輪無異動，免測' in out

def test_zero_change_step_marked_skip_even_with_guide_text():
    r = [StepResult('s10', '自動搬運', [], '怎麼測：xxx')]
    assert '本輪無異動，免測' in render_guide(r)
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `cd src/合併 && python -m pytest tests/test_testguide.py -v`
Expected: FAIL

- [ ] **Step 3: 實作**

```python
# -*- coding: utf-8 -*-
"""每輪建置的實機測試指引。step 自帶模板，這裡只做彙總排版。"""
from dataclasses import dataclass

@dataclass
class StepResult:
    step_id: str
    title: str
    changes: list
    guide: str | None  # step 代入實際改動後的測試說明；None = 沒東西要測

def render_guide(results: list) -> str:
    lines = ['# 測試指引', '',
             '進遊戲方式：單人 → 劇情（不要用編輯器測試模式）。',
             '照順序驗，每項回報「符合預期」或「異常＋截圖」。', '']
    for r in results:
        lines.append(f'### 驗 {r.step_id} {r.title}')
        if not r.changes or not r.guide:
            lines += ['本輪無異動，免測。', '']
        else:
            lines += [f'改動筆數：{len(r.changes)}', r.guide, '']
    return '\n'.join(lines) + '\n'
```

- [ ] **Step 4: 跑測試確認通過**

Run: `cd src/合併 && python -m pytest tests/test_testguide.py -v`
Expected: 2 PASS

- [ ] **Step 5: Commit**

```bash
git add src/合併/core/testguide.py src/合併/tests/test_testguide.py
git commit -m "feat(合併): 測試指引產生器"
```

---

### Task 4: core/specfile.py — merge_spec.yaml 載入與驗證

**Files:**
- Create: `src/合併/core/specfile.py`
- Test: `src/合併/tests/test_specfile.py`

**Interfaces:**
- Produces: `MergeSpec(groups: list[Group], overrides: list[Override], params: dict)`；`Group(name: str, action: str, trigger_ids: list[int])`（action ∈ auto/patch/skip）；`Override(target: str, field: str, value: str, reason: str)`；`load_spec(path: Path) -> MergeSpec`；`SpecError(Exception)`。
- 依賴：PyYAML（先 `python -c "import yaml"` 確認，沒有就 `pip install pyyaml`）。

- [ ] **Step 1: 寫失敗測試**

```python
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
import pytest
from core.specfile import load_spec, SpecError

GOOD = '''
groups:
  - name: 幫眾系統
    action: auto
    trigger_ids: [4102, 4103]
overrides:
  - target: "T210 效果0"
    field: quantity
    value: "300"
    reason: 以劍譜5為準（使用者 2026-08-29 裁決）
params:
  boss_hp:
    targets: []
'''

def test_load_good_spec(tmp_path):
    p = tmp_path / 'merge_spec.yaml'
    p.write_text(GOOD, encoding='utf-8')
    spec = load_spec(p)
    assert spec.groups[0].name == '幫眾系統'
    assert spec.groups[0].action == 'auto'
    assert spec.groups[0].trigger_ids == [4102, 4103]
    assert spec.overrides[0].field == 'quantity'
    assert spec.params['boss_hp'] == {'targets': []}

def test_bad_action_raises_specerror(tmp_path):
    p = tmp_path / 'merge_spec.yaml'
    p.write_text(GOOD.replace('action: auto', 'action: 全搬'), encoding='utf-8')
    with pytest.raises(SpecError) as e:
        load_spec(p)
    assert '幫眾系統' in str(e.value) and '全搬' in str(e.value)

def test_missing_sections_default_empty(tmp_path):
    p = tmp_path / 'merge_spec.yaml'
    p.write_text('params: {}\n', encoding='utf-8')
    spec = load_spec(p)
    assert spec.groups == [] and spec.overrides == []
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `cd src/合併 && python -m pytest tests/test_specfile.py -v`
Expected: FAIL

- [ ] **Step 3: 實作**

```python
# -*- coding: utf-8 -*-
"""merge_spec.yaml —— 圈選與裁決的唯一權威。"""
from dataclasses import dataclass, field
from pathlib import Path
import yaml

class SpecError(Exception):
    pass

@dataclass
class Group:
    name: str
    action: str          # auto / patch / skip
    trigger_ids: list

@dataclass
class Override:
    target: str
    field: str
    value: str
    reason: str

@dataclass
class MergeSpec:
    groups: list
    overrides: list
    params: dict

_ACTIONS = {'auto', 'patch', 'skip'}

def load_spec(path: Path) -> MergeSpec:
    raw = yaml.safe_load(Path(path).read_text(encoding='utf-8')) or {}
    groups = []
    for g in raw.get('groups') or []:
        if g.get('action') not in _ACTIONS:
            raise SpecError(f"群組「{g.get('name')}」的 action={g.get('action')!r} 不合法，"
                            f"必須是 {sorted(_ACTIONS)}")
        groups.append(Group(g['name'], g['action'], list(g.get('trigger_ids') or [])))
    overrides = [Override(o['target'], o['field'], str(o['value']), o.get('reason', ''))
                 for o in (raw.get('overrides') or [])]
    return MergeSpec(groups, overrides, raw.get('params') or {})
```

- [ ] **Step 4: 跑測試確認通過**

Run: `cd src/合併 && python -m pytest tests/test_specfile.py -v`
Expected: 3 PASS

- [ ] **Step 5: Commit**

```bash
git add src/合併/core/specfile.py src/合併/tests/test_specfile.py
git commit -m "feat(合併): merge_spec 載入與驗證"
```

---

### Task 5: steps/base.py — Step 介面與 BuildContext

**Files:**
- Create: `src/合併/steps/__init__.py`
- Create: `src/合併/steps/base.py`
- Test: `src/合併/tests/test_base.py`

**Interfaces:**
- Consumes: `MergeSpec`（Task 4）
- Produces:
  - `class BuildError(Exception)` — message 必須列出「缺哪條裁決」
  - `@dataclass BuildContext: base, source, spec, pairing, notes` —— base=無法無天 scenario（就地變異）、source=劍譜5 scenario（唯讀）、pairing=PairTable|None（Task 9 之後才有）、notes=dict（步驟間傳遞，如 s40 找到的 ref 給 s50 用）
  - `class Step: id: str; title: str; intro: str; def apply(self, ctx) -> list[Change]; def test_guide(self, changes) -> str | None`（基底回傳 None）
  - `steps/__init__.py` 提供 `get_steps() -> list[Step]`：匯入所有 `s*.py` 模組的 `STEP` 實例、依 id 排序回傳。s20 補丁日後丟進資料夾即自動納入。

- [ ] **Step 1: 寫失敗測試**

```python
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from steps.base import Step, BuildContext, BuildError
from core.change import Change

class DummyStep(Step):
    id = 's99'
    title = '假步驟'
    intro = '測試用'
    def apply(self, ctx):
        ctx.notes['ran'] = True
        return [Change(self.id, 'param', 'x', '—', '1', '2', 'test')]

def test_step_apply_and_notes():
    ctx = BuildContext(base=None, source=None, spec=None, pairing=None, notes={})
    step = DummyStep()
    changes = step.apply(ctx)
    assert ctx.notes['ran'] and changes[0].step == 's99'
    assert step.test_guide(changes) is None  # 基底預設免測

def test_builderror_is_exception():
    try:
        raise BuildError('缺裁決：群組 X 的物件 ref 123 在基底不存在')
    except BuildError as e:
        assert '缺裁決' in str(e)
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `cd src/合併 && python -m pytest tests/test_base.py -v`
Expected: FAIL

- [ ] **Step 3: 實作**

`steps/base.py`：

```python
# -*- coding: utf-8 -*-
"""Step 介面：純函式變換。不讀寫檔案、不呼叫 write_to_file。"""
from dataclasses import dataclass, field

class BuildError(Exception):
    """spec 未覆蓋的衝突。message 必須列出缺哪條裁決。"""

@dataclass
class BuildContext:
    base: object        # 無法無天 scenario，就地變異
    source: object      # 劍譜5 scenario，唯讀
    spec: object        # MergeSpec
    pairing: object     # PairTable | None
    notes: dict = field(default_factory=dict)  # 步驟間傳遞

class Step:
    id: str = ''
    title: str = ''
    intro: str = ''
    def apply(self, ctx: BuildContext) -> list:
        raise NotImplementedError
    def test_guide(self, changes: list):
        return None
```

`steps/__init__.py`：

```python
# -*- coding: utf-8 -*-
import importlib, pkgutil

def get_steps():
    steps = []
    pkg = importlib.import_module(__name__)
    for m in pkgutil.iter_modules(pkg.__path__):
        if m.name.startswith('s') and m.name[1].isdigit():
            mod = importlib.import_module(f'{__name__}.{m.name}')
            steps.append(mod.STEP)
    return sorted(steps, key=lambda s: s.id)
```

- [ ] **Step 4: 跑測試確認通過**

Run: `cd src/合併 && python -m pytest tests/test_base.py -v`
Expected: 2 PASS

- [ ] **Step 5: Commit**

```bash
git add src/合併/steps src/合併/tests/test_base.py
git commit -m "feat(合併): Step 介面、BuildContext、自動註冊表"
```

---

### Task 6: core/scenario_io.py — 唯一寫檔點

**Files:**
- Create: `src/合併/core/scenario_io.py`
- Test: `src/合併/tests/test_scenario_io.py`（slow）

**Interfaces:**
- Produces:
  - `ORIGIN_BASE: Path`＝`<repo>/src/origin/古龍921_無法無天.aoe2scenario`；`ORIGIN_SOURCE: Path`＝`…古龍921_新負血劍譜5.aoe2scenario`；`GAME_SCENARIO_DIR: Path`＝`C:/Users/Ricky/Games/Age of Empires 2 DE/76561198023399153/resources/_common/scenario`
  - `load(path) -> AoE2DEScenario`
  - `write_out(scenario, out_path: Path) -> None` — 設 `option_manager.legacy_execution_order = True` 後 `write_to_file`。**全 repo 唯一 write_to_file 呼叫點**
  - `deploy(out_path: Path) -> Path` — copy2 到遊戲資料夾

- [ ] **Step 1: 寫失敗測試（slow 整合測試）**

```python
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
import pytest
from core import scenario_io

def test_paths_exist():
    assert scenario_io.ORIGIN_BASE.exists()
    assert scenario_io.ORIGIN_SOURCE.exists()

@pytest.mark.slow
def test_roundtrip_preserves_trigger_count(tmp_path):
    s = scenario_io.load(scenario_io.ORIGIN_BASE)
    n = len(s.trigger_manager.triggers)
    out = tmp_path / 'rt.aoe2scenario'
    scenario_io.write_out(s, out)
    s2 = scenario_io.load(out)
    assert len(s2.trigger_manager.triggers) == n == 5599
    assert s2.option_manager.legacy_execution_order is True
```

- [ ] **Step 2: 跑快測試確認失敗**

Run: `cd src/合併 && python -m pytest tests/test_scenario_io.py -v`
Expected: test_paths_exist FAIL（no module）；slow 被跳過

- [ ] **Step 3: 實作**

```python
# -*- coding: utf-8 -*-
"""origin 載入與唯一寫檔點。任何 step 不得自行 write_to_file。"""
import shutil
from pathlib import Path
from AoE2ScenarioParser.scenarios.aoe2_de_scenario import AoE2DEScenario

REPO = Path(__file__).resolve().parents[3]   # F:/aoe2de
ORIGIN_BASE = REPO / 'src/origin/古龍921_無法無天.aoe2scenario'
ORIGIN_SOURCE = REPO / 'src/origin/古龍921_新負血劍譜5.aoe2scenario'
GAME_SCENARIO_DIR = Path('C:/Users/Ricky/Games/Age of Empires 2 DE'
                         '/76561198023399153/resources/_common/scenario')

def load(path) -> AoE2DEScenario:
    return AoE2DEScenario.from_file(str(path))

def write_out(scenario, out_path: Path) -> None:
    out_path = Path(out_path); out_path.parent.mkdir(parents=True, exist_ok=True)
    scenario.option_manager.legacy_execution_order = True  # 鐵律：防 DE 重排觸發
    scenario.write_to_file(str(out_path))

def deploy(out_path: Path) -> Path:
    dst = GAME_SCENARIO_DIR / Path(out_path).name
    shutil.copy2(out_path, dst)
    return dst
```

注意 `parents[3]`：`core/scenario_io.py` → `core` → `合併` → `src` → repo 根。實作後先跑 `python -c` 驗 REPO 指向 `F:/aoe2de` 再跑測試。

- [ ] **Step 4: 跑測試（含 slow 一次）**

Run: `cd src/合併 && python -m pytest tests/test_scenario_io.py -v -m ""`（空 -m 解除過濾）
Expected: 2 PASS（slow 約 1~2 分鐘：載入→寫出→再載入）

- [ ] **Step 5: 建 out/.gitignore 並 Commit**

`src/合併/out/.gitignore` 內容兩行：`*` 與 `!.gitignore`。

```bash
git add src/合併/core/scenario_io.py src/合併/tests/test_scenario_io.py src/合併/out/.gitignore
git commit -m "feat(合併): 唯一寫檔點 scenario_io（legacy_execution_order 鐵律內建）"
```

---

### Task 7: build.py — CLI 與編排

**Files:**
- Create: `src/合併/build.py`
- Test: `src/合併/tests/test_build_orchestration.py`

**Interfaces:**
- Consumes: `get_steps()`（Task 5）、`load_spec`（Task 4）、`write_step_logs`（Task 2）、`StepResult`/`render_guide`（Task 3）、`scenario_io`（Task 6）
- Produces:
  - `run_pipeline(ctx, steps, logs_dir) -> list[StepResult]` — 逐步 apply、逐步寫 log、收 StepResult；任何 BuildError 直接往上拋
  - CLI：`python build.py [--until sNN] [--deploy] [--report]`。`--report` 走 Task 12 的盤點（此時先放 stub 印「盤點尚未實作」退出 2）。正常路徑：載 spec 與兩 origin → run_pipeline →（s90 在 steps 清單內把關）→ `write_out` 到 `out/古龍921_合併.aoe2scenario` → 寫 `logs/測試指引.md` → 印 git commit 建議指令。`--until` 只跑 id ≤ 指定值的 steps，輸出檔名帶 `_until_sNN` 後綴。
  - 編排函式與 CLI 分離：`run_pipeline` 不碰檔案系統以外的 I/O（寫 log 除外），可用假 step 測。

- [ ] **Step 1: 寫失敗測試（不載真檔，用假 step）**

```python
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
import pytest
from build import run_pipeline, filter_until
from steps.base import Step, BuildContext, BuildError
from core.change import Change

class Ok(Step):
    id = 's10'; title = 'ok'; intro = 'i'
    def apply(self, ctx): return [Change('s10', 'param', 'x', '—', '', '', 'r')]
    def test_guide(self, changes): return '怎麼測：x'

class Boom(Step):
    id = 's20'; title = 'boom'; intro = 'i'
    def apply(self, ctx): raise BuildError('缺裁決：y')

def test_pipeline_collects_results_and_writes_logs(tmp_path):
    ctx = BuildContext(None, None, None, None, {})
    results = run_pipeline(ctx, [Ok()], tmp_path)
    assert results[0].guide == '怎麼測：x'
    assert (tmp_path / '10_ok.tsv').exists() and (tmp_path / '10_ok.md').exists()

def test_builderror_aborts(tmp_path):
    ctx = BuildContext(None, None, None, None, {})
    with pytest.raises(BuildError):
        run_pipeline(ctx, [Ok(), Boom()], tmp_path)

def test_filter_until():
    steps = [Ok(), Boom()]
    assert [s.id for s in filter_until(steps, 's10')] == ['s10']
    assert [s.id for s in filter_until(steps, None)] == ['s10', 's20']
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `cd src/合併 && python -m pytest tests/test_build_orchestration.py -v`
Expected: FAIL

- [ ] **Step 3: 實作 build.py**

```python
# -*- coding: utf-8 -*-
"""單一入口建置流程。用法：
    python build.py                 # 全跑，出 out/古龍921_合併.aoe2scenario
    python build.py --until s30    # 跑到 s30，出 out/古龍921_合併_until_s30.aoe2scenario
    python build.py --deploy       # 建置後複製到遊戲劇情資料夾
    python build.py --report       # 產盤點報告（不建置）
"""
import argparse, sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parent))
sys.stdout.reconfigure(encoding='utf-8')

from core.change import Change
from core.logging_out import write_step_logs
from core.testguide import StepResult, render_guide
from core.specfile import load_spec
from core import scenario_io
from steps import get_steps
from steps.base import BuildContext, BuildError

HERE = Path(__file__).resolve().parent
LOGS = HERE / 'logs'
OUT = HERE / 'out'

def filter_until(steps, until):
    return [s for s in steps if until is None or s.id <= until]

def run_pipeline(ctx, steps, logs_dir):
    results = []
    for step in steps:
        changes = step.apply(ctx)          # BuildError 直接中止
        write_step_logs(step.id, step.title, step.intro, changes, logs_dir)
        results.append(StepResult(step.id, step.title, changes, step.test_guide(changes)))
        print(f'{step.id} {step.title}: {len(changes)} 筆異動')
    return results

def main():
    ap = argparse.ArgumentParser()
    ap.add_argument('--until')
    ap.add_argument('--deploy', action='store_true')
    ap.add_argument('--report', action='store_true')
    a = ap.parse_args()
    if a.report:
        from analysis.inventory import write_report   # Task 12 前先讓 ImportError 自然爆
        return write_report()
    spec = load_spec(HERE / 'merge_spec.yaml')
    base = scenario_io.load(scenario_io.ORIGIN_BASE)
    source = scenario_io.load(scenario_io.ORIGIN_SOURCE)
    ctx = BuildContext(base=base, source=source, spec=spec, pairing=None, notes={})
    steps = filter_until(get_steps(), a.until)
    try:
        results = run_pipeline(ctx, steps, LOGS)
    except BuildError as e:
        print(f'建置中止：{e}'); return 1
    suffix = f'_until_{a.until}' if a.until else ''
    out_path = OUT / f'古龍921_合併{suffix}.aoe2scenario'
    scenario_io.write_out(ctx.base, out_path)
    (LOGS / '測試指引.md').write_text(render_guide(results), encoding='utf-8')
    print(f'產出：{out_path}')
    if a.deploy:
        print(f'部署：{scenario_io.deploy(out_path)}')
    print('建議 commit：git add src/合併/logs && git commit -m "build(合併): <本輪摘要>"')
    return 0

if __name__ == '__main__':
    sys.exit(main())
```

- [ ] **Step 4: 跑測試確認通過**

Run: `cd src/合併 && python -m pytest tests/test_build_orchestration.py -v`
Expected: 3 PASS

- [ ] **Step 5: Commit**

```bash
git add src/合併/build.py src/合併/tests/test_build_orchestration.py
git commit -m "feat(合併): build.py 單一入口編排（--until/--deploy/--report 骨架）"
```

---

### Task 8: analysis/fingerprint.py — 觸發內容指紋

**Files:**
- Create: `src/合併/analysis/__init__.py`（空檔）
- Create: `src/合併/analysis/fingerprint.py`
- Test: `src/合併/tests/test_fingerprint.py`

**Interfaces:**
- Produces: `fingerprint(trigger) -> tuple` — 條件/效果的**穩定欄位**序列，改名不影響、`trigger_id`/`selected_object_ids`/`message` 排除（這些在兩版間天然漂移）；`type_signature(trigger) -> tuple` — 只取 (kind, type) 序列，供模糊配對。
- 觸發物件只需鴨子型別：`.conditions`/`.effects` 各元素有屬性即可 —— 測試用 `types.SimpleNamespace` 造假。

- [ ] **Step 1: 寫失敗測試**

```python
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from types import SimpleNamespace as NS
from analysis.fingerprint import fingerprint, type_signature

def _eff(**kw):
    d = dict(effect_type=1, quantity=None, tribute_list=None, object_list_unit_id=None,
             source_player=None, target_player=None, technology=None, operation=None,
             object_attributes=None, area_x1=None, area_y1=None, area_x2=None, area_y2=None,
             location_x=None, location_y=None, trigger_id=99, selected_object_ids=[7],
             message='嗨')
    d.update(kw); return NS(**d)

def _cond(**kw):
    d = dict(condition_type=3, quantity=None, attribute=None, unit_object=None,
             next_object=None, object_list=None, source_player=None, technology=None,
             timer=None, area_x1=None, area_y1=None, area_x2=None, area_y2=None,
             inverted=None, comparison=None)
    d.update(kw); return NS(**d)

def _trig(conds, effs):
    return NS(conditions=conds, effects=effs)

def test_rename_invariant_fields_ignored():
    a = _trig([_cond(quantity=5)], [_eff(quantity=100, trigger_id=1, message='A')])
    b = _trig([_cond(quantity=5)], [_eff(quantity=100, trigger_id=2, message='B')])
    assert fingerprint(a) == fingerprint(b)

def test_param_change_changes_fingerprint():
    a = _trig([], [_eff(quantity=100)])
    b = _trig([], [_eff(quantity=200)])
    assert fingerprint(a) != fingerprint(b)
    assert type_signature(a) == type_signature(b)

def test_order_matters():
    e1, e2 = _eff(effect_type=1), _eff(effect_type=2)
    assert fingerprint(_trig([], [e1, e2])) != fingerprint(_trig([], [e2, e1]))
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `cd src/合併 && python -m pytest tests/test_fingerprint.py -v`
Expected: FAIL

- [ ] **Step 3: 實作**

```python
# -*- coding: utf-8 -*-
"""觸發內容指紋：跨版本配對改名觸發用。
排除 trigger_id / selected_object_ids / message —— 這些在兩版間天然漂移。"""

_EFFECT_FIELDS = ('effect_type', 'quantity', 'tribute_list', 'object_list_unit_id',
                  'source_player', 'target_player', 'technology', 'operation',
                  'object_attributes', 'area_x1', 'area_y1', 'area_x2', 'area_y2',
                  'location_x', 'location_y')
_COND_FIELDS = ('condition_type', 'quantity', 'attribute', 'unit_object', 'next_object',
                'object_list', 'source_player', 'technology', 'timer',
                'area_x1', 'area_y1', 'area_x2', 'area_y2', 'inverted', 'comparison')

def _row(obj, fields, tag):
    return (tag,) + tuple(getattr(obj, f, None) for f in fields)

def fingerprint(trigger) -> tuple:
    return (tuple(_row(c, _COND_FIELDS, 'c') for c in trigger.conditions)
            + tuple(_row(e, _EFFECT_FIELDS, 'e') for e in trigger.effects))

def type_signature(trigger) -> tuple:
    return (tuple(('c', c.condition_type) for c in trigger.conditions)
            + tuple(('e', e.effect_type) for e in trigger.effects))
```

- [ ] **Step 4: 跑測試確認通過**

Run: `cd src/合併 && python -m pytest tests/test_fingerprint.py -v`
Expected: 3 PASS

- [ ] **Step 5: Commit**

```bash
git add src/合併/analysis src/合併/tests/test_fingerprint.py
git commit -m "feat(合併): 觸發內容指紋"
```

---

### Task 9: analysis/pairing.py — 兩版觸發配對

**Files:**
- Create: `src/合併/analysis/pairing.py`
- Test: `src/合併/tests/test_pairing.py`

**Interfaces:**
- Consumes: `fingerprint`, `type_signature`（Task 8）
- Produces: `PairTable(a2b: dict[int,int], b2a: dict[int,int], method: dict[int,str], unmatched_a: list[int], unmatched_b: list[int])`（method 鍵為 a 的 id，值 ∈ `name`/`fp`/`fuzzy`）；`pair_triggers(a_triggers: list, b_triggers: list) -> PairTable`。
- 演算法（依序，先配走的不再參賽）：
  1. **name**：名稱在雙方都唯一且相等 → 配對
  2. **fp**：剩餘者指紋完全相等且雙方唯一 → 配對
  3. **fuzzy**：剩餘者 type_signature 相等且雙方唯一 → 配對（標 fuzzy，盤點報告要列出來給人看）
  4. 剩下進 unmatched

- [ ] **Step 1: 寫失敗測試**

```python
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from types import SimpleNamespace as NS
from analysis.pairing import pair_triggers

def _t(tid, name, etypes, q=0):
    effs = [NS(effect_type=et, quantity=q, tribute_list=None, object_list_unit_id=None,
               source_player=None, target_player=None, technology=None, operation=None,
               object_attributes=None, area_x1=None, area_y1=None, area_x2=None,
               area_y2=None, location_x=None, location_y=None,
               trigger_id=-1, selected_object_ids=[], message='') for et in etypes]
    return NS(trigger_id=tid, name=name, conditions=[], effects=effs)

def test_name_match_first():
    a = [_t(0, '幫眾', [1])]; b = [_t(0, '幫眾', [2])]  # 內容不同也照名配
    pt = pair_triggers(a, b)
    assert pt.a2b == {0: 0} and pt.method[0] == 'name'

def test_fingerprint_match_for_renamed():
    a = [_t(0, '8城名', [5], q=7)]; b = [_t(0, '8城', [5], q=7)]
    pt = pair_triggers(a, b)
    assert pt.a2b == {0: 0} and pt.method[0] == 'fp'

def test_fuzzy_by_type_signature():
    a = [_t(0, '武士2', [5], q=1)]; b = [_t(0, '武士4', [5], q=999)]
    pt = pair_triggers(a, b)
    assert pt.a2b == {0: 0} and pt.method[0] == 'fuzzy'

def test_duplicate_names_not_name_matched():
    a = [_t(0, '少林寺', [1], q=1), _t(1, '少林寺', [2], q=2)]
    b = [_t(0, '少林寺', [1], q=1)]
    pt = pair_triggers(a, b)
    assert pt.method.get(0) == 'fp'      # 靠指紋配上
    assert 1 in pt.unmatched_a

def test_unmatched_collected():
    a = [_t(0, '獨有A', [9], q=1)]; b = [_t(0, '獨有B', [8], q=1)]
    pt = pair_triggers(a, b)
    assert pt.unmatched_a == [0] and pt.unmatched_b == [0]
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `cd src/合併 && python -m pytest tests/test_pairing.py -v`
Expected: FAIL

- [ ] **Step 3: 實作**

```python
# -*- coding: utf-8 -*-
"""兩版觸發配對：name → fingerprint → type_signature（fuzzy）。"""
from collections import Counter, defaultdict
from dataclasses import dataclass, field
from .fingerprint import fingerprint, type_signature

@dataclass
class PairTable:
    a2b: dict = field(default_factory=dict)
    b2a: dict = field(default_factory=dict)
    method: dict = field(default_factory=dict)   # a_id -> 'name'|'fp'|'fuzzy'
    unmatched_a: list = field(default_factory=list)
    unmatched_b: list = field(default_factory=list)

def _match_round(rest_a, rest_b, key_fn, tag, pt):
    ka = defaultdict(list); kb = defaultdict(list)
    for t in rest_a: ka[key_fn(t)].append(t)
    for t in rest_b: kb[key_fn(t)].append(t)
    for k, la in ka.items():
        lb = kb.get(k, [])
        if len(la) == 1 and len(lb) == 1:
            a, b = la[0], lb[0]
            pt.a2b[a.trigger_id] = b.trigger_id
            pt.b2a[b.trigger_id] = a.trigger_id
            pt.method[a.trigger_id] = tag
    return ([t for t in rest_a if t.trigger_id not in pt.a2b],
            [t for t in rest_b if t.trigger_id not in pt.b2a])

def pair_triggers(a_triggers, b_triggers) -> PairTable:
    pt = PairTable()
    ra, rb = list(a_triggers), list(b_triggers)
    ra, rb = _match_round(ra, rb, lambda t: t.name, 'name', pt)
    ra, rb = _match_round(ra, rb, fingerprint, 'fp', pt)
    ra, rb = _match_round(ra, rb, type_signature, 'fuzzy', pt)
    pt.unmatched_a = [t.trigger_id for t in ra]
    pt.unmatched_b = [t.trigger_id for t in rb]
    return pt
```

- [ ] **Step 4: 跑測試確認通過**

Run: `cd src/合併 && python -m pytest tests/test_pairing.py -v`
Expected: 5 PASS

- [ ] **Step 5: Commit**

```bash
git add src/合併/analysis/pairing.py src/合併/tests/test_pairing.py
git commit -m "feat(合併): 兩版觸發三輪配對"
```

---

### Task 10: analysis/grouping.py — 啟動圖分群

**Files:**
- Create: `src/合併/analysis/grouping.py`
- Test: `src/合併/tests/test_grouping.py`

**Interfaces:**
- Consumes: 無（鴨子型別觸發）
- Produces: `activation_edges(triggers) -> list[tuple[int,int]]`（(De)Activate 效果的 src_id→dst_id；effect_type 用 `EffectId.ACTIVATE_TRIGGER`＝53 與 `EffectId.DEACTIVATE_TRIGGER`＝54，實作時 `from AoE2ScenarioParser.datasets.effects import EffectId` 取值，勿寫死數字）；`group_ids(candidate_ids: set[int], edges) -> list[set[int]]` — 只在候選集合（unmatched_a）內做 union-find 連通分量，跨出集合的邊不併群（那是依賴，Task 11 的事）；`group_label(triggers_by_id, members: set[int]) -> str` — 取成員名稱的最長共同前綴，全空則 `組_<最小id>`。

- [ ] **Step 1: 寫失敗測試**

```python
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from types import SimpleNamespace as NS
from analysis.grouping import activation_edges, group_ids, group_label
from AoE2ScenarioParser.datasets.effects import EffectId

def _t(tid, name, act_targets=()):
    effs = [NS(effect_type=EffectId.ACTIVATE_TRIGGER, trigger_id=x) for x in act_targets]
    return NS(trigger_id=tid, name=name, conditions=[], effects=effs)

def test_edges_extracted():
    ts = [_t(0, 'a', [1]), _t(1, 'b'), _t(2, 'c', [0])]
    assert set(activation_edges(ts)) == {(0, 1), (2, 0)}

def test_grouping_within_candidates_only():
    edges = [(0, 1), (1, 5)]          # 5 不在候選集
    gs = group_ids({0, 1, 2}, edges)
    assert sorted(map(sorted, gs)) == [[0, 1], [2]]

def test_group_label_common_prefix():
    ts = {0: _t(0, '踢2p'), 1: _t(1, '踢3p')}
    assert group_label(ts, {0, 1}) == '踢'
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `cd src/合併 && python -m pytest tests/test_grouping.py -v`
Expected: FAIL

- [ ] **Step 3: 實作**

```python
# -*- coding: utf-8 -*-
"""啟動關係分群：unmatched 觸發的連通分量 = 圈選單位。"""
import os
from AoE2ScenarioParser.datasets.effects import EffectId

_ACT = {int(EffectId.ACTIVATE_TRIGGER), int(EffectId.DEACTIVATE_TRIGGER)}

def activation_edges(triggers):
    edges = []
    for t in triggers:
        for e in t.effects:
            if int(e.effect_type) in _ACT and e.trigger_id is not None and e.trigger_id >= 0:
                edges.append((t.trigger_id, e.trigger_id))
    return edges

def group_ids(candidate_ids, edges):
    parent = {i: i for i in candidate_ids}
    def find(x):
        while parent[x] != x:
            parent[x] = parent[parent[x]]; x = parent[x]
        return x
    for a, b in edges:
        if a in parent and b in parent:
            parent[find(a)] = find(b)
    comps = {}
    for i in candidate_ids:
        comps.setdefault(find(i), set()).add(i)
    return list(comps.values())

def group_label(triggers_by_id, members):
    names = [triggers_by_id[i].name for i in sorted(members) if triggers_by_id[i].name]
    if not names:
        return f'組_{min(members)}'
    prefix = os.path.commonprefix(names).strip()
    return prefix if prefix else f'組_{min(members)}'
```

- [ ] **Step 4: 跑測試確認通過**

Run: `cd src/合併 && python -m pytest tests/test_grouping.py -v`
Expected: 3 PASS

- [ ] **Step 5: Commit**

```bash
git add src/合併/analysis/grouping.py src/合併/tests/test_grouping.py
git commit -m "feat(合併): 啟動圖分群"
```

---

### Task 11: analysis/dependency.py — 依賴度評估

**Files:**
- Create: `src/合併/analysis/dependency.py`
- Test: `src/合併/tests/test_dependency.py`

**Interfaces:**
- Consumes: `PairTable`（Task 9）、`activation_edges`（Task 10）
- Produces:
  - `collect_object_refs(triggers) -> set[int]` — 群內所有效果/條件的 `selected_object_ids` ∪ `location_object_reference` ∪ 條件的 `unit_object`/`next_object`（非 None 且 ≥0）
  - `UnitIndex = dict[int, tuple]`：`build_unit_index(scenario) -> {ref: (player, unit_const, x, y)}`
  - `GroupReport(label, members, refs_same, refs_a_only, refs_collision, area_effect_count, cross_out_edges, cross_in_hint, classification, reasons)`；classification ∈ `auto`/`patch`
  - `assess_group(label, members: set[int], a_triggers_by_id, idx_a: UnitIndex, idx_b: UnitIndex, pairing: PairTable) -> GroupReport`
- 判定規則（順序重要）：
  - ref 在 idx_a 與 idx_b 且 tuple 相等 → same；只在 idx_a → a_only（可搬）；兩邊都有但 tuple 不等 → **collision**（衝突類型 2/5）
  - 群內效果帶 area 欄位且無 selected_object_ids → `area_effect_count` +1（區域效果無法靜態解析，報告標人工過目）
  - 群內觸發引用群外 trigger_id：若該目標在 pairing.a2b 有對應 → cross_out_edges 記 (src, 對應b_id)；無對應 → 記 (src, None)
  - classification：collision 或 (src, None) 存在 → `patch`；否則 `auto`（reasons 說明原因）

- [ ] **Step 1: 寫失敗測試**

```python
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from types import SimpleNamespace as NS
from analysis.dependency import collect_object_refs, assess_group
from analysis.pairing import PairTable
from AoE2ScenarioParser.datasets.effects import EffectId

def _eff(sel=(), loc=None, et=1, tid=-1, area=None):
    d = dict(effect_type=et, selected_object_ids=list(sel), location_object_reference=loc,
             trigger_id=tid, area_x1=None, area_y1=None, area_x2=None, area_y2=None)
    if area:
        d.update(area_x1=area[0], area_y1=area[1], area_x2=area[2], area_y2=area[3])
    return NS(**d)

def _t(tid, name, effs):
    return NS(trigger_id=tid, name=name, conditions=[], effects=effs)

def test_collect_refs():
    t = _t(0, 'x', [_eff(sel=[7, 8], loc=9)])
    assert collect_object_refs([t]) == {7, 8, 9}

def _assess(effs, idx_a, idx_b, pairing=None):
    ts = {0: _t(0, 'g1', effs)}
    return assess_group('g', {0}, ts, idx_a, idx_b, pairing or PairTable())

def test_auto_when_refs_identical():
    idx = {7: (0, 100, 1.0, 2.0)}
    r = _assess([_eff(sel=[7])], idx, dict(idx))
    assert r.classification == 'auto' and r.refs_same == 1

def test_patch_on_collision():
    r = _assess([_eff(sel=[7])], {7: (0, 100, 1.0, 2.0)}, {7: (1, 200, 3.0, 4.0)})
    assert r.classification == 'patch' and r.refs_collision == [7]

def test_patch_on_unmapped_cross_edge():
    e = _eff(et=int(EffectId.ACTIVATE_TRIGGER), tid=999)   # 999 在群外且 pairing 無對應
    r = _assess([e], {}, {})
    assert r.classification == 'patch' and (0, None) in r.cross_out_edges

def test_auto_with_mapped_cross_edge():
    pt = PairTable(a2b={999: 42})
    e = _eff(et=int(EffectId.ACTIVATE_TRIGGER), tid=999)
    r = _assess([e], {}, {}, pt)
    assert r.classification == 'auto' and (0, 42) in r.cross_out_edges

def test_area_effect_counted():
    r = _assess([_eff(area=(1, 1, 5, 5))], {}, {})
    assert r.area_effect_count == 1
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `cd src/合併 && python -m pytest tests/test_dependency.py -v`
Expected: FAIL

- [ ] **Step 3: 實作**

```python
# -*- coding: utf-8 -*-
"""群依賴度評估：決定 自動搬運 / 需手寫補丁，並列衝突。"""
from dataclasses import dataclass, field
from AoE2ScenarioParser.datasets.effects import EffectId

_ACT = {int(EffectId.ACTIVATE_TRIGGER), int(EffectId.DEACTIVATE_TRIGGER)}

def collect_object_refs(triggers):
    refs = set()
    for t in triggers:
        for e in t.effects:
            refs |= {r for r in (e.selected_object_ids or []) if r >= 0}
            loc = getattr(e, 'location_object_reference', None)
            if loc is not None and loc >= 0:
                refs.add(loc)
        for c in t.conditions:
            for f in ('unit_object', 'next_object'):
                v = getattr(c, f, None)
                if v is not None and v >= 0:
                    refs.add(v)
    return refs

def build_unit_index(scenario):
    idx = {}
    for p in range(9):
        for u in scenario.unit_manager.units[p]:
            idx[u.reference_id] = (p, u.unit_const, round(u.x, 2), round(u.y, 2))
    return idx

@dataclass
class GroupReport:
    label: str
    members: set
    refs_same: int = 0
    refs_a_only: list = field(default_factory=list)
    refs_collision: list = field(default_factory=list)
    area_effect_count: int = 0
    cross_out_edges: list = field(default_factory=list)  # (src_a_id, b_id|None)
    classification: str = 'auto'
    reasons: list = field(default_factory=list)

def assess_group(label, members, a_triggers_by_id, idx_a, idx_b, pairing) -> GroupReport:
    r = GroupReport(label, members)
    ts = [a_triggers_by_id[i] for i in sorted(members)]
    for ref in sorted(collect_object_refs(ts)):
        in_a, in_b = ref in idx_a, ref in idx_b
        if in_a and in_b and idx_a[ref] == idx_b[ref]:
            r.refs_same += 1
        elif in_a and not in_b:
            r.refs_a_only.append(ref)
        elif in_a and in_b:
            r.refs_collision.append(ref)
    for t in ts:
        for e in t.effects:
            if not (e.selected_object_ids or []) and e.area_x1 is not None and e.area_x1 >= 0:
                r.area_effect_count += 1
            if int(e.effect_type) in _ACT and e.trigger_id is not None \
                    and e.trigger_id >= 0 and e.trigger_id not in members:
                r.cross_out_edges.append((t.trigger_id, pairing.a2b.get(e.trigger_id)))
    if r.refs_collision:
        r.classification = 'patch'
        r.reasons.append(f'ref 衝突（兩版同 ref 不同物件）: {r.refs_collision}')
    if any(b is None for _, b in r.cross_out_edges):
        r.classification = 'patch'
        r.reasons.append('有啟動邊指向配對不到的基底觸發（掛點衝突）')
    if r.classification == 'auto':
        r.reasons.append('物件全數可解析、啟動邊全數可配對')
    return r
```

- [ ] **Step 4: 跑測試確認通過**

Run: `cd src/合併 && python -m pytest tests/test_dependency.py -v`
Expected: 6 PASS

- [ ] **Step 5: Commit**

```bash
git add src/合併/analysis/dependency.py src/合併/tests/test_dependency.py
git commit -m "feat(合併): 群依賴度評估（auto/patch 判定與衝突偵測）"
```

---

### Task 12: analysis/inventory.py — 盤點報告（交付物）

**Files:**
- Create: `src/合併/analysis/inventory.py`
- Test: `src/合併/tests/test_inventory.py`（組裝邏輯用假資料＋一個 slow 全流程）

**Interfaces:**
- Consumes: Task 8–11 全部、`scenario_io`（Task 6）
- Produces: `write_report() -> int` — 給 `build.py --report` 呼叫。載兩 origin → 配對 → 對 `pairing.unmatched_a` 分群 → 逐群 assess → 產 `reports/盤點報告.md` 與 `reports/盤點明細.tsv`。
- 報告結構（spec §7）：
  1. 開頭圈選總表：`| 群 | 觸發數 | 物件(同/獨有/衝突) | 區域效果 | 判定 | 建議 | 圈選 |`，圈選欄留空白給使用者填 auto/patch/skip
  2. 每群一節：成員觸發清單（id＋名稱）、依賴細節、reasons
  3. **同名不同內容**：pairing 中 method=name 但 `fingerprint(a) != fingerprint(b)` 的觸發——列名稱＋逐效果的參數差異（欄位、a 值、b 值），這是衝突類型 1 的清單
  4. **fuzzy 配對清單**：method=fuzzy 的配對（人工確認配對正確性）
  5. **孤兒物件**：a 獨有物件（idx_a − idx_b）中沒被任何觸發引用的，按 unit_const 分組計數
  6. 附 b 側 unmatched（無法無天獨有觸發）數量統計——只列數字與名稱，不需動作
- md 組裝函式與 I/O 分離：`render_report(pairing, group_reports, diff_rows, fuzzy_rows, orphans, stats) -> str` 可單元測試。

- [ ] **Step 1: 寫失敗測試（render 用假資料）**

```python
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from analysis.inventory import render_report
from analysis.dependency import GroupReport

def test_render_contains_sections_and_selection_table():
    gr = GroupReport('踢人系統', {1, 2}); gr.refs_same = 3; gr.classification = 'auto'
    gr.reasons = ['物件全數可解析、啟動邊全數可配對']
    out = render_report(
        pairing=None,
        group_reports=[gr],
        diff_rows=[('武士2', '效果0.quantity', '100', '999')],
        fuzzy_rows=[('武士2', '武士4')],
        orphans={1358: 5},
        stats={'a_total': 5232, 'b_total': 5599, 'unmatched_b': 317})
    assert '踢人系統' in out and '| 圈選 |' in out.replace(' ', '')[:2000] or '圈選' in out
    assert '同名不同內容' in out and '武士2' in out
    assert 'fuzzy' in out.lower() or '模糊' in out
    assert '孤兒物件' in out
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `cd src/合併 && python -m pytest tests/test_inventory.py -v`
Expected: FAIL

- [ ] **Step 3: 實作**

`render_report` 按上述六節組 markdown（純字串組裝，30~50 行）；`write_report()`：

```python
def write_report() -> int:
    from core import scenario_io
    from .fingerprint import fingerprint
    from .pairing import pair_triggers
    from .grouping import activation_edges, group_ids, group_label
    from .dependency import assess_group, build_unit_index, collect_object_refs
    a = scenario_io.load(scenario_io.ORIGIN_SOURCE)   # 劍譜5 = a
    b = scenario_io.load(scenario_io.ORIGIN_BASE)     # 無法無天 = b
    ta, tb = a.trigger_manager.triggers, b.trigger_manager.triggers
    pt = pair_triggers(ta, tb)
    by_id = {t.trigger_id: t for t in ta}
    idx_a, idx_b = build_unit_index(a), build_unit_index(b)
    edges = activation_edges(ta)
    groups = group_ids(set(pt.unmatched_a), edges)
    reports = [assess_group(group_label(by_id, g), g, by_id, idx_a, idx_b, pt)
               for g in groups]
    # 同名不同內容 / fuzzy / 孤兒 / stats（组裝細節見 render_report 的參數形狀）
    ...
    out_dir = Path(__file__).resolve().parents[1] / 'reports'
    out_dir.mkdir(exist_ok=True)
    (out_dir / '盤點報告.md').write_text(render_report(...), encoding='utf-8')
    return 0
```

「同名不同內容」的參數差異：對 method=name 且指紋不等的配對，逐位置比 `fingerprint` 的 row tuple，不等的欄位輸出 `(名稱, '效果i.欄位', a值, b值)`；效果數不同就輸出 `(名稱, '效果數', len_a, len_b)`。孤兒物件：`(idx_a.keys() − idx_b.keys()) − 所有觸發引用的 refs`，按 unit_const Counter。同時把總表列成 `盤點明細.tsv`（群、成員id、判定、原因）。

- [ ] **Step 4: 跑單元測試通過後，跑真檔全流程（slow）**

```python
@pytest.mark.slow
def test_write_report_end_to_end():
    from analysis.inventory import write_report
    assert write_report() == 0
    from pathlib import Path
    rp = Path(__file__).resolve().parents[1] / 'reports/盤點報告.md'
    body = rp.read_text(encoding='utf-8')
    assert '圈選' in body and '同名不同內容' in body
```

Run: `cd src/合併 && python -m pytest tests/test_inventory.py -v -m ""`
Expected: 全 PASS（slow 約 1 分鐘）。**跑完人工瀏覽 `reports/盤點報告.md` 一遍**：預期群數大約落在 5~20（41 個 unmatched 觸發），若出現上百群代表分群斷鏈，回頭檢查 edges。

- [ ] **Step 5: Commit（含產出的報告）**

```bash
git add src/合併/analysis/inventory.py src/合併/tests/test_inventory.py src/合併/reports/
git commit -m "feat(合併): 盤點報告產生器＋首份盤點報告"
```

---

### Task 13: steps/s10_transplant.py — 自動搬運器

**Files:**
- Create: `src/合併/steps/s10_transplant.py`
- Test: `src/合併/tests/test_s10.py`

**Interfaces:**
- Consumes: `Step`/`BuildContext`/`BuildError`（Task 5）、`pair_triggers`（Task 9）、`build_unit_index`/`collect_object_refs`（Task 11）、`Change`（Task 1）
- Produces: `STEP`（模組層級實例）。核心流程 `apply(ctx)`：
  1. `ctx.pairing = pair_triggers(source.triggers, base.triggers)`（存回 ctx 供後續 step 用）
  2. 對 spec 中每個 action=auto 的群：
     a. 取 source 觸發 deepcopy
     b. **跨界引用記錄**：掃 (de)activate 效果中 `trigger_id ∉ 群`者 —— pairing.a2b 有對應 → 記 `(copy_idx, effect_idx, b_id)`；無對應 → 收集後 raise `BuildError('缺裁決：群「X」觸發「Y」啟動的「Z」在基底配不到掛點，需改 patch 或在 spec 排除')`
     c. **物件搬運**：群引用 refs 中，同 tuple 存在於 base → 跳過；只在 source → `base.unit_manager.add_unit(player, unit_const, x=…, y=…, z=…, rotation=…, reference_id=ref)`（emit `unit_add`）；collision → BuildError
     d. `imported = base.trigger_manager.import_triggers(copies, deepcopy=False)`（內部引用自動重映射；被重設 -1 的跨界引用用 b 步的記錄回填，emit `remap`）
     e. 每個搬入觸發 emit `trigger_add`（reason=`merge_spec: <群名>`）
  3. 套 spec.overrides 中 target 落在本步搬入觸發者（其餘 override 留給對應 step；本計畫先實作「無 override」路徑，遇到未消化的 override 在 s90 對帳）
- `test_guide`: 「搬入群 <名單>。怎麼測：進遊戲觸發各群入口（報告節有各群的觸發條件摘要），確認功能動作；同時抽測基底原主線一項未壞。」

- [ ] **Step 1: 寫失敗測試（純邏輯部分用假物件）**

跨界記錄與回填邏輯抽成純函式測：

```python
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
import pytest
from types import SimpleNamespace as NS
from steps.s10_transplant import plan_cross_refs
from steps.base import BuildError
from analysis.pairing import PairTable
from AoE2ScenarioParser.datasets.effects import EffectId

def _t(tid, act=()):
    return NS(trigger_id=tid, name=f'T{tid}', conditions=[],
              effects=[NS(effect_type=int(EffectId.ACTIVATE_TRIGGER), trigger_id=x) for x in act])

def test_internal_refs_not_planned():
    plan = plan_cross_refs([_t(0, act=[1]), _t(1)], {0, 1}, PairTable(), '群')
    assert plan == []

def test_cross_ref_mapped():
    pt = PairTable(a2b={99: 42})
    plan = plan_cross_refs([_t(0, act=[99])], {0}, pt, '群')
    assert plan == [(0, 0, 42)]     # (第0個copy, 第0個效果, 回填 42)

def test_cross_ref_unmapped_raises():
    with pytest.raises(BuildError) as e:
        plan_cross_refs([_t(0, act=[99])], {0}, PairTable(), '幫眾')
    assert '幫眾' in str(e.value) and '99' in str(e.value)
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `cd src/合併 && python -m pytest tests/test_s10.py -v`
Expected: FAIL

- [ ] **Step 3: 實作 s10（含 plan_cross_refs 純函式與 STEP.apply）**

`plan_cross_refs(triggers: list, member_ids: set, pairing, group_name) -> list[tuple[int,int,int]]`：走訪每個觸發的效果，(de)activate 且 `trigger_id ∉ member_ids`：pairing 查得到記 (trigger 位置, 效果位置, b_id)，查不到收集成清單，最後一次 raise BuildError 列全部缺項。`apply` 按 Interfaces 節流程組裝，全程 emit Change。

- [ ] **Step 4: 跑測試確認通過**

Run: `cd src/合併 && python -m pytest tests/test_s10.py -v`
Expected: 3 PASS

- [ ] **Step 5: slow 冒煙測試 — 空圈選不變性**

```python
@pytest.mark.slow
def test_s10_with_empty_spec_is_noop():
    from core import scenario_io
    from core.specfile import MergeSpec
    from steps.s10_transplant import STEP
    from steps.base import BuildContext
    base = scenario_io.load(scenario_io.ORIGIN_BASE)
    src = scenario_io.load(scenario_io.ORIGIN_SOURCE)
    n = len(base.trigger_manager.triggers)
    ctx = BuildContext(base, src, MergeSpec([], [], {}), None, {})
    assert STEP.apply(ctx) == []
    assert len(base.trigger_manager.triggers) == n
    assert ctx.pairing is not None   # 配對表已建，供後續 step
```

Run: `cd src/合併 && python -m pytest tests/test_s10.py -v -m ""`
Expected: 4 PASS

- [ ] **Step 6: Commit**

```bash
git add src/合併/steps/s10_transplant.py src/合併/tests/test_s10.py
git commit -m "feat(合併): s10 自動搬運器（跨界回填、物件搬運、缺裁決即中止）"
```

---

### Task 14: steps/s30_attackfix.py — 攻擊力修復

**Files:**
- Create: `src/合併/steps/s30_attackfix.py`
- Test: `src/合併/tests/test_s30.py`
- 移植來源：`src/新負血劍譜/fix_attack.py`（已實測可用——先整檔讀完再移植，特別是它對 `armour_attack_quantity`/`armour_attack_class` 的寫法與 operation 判斷）

**Interfaces:**
- Consumes: Task 5 介面
- Produces: `STEP`；純函式 `decode(v: int) -> int`＝`(abs(v) >> 16) * 256 + (abs(v) & 0xFFFF)`；修復條件：`effect_type == EffectId.CHANGE_OBJECT_ATTACK 且 abs(quantity) >= 65536`。正值 → class=0, amount=V, operation=ADD；負值 → class=0, amount=V, operation=SUBTRACT（operation 常數照 fix_attack.py 用法）。
- `test_guide`: 「修復 N 個攻擊力效果。怎麼測：買一次神兵（任一職業），攻擊測試怪，確認傷害明顯上升；對照買前傷害。異常：買後傷害不變＝修復未生效。」

- [ ] **Step 1: 寫失敗測試**

```python
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from steps.s30_attackfix import decode, needs_fix
from types import SimpleNamespace as NS
from AoE2ScenarioParser.datasets.effects import EffectId

def test_decode_known_values():
    assert decode(16646156) == 254 * 256 + 12      # 65036，遷移記錄 §3.3e 例
    assert decode(-16646156) == 65036
    assert decode(65536) == 256

def test_needs_fix_threshold():
    def e(q, et=int(EffectId.CHANGE_OBJECT_ATTACK)):
        return NS(effect_type=et, quantity=q)
    assert needs_fix(e(65536)) and needs_fix(e(-16646156))
    assert not needs_fix(e(30000))                  # 合法大數值不動
    assert not needs_fix(e(65536, et=1))            # 其他效果不碰
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `cd src/合併 && python -m pytest tests/test_s30.py -v`
Expected: FAIL

- [ ] **Step 3: 實作（移植 fix_attack.py 的變換核心進 Step 介面）**

- [ ] **Step 4: 跑測試確認通過，再加 slow 計數測試**

```python
@pytest.mark.slow
def test_s30_fixes_all_481_on_base():
    from core import scenario_io
    from core.specfile import MergeSpec
    from steps.s30_attackfix import STEP, needs_fix
    from steps.base import BuildContext
    base = scenario_io.load(scenario_io.ORIGIN_BASE)
    ctx = BuildContext(base, None, MergeSpec([], [], {}), None, {})
    changes = STEP.apply(ctx)
    assert len(changes) == 481        # 2026-08-29 實測基底壞效果數
    remaining = [e for t in base.trigger_manager.triggers for e in t.effects if needs_fix(e)]
    assert remaining == []
```

Run: `cd src/合併 && python -m pytest tests/test_s30.py -v -m ""`
Expected: 全 PASS

- [ ] **Step 5: Commit**

```bash
git add src/合併/steps/s30_attackfix.py src/合併/tests/test_s30.py
git commit -m "feat(合併): s30 攻擊力修復（fix_attack 公式移植，基底 481 效果驗證）"
```

---

### Task 15: steps/s40_boss_hp.py — 雙0血

**Files:**
- Create: `src/合併/steps/s40_boss_hp.py`
- Test: `src/合併/tests/test_s40.py`
- 必讀：遷移記錄 §5.6（原理：只有 max=0 時建築夾血不生效）、§5.7（13 個目標與座標表）、§1.2/1.2d（Change HP 是 ADD、int16 回繞：加 `65536 − 現有max` 使 max 回繞成 0）

**Interfaces:**
- Consumes: Task 5 介面；spec `params.boss_hp.targets`
- Produces: `STEP`。spec 參數形狀（Task 19 建初版數值）：

```yaml
params:
  boss_hp:
    targets:
      - label: 亡魂之堡
        unit_const: 82        # 從盤點/實機核對後填
        tile: [3, 5]
        wrap_add: 62536       # 65536 − 該物件在本檔的有效 max（校正參數，實測回報後修）
        note: §5.7 CASTLE ref38862（劍譜5 ref，合併版重找）
```

- `apply`：對每個 target 在 `ctx.base` 的 unit index 中找 `unit_const 相符且 (int(x), int(y)) == tile` 的物件 → 唯一命中取其 ref，記入 `ctx.notes['boss_refs'][label] = ref`（s50 用）；0 或 ≥2 命中 → `BuildError('缺裁決：目標「亡魂之堡」在基底 tile(3,5) 找到 N 個 const 82')`。建一支觸發 `ZZ_雙0血`（enabled=True, looping=False，無條件＝開場執行），每 target 一個效果：`new_effect.change_object_hp(quantity=wrap_add, operation=ADD? …)` —— **實作前先開 `verify/01n_雙0血.md` 對照當時實測可行的效果寫法**（operation 常數、quantity 正負），照抄那個形狀。
- `test_guide`: 「目標 <名單> 已套雙0血（wrap_add 目前是估值）。怎麼測：進遊戲滑鼠移到各目標上，tooltip 應顯示「生命 0」；派兵打亡魂之堡 30 秒，應持續掉血不回滿。異常A：tooltip 顯示正數 = wrap_add 錯，回報 tooltip 數字（我用 65536−該數修參數）。異常B：打不動/回滿 = 夾血未解除，回報截圖。」——**這則指引就是 wrap_add 校正迴路**。

- [ ] **Step 1: 寫失敗測試（找 ref 邏輯）**

```python
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
import pytest
from steps.s40_boss_hp import locate_target
from steps.base import BuildError

IDX = {100: (1, 82, 3.5, 5.5), 101: (1, 82, 9.0, 9.0), 102: (2, 72, 3.5, 5.5)}

def test_locate_unique():
    assert locate_target(IDX, unit_const=82, tile=(3, 5), label='堡') == 100

def test_locate_none_raises():
    with pytest.raises(BuildError) as e:
        locate_target(IDX, unit_const=82, tile=(7, 7), label='堡')
    assert '堡' in str(e.value)

def test_locate_ambiguous_raises():
    idx = dict(IDX); idx[103] = (1, 82, 3.9, 5.1)
    with pytest.raises(BuildError):
        locate_target(idx, unit_const=82, tile=(3, 5), label='堡')
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `cd src/合併 && python -m pytest tests/test_s40.py -v`
Expected: FAIL

- [ ] **Step 3: 實作（locate_target ＋ STEP.apply 建 ZZ_雙0血 觸發）**

`locate_target(idx, unit_const, tile, label) -> int`：`[r for r,(p,c,x,y) in idx.items() if c==unit_const and (int(x),int(y))==tuple(tile)]`，len==1 回傳，否則 BuildError。效果寫法照 `verify/01n_雙0血.md` 的實測形狀。

- [ ] **Step 4: 跑測試確認通過**

Run: `cd src/合併 && python -m pytest tests/test_s40.py -v`
Expected: 3 PASS

- [ ] **Step 5: Commit**

```bash
git add src/合併/steps/s40_boss_hp.py src/合併/tests/test_s40.py
git commit -m "feat(合併): s40 雙0血（tile 定位、wrap_add 校正參數、notes 傳遞 ref）"
```

---

### Task 16: steps/s50_healthbar.py — caption 血條

**Files:**
- Create: `src/合併/steps/s50_healthbar.py`
- Test: `src/合併/tests/test_s50.py`
- 必讀：遷移記錄 §5.7a（樣式 3 定案、caption 字型限制）、`verify/15_血條顯示方式.md` 與 `verify/01s_魔王血條百分比.md`（實測可行的條件/效果確切寫法——條件用 Object HP 門檻、效果用 Change Object Caption，照抄實測形狀）

**Interfaces:**
- Consumes: Task 5 介面；`ctx.notes['boss_refs']`（Task 15 產）；spec `params.healthbar`
- Produces: `STEP`；純函式 `bar_text(name: str, pct: int) -> str`＝樣式 3：`f'{name} [{"#" * (pct // 10)}{"_" * (10 - pct // 10)}] {pct}%'`（半形 %）；`thresholds(start_hp: float) -> list[tuple[int, float]]`＝[(99, start_hp*0.99), …, (1, start_hp*0.01)]，99 階（100% 是初始態不用觸發；每 1% 一階，條件 `Object HP < 門檻`）。
- spec 參數形狀：

```yaml
params:
  healthbar:
    enabled: false        # start_hp 實測回報前保持 false，s50 直接免測跳過
    targets:
      - label: 亡魂之堡
        display_name: 亡魂之堡
        start_hp: 7000200   # 估值；s40 指引的實測回報後改
```

- `enabled: false` → 回傳 []（測試指引自動標免測）——這就是 spec §5 的參數迴路：第一輪 build 先關，s40 實測回報 start_hp 後開。
- 每 target 每階一支觸發：`tm.add_trigger(f'ZZ_血條_{label}_{pct:02d}', enabled=True, looping=False)`；條件 Object HP < 門檻（unit_object=ref）；效果 Change Object Caption（selected_object_ids=[ref], message=bar_text）。非循環＋每階獨立觸發＝實測驗證過的階梯形狀（01s）。
- `test_guide`: 「血條已生成（每目標 99 階）。怎麼測：打亡魂之堡，物件上方浮字應從 99% 逐步下降，格數同步減少；% 為半形不豆腐。異常：字不出現/亂碼/百分比不動，回報階段截圖。」

- [ ] **Step 1: 寫失敗測試**

```python
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from steps.s50_healthbar import bar_text, thresholds

def test_bar_text_style3():
    assert bar_text('亡魂之堡', 40) == '亡魂之堡 [####______] 40%'
    assert bar_text('聚魂塔', 80) == '聚魂塔 [########__] 80%'
    assert bar_text('X', 5) == 'X [__________] 5%'

def test_bar_text_ascii_cjk_only():
    for pct in range(0, 100):
        s = bar_text('聚魂塔', pct)
        assert all(ord(ch) < 128 or '\u4e00' <= ch <= '\u9fff' for ch in s), s

def test_thresholds_99_down_to_1():
    th = thresholds(1000.0)
    assert th[0] == (99, 990.0) and th[-1] == (1, 10.0) and len(th) == 99
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `cd src/合併 && python -m pytest tests/test_s50.py -v`
Expected: FAIL

- [ ] **Step 3: 實作（bar_text／thresholds／STEP.apply，效果形狀照 verify/01s 與 15）**

- [ ] **Step 4: 跑測試確認通過**

Run: `cd src/合併 && python -m pytest tests/test_s50.py -v`
Expected: 3 PASS

- [ ] **Step 5: Commit**

```bash
git add src/合併/steps/s50_healthbar.py src/合併/tests/test_s50.py
git commit -m "feat(合併): s50 caption 血條（樣式3、99 階、enabled 參數迴路）"
```

---

### Task 17: steps/s60_misc.py — 雜項替換

**Files:**
- Create: `src/合併/steps/s60_misc.py`
- Test: `src/合併/tests/test_s60.py`

**Interfaces:**
- Consumes: Task 5 介面；spec `params.misc.const_replacements`
- Produces: `STEP`。spec 形狀：

```yaml
params:
  misc:
    const_replacements:
      - label: 擱淺沉船
        find: {unit_const: 436}     # 全圖找 const 436 的物件
        to_const: 2353
        expect_count: 1             # 找到數量不符 → BuildError（合併版與劍譜5 可能不同）
        reason: 436 在 DE 無外觀，使用者 2026-08-29 選定 2353 替代
```

- `apply`：對每條 replacement 掃 `ctx.base` 全物件，`unit_const == find.unit_const` 的清單長度必須等於 `expect_count`（0 也是合法設定值——若盤點發現無法無天沒有 436，就把 expect_count 設 0，此條自動變 no-op），逐個 `u.unit_const = to_const` 並 emit `unit_field`。
- `test_guide`: 「const 替換 <清單>。怎麼測：到對應座標（log 有列）看物件是否有外觀且顯示正確模型。」

- [ ] **Step 1: 寫失敗測試（用假 unit 物件）**

```python
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
import pytest
from types import SimpleNamespace as NS
from steps.s60_misc import apply_replacement
from steps.base import BuildError

def _units(*consts):
    return [NS(reference_id=i, unit_const=c, x=1.0, y=2.0) for i, c in enumerate(consts)]

def test_replaces_matching_const():
    us = _units(436, 100)
    changes = apply_replacement(us, {'label': '船', 'find': {'unit_const': 436},
                                     'to_const': 2353, 'expect_count': 1, 'reason': 'r'})
    assert us[0].unit_const == 2353 and us[1].unit_const == 100
    assert len(changes) == 1 and changes[0].field == 'unit_const'

def test_count_mismatch_raises():
    with pytest.raises(BuildError):
        apply_replacement(_units(436, 436), {'label': '船', 'find': {'unit_const': 436},
                                             'to_const': 2353, 'expect_count': 1, 'reason': 'r'})

def test_expect_zero_is_noop():
    us = _units(100)
    assert apply_replacement(us, {'label': '船', 'find': {'unit_const': 436},
                                  'to_const': 2353, 'expect_count': 0, 'reason': 'r'}) == []
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `cd src/合併 && python -m pytest tests/test_s60.py -v`
Expected: FAIL

- [ ] **Step 3: 實作（apply_replacement 純函式＋STEP.apply 走訪 9 玩家 units）**

- [ ] **Step 4: 跑測試確認通過**

Run: `cd src/合併 && python -m pytest tests/test_s60.py -v`
Expected: 3 PASS

- [ ] **Step 5: Commit**

```bash
git add src/合併/steps/s60_misc.py src/合併/tests/test_s60.py
git commit -m "feat(合併): s60 雜項 const 替換（expect_count 防呆）"
```

---

### Task 18: steps/s90_audit.py — 終檢不變量

**Files:**
- Create: `src/合併/steps/s90_audit.py`
- Test: `src/合併/tests/test_s90.py`

**Interfaces:**
- Consumes: Task 5 介面、`needs_fix`（Task 14）、`build_unit_index`（Task 11）
- Produces: `STEP`（id='s90'）。`apply` 跑五項檢查，全過回傳 `[Change('s90','audit','全檔','—','','5/5 通過','spec §8')]`，任一失敗收集全部違規後 raise `BuildError`：
  1. 攻擊力：`needs_fix(e)` 為真的效果數 == 0
  2. 觸發引用：所有 (de)activate 的 `trigger_id` ∈ [0, len(triggers)) 或 == -1
  3. 物件引用：所有 `selected_object_ids`/`location_object_reference` ≥0 者存在於 unit index（**先在 origin 基底上驗證此檢查會不會誤報**——原檔可能本來就有 dangling ref；有的話改成「不得比 origin 多」：build 開始時 ctx.notes 存 origin 的 dangling 集合，s90 只驗沒有新增）
  4. 對帳：`len(triggers) == 5599 + Σ(s10 trigger_add) + Σ(s40/s50 新增觸發)`（各 step 的 Change 清單經 ctx.notes['all_changes'] 傳入——build.py 在 run_pipeline 中每步後把 changes 追加進 ctx.notes.setdefault('all_changes', [])，本 Task 順手在 build.py 加這行）
  5. 雙0血：`params.boss_hp.targets` 每個 label 都在 `ctx.notes['boss_refs']`（s40 跑過且找齊）；boss_hp targets 為空清單時跳過
- `test_guide`: None（機器檢查，免實機測）。

- [ ] **Step 1: 寫失敗測試（每項不變量一正一反，用假 scenario：NS(trigger_manager=NS(triggers=[…]), unit_manager=NS(units=[[…]×9]))）**

```python
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
import pytest
from types import SimpleNamespace as NS
from steps.s90_audit import check_attack, check_trigger_refs, check_object_refs
from AoE2ScenarioParser.datasets.effects import EffectId

def _eff(**kw):
    d = dict(effect_type=1, quantity=0, trigger_id=-1, selected_object_ids=[],
             location_object_reference=None)
    d.update(kw); return NS(**d)

def _scn(effs, units=()):
    t = NS(trigger_id=0, name='t', conditions=[], effects=list(effs))
    um = NS(units=[[NS(reference_id=r, unit_const=1, x=0, y=0) for r in units]] + [[]] * 8)
    return NS(trigger_manager=NS(triggers=[t]), unit_manager=um)

def test_check_attack():
    ok = _scn([_eff(effect_type=int(EffectId.CHANGE_OBJECT_ATTACK), quantity=100)])
    bad = _scn([_eff(effect_type=int(EffectId.CHANGE_OBJECT_ATTACK), quantity=65536)])
    assert check_attack(ok) == [] and len(check_attack(bad)) == 1

def test_check_trigger_refs():
    ok = _scn([_eff(effect_type=int(EffectId.ACTIVATE_TRIGGER), trigger_id=0)])
    bad = _scn([_eff(effect_type=int(EffectId.ACTIVATE_TRIGGER), trigger_id=999)])
    assert check_trigger_refs(ok) == [] and len(check_trigger_refs(bad)) == 1

def test_check_object_refs_against_baseline():
    bad = _scn([_eff(selected_object_ids=[777])], units=(1, 2))
    assert check_object_refs(bad, baseline=set()) != []
    assert check_object_refs(bad, baseline={777}) == []   # origin 本來就 dangling 者豁免
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `cd src/合併 && python -m pytest tests/test_s90.py -v`
Expected: FAIL

- [ ] **Step 3: 實作（五個 check_* 純函式＋STEP.apply 彙總；build.py 加 all_changes 與 origin dangling 基線兩行）**

- [ ] **Step 4: 跑測試確認通過**

Run: `cd src/合併 && python -m pytest tests/test_s90.py -v`
Expected: 3 PASS

- [ ] **Step 5: Commit**

```bash
git add src/合併/steps/s90_audit.py src/合併/tests/test_s90.py src/合併/build.py
git commit -m "feat(合併): s90 終檢不變量（五項檢查、違規全列後中止）"
```

---

### Task 19: 首輪建置（fix-only）＋交付測試指引

**Files:**
- Create: `src/合併/merge_spec.yaml`（初版）
- Modify: 無程式碼——這是整合驗收

**Interfaces:**
- Consumes: 全部前置 Task

- [ ] **Step 1: 建初版 merge_spec.yaml（fix-only：不搬任何群）**

```yaml
# 合併規格 —— 圈選與裁決的唯一權威。
# 首輪 fix-only：不搬任何群，先驗流水線與基底修復。
# 圈選方式：看 reports/盤點報告.md，把要搬的群抄進 groups。
groups: []
overrides: []
params:
  boss_hp:
    targets: []      # 待使用者確認合併版 13 目標後填（§5.7 表為劍譜5 座標，需盤點核對）
  healthbar:
    enabled: false   # 等 s40 實測回報 start_hp 後開
    targets: []
  misc:
    const_replacements: []   # 待盤點確認無法無天是否有 436
```

- [ ] **Step 2: 全量跑一次快測試**

Run: `cd src/合併 && python -m pytest tests/ -v`
Expected: 全 PASS

- [ ] **Step 3: 首輪建置**

Run: `cd src/合併 && python build.py`
Expected: 各步印異動筆數（s10: 0、s30: 481、s40: 0、s50: 0、s60: 0、s90: 1），產出 `out/古龍921_合併.aoe2scenario`、`logs/` 六組 tsv+md、`logs/測試指引.md`

- [ ] **Step 4: 產出檔回讀驗證（slow roundtrip 已在 Task 6 驗過機制，這裡驗產物）**

Run: `cd src/合併 && python -c "import sys; sys.path.insert(0,'.'); from core import scenario_io; s=scenario_io.load('out/古龍921_合併.aoe2scenario'); print(len(s.trigger_manager.triggers))"`
Expected: 5599

- [ ] **Step 5: Commit＋部署給使用者實測**

```bash
git add src/合併/merge_spec.yaml src/合併/logs
git commit -m "build(合併): 首輪 fix-only 建置（s30 修復 481 攻擊力效果）"
cd src/合併 && python build.py --deploy
```

然後把 `logs/測試指引.md` 內容轉述給使用者，等實測回報。

---

## 收尾（本計畫完成後的流程，不是 Task）

1. 使用者實測 fix-only 版 → 回報
2. `python build.py --report` 產盤點報告 → 使用者圈選 → 填 merge_spec
3. 逐群建置實測；patch 群逐支寫 `s2N_patch_*.py`（按 base.py 介面，TDD 同上）
4. boss_hp targets 定位核對 → s40 → 實測回報 start_hp → 開 healthbar → s50
5. 每輪建置一個 commit（logs 全進 git）

## Self-Review 紀錄

- Spec 覆蓋：§4 佈局=Task 1-7；§5 各階段=Task 13-18（s20 明確排除，理由見檔案地圖節）；§6 紀錄/指引=Task 2/3；§7 盤點=Task 8-12；§7a 衝突=Task 11（偵測）＋13/15/17（BuildError 缺裁決）；§8 終檢=Task 18；§9 測試策略=各 test_guide＋Task 19；§10 未決=merge_spec params 迴路。
- 型別一致：`Change` 七欄、`Step.apply(ctx)->list[Change]`、`PairTable.a2b`、`ctx.notes['boss_refs']` 各 Task 引用相同名稱。
- 已知風險：(a) `import_triggers` 對跨界引用 warn+重設-1 的回填依賴 deepcopy=False 路徑，Task 13 slow 測試覆蓋；(b) 效果/條件確切工廠方法名（new_effect.change_object_caption 等）以 verify 文件實測形狀為準，Task 15/16 標必讀。
