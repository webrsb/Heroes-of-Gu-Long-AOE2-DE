# 野怪生成改名 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 野怪生成時把 DE 預設單位名換成「Lv{等級} {真實名}」（無等級者只套名），54 支生成觸發全部覆蓋。

**Architecture:** 新建置步驟 `s373_mobname` 讀 `merge_spec.yaml` 的 `mob_names` 對照表（鍵＝(tid, const)），在每支生成觸發尾端、對每個帶座標的 P7 `建立` 效果追加一個 `change_object_name(sp=7, 單位類型, 建立點單格)`——與原作改血/改攻同一套定址。不新增觸發、不進矩陣（全 sp=7）。

**Tech Stack:** Python 3.12、AoE2ScenarioParser 0.8.4、pytest（`src/合併/tests/conftest.py` 假件工廠）、merge pipeline（`src/合併/build.py`）。

**Spec:** `docs/superpowers/specs/2026-08-30-野怪改名-design.md`

## Global Constraints

- 絕不覆寫來源檔；所有寫入走 `core/scenario_io.py::write_out()`（唯一 write_to_file 呼叫點）。
- 絕不從 DE 編輯器存檔。
- merge_spec.yaml 是唯一裁決權威：對照表缺行、名稱/const 不符 → `BuildError('缺裁決：…')`。
- 一次建置＝一個 commit；commit 訊息含 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` 與 `Claude-Session: https://claude.ai/code/session_01JZPEEworRxiofecBsxQ1RM`。
- 步驤 id 字串排序決定執行順序：`s372` < `s373` < `s375` < `s38`。
- 觸發總數上限 33,000（本步不增觸發）。
- 所有指令在 `F:\aoe2de\src\合併` 執行（`cd /f/aoe2de/src/合併`，Bash 每次呼叫 cwd 會漂，務必帶 cd）。
- 顯示格式：有等級 `f'Lv{lv} {display}'`（ASCII 空格）；無等級 `display`。
- 既有 boss 命名觸發（`賓名`、`1惡2`、神兵 KING）不動。

---

### Task 1: 步驤模組 `s373_mobname`（TDD）

**Files:**
- Create: `src/合併/steps/s373_mobname.py`
- Test: `src/合併/tests/test_s373_mobname.py`

**Interfaces:**
- Consumes: `steps.base.trig_by_id(tm, tid)`, `steps.base.Step`, `steps.base.BuildError`, `core.change.Change(step, kind, target, field, old, new, reason)`；假件 `f.trig(tid=, name=, conds=, effects=)`, `f.eff_create(sp=, olu=, x=, y=)`, `f.tm([...])`；Recorder：`t.new_effect.calls == [(name, kwargs), ...]`。
- Produces: `apply_mob_names(tm, rows) -> list[Change]`；`rows` 為 dict 列表 `{tid:int, name:str, const:int, display:str, lv:int|None}`；`STEP = MobNameStep()`（id `'s373'`，讀 `ctx.spec.params['mob_names']`，無此鍵即回傳 `[]`）。

- [ ] **Step 1: 寫失敗測試**

```python
# src/合併/tests/test_s373_mobname.py
# -*- coding: utf-8 -*-
"""s373 野怪生成改名：每個帶座標建立效果尾端追加改名、格式、跳過無座標、防呆。"""
import pytest
from steps.base import BuildError
from steps.s373_mobname import apply_mob_names


def spawn(f, tid=2743, name='青銅獅', const=700, locs=((93, 56), (86, 54)), stray=True):
    effs = []
    for x, y in locs:
        effs += [f.eff_remove(sp=7, area=(x, y, x, y)),
                 f.eff_create(sp=7, olu=const, x=x, y=y),
                 f.eff_hp(sel=[], sp=7), f.eff_attack(sel=[], sp=7)]
    if stray:
        effs.append(f.eff_create(sp=7, olu=const))          # 無座標殘留（T2743 E16 型）
    return f.trig(tid=tid, name=name, effects=effs)


def renames(t):
    return [kw for n, kw in t.new_effect.calls if n == 'change_object_name']


def test_one_rename_per_located_create_with_level(f):
    t = spawn(f)
    tm = f.tm([t])
    changes = apply_mob_names(tm, [dict(tid=2743, name='青銅獅', const=700, display='青銅獅', lv=14)])
    rs = renames(t)
    assert len(rs) == 2                                  # 無座標的第 3 個建立不改名
    assert rs[0] == dict(object_list_unit_id=700, source_player=7,
                         area_x1=93, area_y1=56, area_x2=93, area_y2=56, message='Lv14 青銅獅')
    assert rs[1]['area_x1'] == 86 and rs[1]['message'] == 'Lv14 青銅獅'
    kinds = [c.kind for c in changes]
    assert kinds.count('eff_add') == 2 and 'skip' in kinds


def test_no_level_uses_display_only(f):
    t = spawn(f, tid=3998, name='豬王', const=424, locs=((238, 65),), stray=False)
    apply_mob_names(f.tm([t]), [dict(tid=3998, name='豬王', const=424, display='鐵豬王', lv=None)])
    assert renames(t)[0]['message'] == '鐵豬王'


def test_multi_const_trigger_uses_row_per_const(f):
    effs = [f.eff_create(sp=7, olu=424, x=10, y=10), f.eff_create(sp=7, olu=698, x=12, y=10)]
    t = f.trig(tid=4680, name='鐵鎚', effects=effs)
    apply_mob_names(f.tm([t]), [dict(tid=4680, name='鐵鎚', const=424, display='賓周幫幫眾', lv=None),
                                dict(tid=4680, name='鐵鎚', const=698, display='賓周幫幫眾', lv=None)])
    rs = renames(t)
    assert [(r['object_list_unit_id'], r['area_x1']) for r in rs] == [(424, 10), (698, 12)]


def test_name_mismatch_raises(f):
    t = spawn(f)
    with pytest.raises(BuildError):
        apply_mob_names(f.tm([t]), [dict(tid=2743, name='不對', const=700, display='青銅獅', lv=14)])


def test_missing_const_row_raises(f):
    effs = [f.eff_create(sp=7, olu=424, x=10, y=10), f.eff_create(sp=7, olu=698, x=12, y=10)]
    t = f.trig(tid=4680, name='鐵鎚', effects=effs)
    with pytest.raises(BuildError):
        apply_mob_names(f.tm([t]), [dict(tid=4680, name='鐵鎚', const=424, display='賓周幫幫眾', lv=None)])


def test_unused_row_raises(f):
    t = spawn(f)
    with pytest.raises(BuildError):
        apply_mob_names(f.tm([t]), [dict(tid=2743, name='青銅獅', const=700, display='青銅獅', lv=14),
                                    dict(tid=2743, name='青銅獅', const=999, display='幽靈', lv=1)])


def test_duplicate_row_raises(f):
    t = spawn(f)
    with pytest.raises(BuildError):
        apply_mob_names(f.tm([t]), [dict(tid=2743, name='青銅獅', const=700, display='青銅獅', lv=14),
                                    dict(tid=2743, name='青銅獅', const=700, display='青銅獅', lv=14)])


def test_trigger_without_located_create_raises(f):
    t = f.trig(tid=1, name='空', effects=[f.eff_create(sp=7, olu=700)])
    with pytest.raises(BuildError):
        apply_mob_names(f.tm([t]), [dict(tid=1, name='空', const=700, display='x', lv=1)])


def test_missing_trigger_raises(f):
    with pytest.raises(BuildError):
        apply_mob_names(f.tm([]), [dict(tid=5, name='x', const=1, display='x', lv=1)])
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `cd /f/aoe2de/src/合併 && python -m pytest tests/test_s373_mobname.py -q`
Expected: 全部 ERROR/FAIL，`ModuleNotFoundError: No module named 'steps.s373_mobname'`

- [ ] **Step 3: 寫最小實作**

```python
# src/合併/steps/s373_mobname.py
# -*- coding: utf-8 -*-
"""s373 野怪生成改名（params.mob_names；spec docs/superpowers/specs/2026-08-30-野怪改名-design.md）。

野怪全由生成觸發動態建立（移除格→建立→改血(格)→改攻(格)）。本步在每支生成觸發尾端、
對每個帶座標的 P7 建立效果追加 change_object_name(sp=7, 該單位類型, 建立點單格)，
與原作改血/改攻同一套定址；同 tick 內建立即改名。對照表鍵＝(tid, const)。
顯示：有等級 'Lv{lv} {display}'，無等級 '{display}'。全 sp=7、不進矩陣、不增觸發。"""
from core.change import Change
from .base import trig_by_id, Step, BuildError

CREATE, P7 = 11, 7


def _fmt(row):
    lv = row.get('lv')
    return f"Lv{lv} {row['display']}" if lv not in (None, '') else row['display']


def apply_mob_names(tm, rows):
    changes = []
    table = {}
    for r in rows:
        key = (int(r['tid']), int(r['const']))
        if key in table:
            raise BuildError(f'缺裁決：mob_names (tid,const)={key} 重複')
        table[key] = r
    for tid in sorted({k[0] for k in table}):
        rows_t = {c: r for (t_, c), r in table.items() if t_ == tid}
        name = next(iter(rows_t.values()))['name']
        t = trig_by_id(tm, tid)
        if t is None or (t.name or '') != name:
            raise BuildError(f'缺裁決：T{tid} 名稱「{getattr(t, "name", None)}」≠ 預期「{name}」，'
                             f'基底觸發編號可能位移')
        creates = [e for e in t.effects
                   if getattr(e, 'effect_type', None) == CREATE and getattr(e, 'source_player', -1) == P7]
        located = [e for e in creates if getattr(e, 'location_x', -1) not in (-1, None)]
        if not located:
            raise BuildError(f'缺裁決：T{tid}「{name}」無帶座標的 P7 建立效果')
        present = {getattr(e, 'object_list_unit_id', -1) for e in located}
        missing, unused = present - set(rows_t), set(rows_t) - present
        if missing:
            raise BuildError(f'缺裁決：T{tid}「{name}」建立單位 const {sorted(missing)} 無對照行')
        if unused:
            raise BuildError(f'缺裁決：T{tid}「{name}」對照行 const {sorted(unused)} 未出現於觸發')
        for e in located:
            msg = _fmt(rows_t[e.object_list_unit_id])
            x, y = e.location_x, e.location_y
            t.new_effect.change_object_name(object_list_unit_id=e.object_list_unit_id, source_player=P7,
                                            area_x1=x, area_y1=y, area_x2=x, area_y2=y, message=msg)
            changes.append(Change('s373', 'eff_add', f'T{tid}「{name}」', 'change_object_name', '',
                                  f'({x},{y})→「{msg}」', '野怪生成改名'))
        stray = len(creates) - len(located)
        if stray:
            changes.append(Change('s373', 'skip', f'T{tid}「{name}」', 'create_no_loc', '',
                                  str(stray), '無座標建立效果跳過'))
    return changes


class MobNameStep(Step):
    id = 's373'
    title = '野怪生成改名'
    intro = '生成觸發尾端追加 change_object_name（建立點單格定址）。無 mob_names 參數時跳過。'

    def apply(self, ctx):
        rows = ctx.spec.params.get('mob_names')
        if not rows:
            return []
        return apply_mob_names(ctx.base.trigger_manager, rows)

    def test_guide(self, changes):
        n = sum(1 for c in changes if c.kind == 'eff_add')
        return (f'野怪改名：{n} 個建立點掛改名。單人驗：殺一隻民團等補怪，點選看「Lv1 民團」；'
                '東北角鐵豬王、賓周走廊增援「賓周幫幫眾」。\n'
                '陽性對照：金獅/銀龍/賓周名稱不變、血條系統照舊。')


STEP = MobNameStep()
```

- [ ] **Step 4: 跑測試確認通過**

Run: `cd /f/aoe2de/src/合併 && python -m pytest tests/test_s373_mobname.py -q && python -m pytest tests -q`
Expected: 9 passed；全套 `174 passed, 4 deselected`（既有 165＋9）

- [ ] **Step 5: Commit**

```bash
cd /f/aoe2de && git add src/合併/steps/s373_mobname.py src/合併/tests/test_s373_mobname.py && git commit -m "feat(合併): s373 野怪生成改名步驟——建立點單格定址追加 change_object_name，(tid,const) 對照表防呆

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JZPEEworRxiofecBsxQ1RM"
```

---

### Task 2: 對照表落地、全量建置、驗證、部署

**Files:**
- Modify: `src/合併/merge_spec.yaml`（在 `  killvar:` 區塊之前插入 `  mob_names:`）
- Modify: `src/新負血劍譜/古龍921_DE遷移記錄.md`（補一段工項記錄）
- 產物: `src/合併/out/古龍921_合併.aoe2scenario`、`src/合併/logs/`

**Interfaces:**
- Consumes: Task 1 的 `STEP`（`steps/__init__.py::get_steps` 自動掃描 `s*` 模組，無需註冊）。
- Produces: 建置產物與遊戲目錄部署檔。

- [ ] **Step 1: 寫入 mob_names 對照表**

在 `merge_spec.yaml` 的註解行 `  # 【2026-08-30 擊殺獎勵變數化】` 之前插入（縮排 2 空格，與 `killvar:` 同層）：

```yaml
  # 【2026-08-30 野怪生成改名】spec docs/superpowers/specs/2026-08-30-野怪改名-design.md。
  # 名稱取生成觸發名、等級取任務面板「入侵者強弱排名」；D 類推定名（鐵豬王/豬頭/賓周幫幫眾/非魂/赤武）
  # 為使用者裁決。B 類區間等級 7 支（飛鵬幫先鋒/刀組、渦怪/渦怪手下、紅衣/9天/劍勇）待補，見 reports/野怪改名待補.md。
  # 鍵＝(tid, const)；同觸發多種單位一行一個 const（鐵鎚）。
  mob_names:
    - {tid: 2711, name: 母夜叉, const: 430, display: 母夜叉, lv: 52}
    - {tid: 2712, name: 青武, const: 763, display: 青武, lv: 55}
    - {tid: 2713, name: 渦鬼(珠光), const: 428, display: 渦鬼, lv: 250}
    - {tid: 2714, name: 渦鬼(殺人), const: 428, display: 渦鬼, lv: 250}
    - {tid: 2715, name: 孽僧, const: 726, display: 孽僧, lv: 59}
    - {tid: 2716, name: 獅頭魔, const: 752, display: 獅頭魔, lv: 60}
    - {tid: 2717, name: 石獅, const: 168, display: 石獅, lv: 61}
    - {tid: 2718, name: 斬狂, const: 556, display: 斬狂, lv: 61}
    - {tid: 2719, name: 幽靈武士, const: 291, display: 幽靈武士, lv: 65}
    - {tid: 2720, name: 赤武, const: 771, display: 赤武, lv: 66}
    - {tid: 2721, name: 狂屍, const: 25, display: 狂屍, lv: 87}      # 排名表「狂獅」判定錯字
    - {tid: 2722, name: 血鋼爪, const: 860, display: 血鋼爪, lv: 65}
    - {tid: 2724, name: 飛鵬幫戰將, const: 198, display: 飛鵬幫戰將, lv: 90}
    - {tid: 2726, name: 狂夜叉(下), const: 783, display: 狂夜叉, lv: 101}
    - {tid: 2727, name: 狂夜叉(上), const: 783, display: 狂夜叉, lv: 101}
    - {tid: 2728, name: 炎武(左), const: 569, display: 炎武, lv: 110}
    - {tid: 2729, name: 炎武(右), const: 569, display: 炎武, lv: 110}
    - {tid: 2730, name: 靈鳳劍仙, const: 629, display: 靈鳳劍仙, lv: 120}
    - {tid: 2732, name: 偏將, const: 73, display: 偏將, lv: 48}
    - {tid: 2733, name: 鏢師, const: 232, display: 鏢師, lv: 45}
    - {tid: 2734, name: 青衣樓殺手, const: 77, display: 青衣樓殺手, lv: 43}
    - {tid: 2735, name: 仙童, const: 41, display: 仙童, lv: 38}
    - {tid: 2736, name: 峨嵋弟子(男), const: 555, display: 峨嵋弟子(男), lv: 35}
    - {tid: 2737, name: 峨嵋弟子(女), const: 629, display: 峨嵋弟子(女), lv: 35}
    - {tid: 2738, name: 武僧, const: 281, display: 武僧, lv: 28}
    - {tid: 2739, name: 紅鞋子幫眾, const: 5, display: 紅鞋子幫眾, lv: 26}
    - {tid: 2740, name: 六扇門門生, const: 686, display: 六扇門門生, lv: 24}
    - {tid: 2741, name: 官兵, const: 692, display: 官兵, lv: 21}
    - {tid: 2742, name: 獅頭族人, const: 751, display: 獅頭族人, lv: 18}
    - {tid: 2743, name: 青銅獅, const: 700, display: 青銅獅, lv: 14}
    - {tid: 2744, name: 獵人, const: 93, display: 獵人, lv: 9}
    - {tid: 2745, name: 山賊, const: 448, display: 山賊, lv: 4}
    - {tid: 2746, name: 民團, const: 74, display: 民團, lv: 1}
    - {tid: 3959, name: 首領, const: 172, display: 首領, lv: 10}
    - {tid: 3997, name: 靈象, const: 558, display: 靈象, lv: 100}
    # ---- D 類推定名（無等級）----
    - {tid: 3998, name: 豬王, const: 424, display: 鐵豬王, lv: null}
    - {tid: 5112, name: 豬頭, const: 561, display: 豬頭, lv: null}
    - {tid: 4661, name: 成吉1, const: 172, display: 賓周幫幫眾, lv: null}
    - {tid: 4663, name: 書台部1, const: 698, display: 賓周幫幫眾, lv: null}
    - {tid: 4665, name: 武士1, const: 706, display: 賓周幫幫眾, lv: null}
    - {tid: 4667, name: 火砲1, const: 556, display: 賓周幫幫眾, lv: null}
    - {tid: 4669, name: 武士3, const: 165, display: 賓周幫幫眾, lv: null}
    - {tid: 4671, name: 武士4, const: 426, display: 賓周幫幫眾, lv: null}
    - {tid: 4672, name: 成吉3, const: 731, display: 賓周幫幫眾, lv: null}
    - {tid: 4673, name: 火砲2, const: 644, display: 賓周幫幫眾, lv: null}
    - {tid: 4680, name: 鐵鎚, const: 424, display: 賓周幫幫眾, lv: null}
    - {tid: 4680, name: 鐵鎚, const: 698, display: 賓周幫幫眾, lv: null}
    - {tid: 5335, name: 火炮3, const: 650, display: 賓周幫幫眾, lv: null}
    - {tid: 5336, name: 爆炸3, const: 428, display: 賓周幫幫眾, lv: null}
    - {tid: 5337, name: 馬弓4, const: 474, display: 賓周幫幫眾, lv: null}
    - {tid: 5338, name: 日本4, const: 844, display: 賓周幫幫眾, lv: null}
    - {tid: 5339, name: 武士5, const: 845, display: 賓周幫幫眾, lv: null}
    - {tid: 5340, name: 小哈5, const: 165, display: 賓周幫幫眾, lv: null}
    - {tid: 5372, name: 非魂, const: 424, display: 非魂, lv: null}
    - {tid: 4788, name: 遊俠, const: 773, display: 赤武, lv: 66}   # 使用者裁決：生成征服者＝赤武
```

- [ ] **Step 2: 全量建置**

Run: `cd /f/aoe2de/src/合併 && python build.py 2>&1 | grep -v '🔄\|✔' | grep 's372\|s373\|s375\|s39\|s90\|產出\|中止\|Error\|Traceback'`
Expected:
```
s372 野怪獎勵補回: 81 筆異動
s373 野怪生成改名: 190 筆異動      ← 189 eff_add ＋ 1 skip（T2743 E16 無座標）
s375 擊殺獎勵變數化: 332 筆異動
s39 ...
s90 終檢: 1 筆異動
產出：F:\aoe2de\src\合併\out\古龍921_合併.aoe2scenario
```
若 s373 中止於 `缺裁決`：訊息會指出 tid 與 const，回頭對照 spec 第二節修表，不得放寬防呆。

- [ ] **Step 3: 產物驗證（稽核＋抽查）**

Run:
```bash
cd /f/aoe2de/src/合併 && python -m analysis.audit_activation out/古龍921_合併.aoe2scenario ../../tmp/啟動稽核.md 2>&1 | tail -1
```
Expected: `違規 0 項、複核 15348 項`

Run:
```bash
cd /f/aoe2de/src/合併 && python - <<'EOF' 2>&1 | grep -v '🔄\|✔\|^\[\|^$\|Reading\|####\|>>>'
import io, sys
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')
from AoE2ScenarioParser.scenarios.aoe2_de_scenario import AoE2DEScenario
tm = AoE2DEScenario.from_file(r'F:\aoe2de\src\合併\out\古龍921_合併.aoe2scenario').trigger_manager
T = {t.trigger_id: t for t in tm.triggers}
n = sum(1 for t in tm.triggers for e in t.effects if e.effect_type == 26 and e.source_player == 7)
print('P7 改名效果總數:', n)
for tid in (2743, 3998, 4680, 4788):
    t = T[tid]
    print(f'T{tid}「{t.name}」:', [(e.object_list_unit_id, e.area_x1, e.area_y1, e.message)
                                  for e in t.effects if e.effect_type == 26])
print('賓名保留:', [e.message for e in T[4421].effects if e.effect_type == 26][:3])
EOF
```
Expected：`P7 改名效果總數: 189`；T2743 四筆 `(700, x, y, 'Lv14 青銅獅')`；T3998 `(424, 238, 65, '鐵豬王')`；T4680 兩筆 `(424,…,'賓周幫幫眾')`、`(698,…,'賓周幫幫眾')`；T4788 `(773, …, 'Lv66 赤武')`；`賓名保留` 仍是 `['成仔之筆記　　等級207', '神醫獨孤鐵', '雪毒堂護法']`。

Run: `cd /f/aoe2de/src/合併 && python -m pytest tests -q -m slow 2>&1 | tail -1`
Expected: `4 passed`

- [ ] **Step 4: 部署**

Run: `cp /f/aoe2de/src/合併/out/古龍921_合併.aoe2scenario "/c/Users/Ricky/Games/Age of Empires 2 DE/76561198023399153/resources/_common/scenario/古龍921_合併.aoe2scenario" && echo 部署完成`
Expected: `部署完成`

- [ ] **Step 5: 遷移記錄補記**

在 `src/新負血劍譜/古龍921_DE遷移記錄.md` 的 Kill Ratio 段（搜尋字串 `解法（\`spike_killvar\` 實證、\`s375_killvar\` 落地）`）該引用區塊結尾之後，加入：

```markdown
> **2026-08-30 野怪生成改名（s373）**：野怪全由生成觸發動態建立（移除格→建立→改血(格)→改攻(格)），
> 於各觸發尾端追加 `change_object_name(sp=7, 單位類型, 建立點單格)` 套「Lv{等級} {真實名}」；
> 名稱取觸發名、等級取任務面板「入侵者強弱排名」。54 支／189 建立點覆蓋；7 支區間等級待補
> （`src/合併/reports/野怪改名待補.md`）。spec：`docs/superpowers/specs/2026-08-30-野怪改名-design.md`。
```

- [ ] **Step 6: Commit（建置一次一 commit）**

```bash
cd /f/aoe2de && git add src/合併 src/新負血劍譜/古龍921_DE遷移記錄.md && git commit -m "build(合併): s373 野怪生成改名——54 支生成觸發 189 建立點套 Lv{等級} {真實名}

名稱取生成觸發名、等級取任務面板入侵者排名；D 類推定名（鐵豬王/豬頭/賓周幫幫眾×15/非魂/赤武）
依使用者裁決；B 類區間等級 7 支待補。建立點單格定址同原作改血/改攻，不增觸發。
測試 178 全綠（含慢測）、s90 過、稽核違規 0、已部署

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JZPEEworRxiofecBsxQ1RM"
```

- [ ] **Step 7: 回報使用者遊戲內驗收項**

1. 香城周邊殺民團等補怪 → 點選顯示「Lv1 民團」；狼窟「Lv14 青銅獅」
2. 東北角 (234-239,63-71)「鐵豬王」、「豬頭」
3. 賓周走廊護法死後增援顯示「賓周幫幫眾」
4. 陽性對照：金獅、銀龍、賓周（等級223）、六堂護法名稱不變；血條系統照舊
