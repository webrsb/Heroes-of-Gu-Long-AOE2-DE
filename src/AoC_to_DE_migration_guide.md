# AoC 1.0c/1.21z → AoE2 DE 劇情遷移通用參考

> 本文件是通用參考，適用於**任何**要從舊版（Age of Kings / The Conquerors /
> 1.21z 社群版）搬到 Definitive Edition 的自訂劇情。
> 所有結論都在 2026-08 的 DE（scenario v1.58 / trigger v2.5）上實測驗證，
> 測試檔為 `ZZ_test01`~`ZZ_test15` 系列；針對特定戰役（古龍921）的細節
> 見 `新負血劍譜/古龍921_DE遷移記錄.md`，那份是本文件的實測來源。
>
> 證據等級：本文所有「實測」條目 = 在 DE 遊戲內觀察到的行為；
> 「格式」條目 = 由檔案位元組直接讀出。無推測條目。

---

## 目錄

1. [轉檔流程與第一個陷阱](#1-轉檔流程與第一個陷阱)
2. [引擎數值模型（血量／傷害／哨兵值）](#2-引擎數值模型)
3. [轉檔造成的資料變形（必查）](#3-轉檔造成的資料變形)
4. [AoC 慣用技巧在 DE 的存活對照表](#4-aoc-慣用技巧在-de-的存活對照表)
5. [顯示層（任務面板／caption／字型）](#5-顯示層)
6. [未知物件 ID 的處理方法](#6-未知物件-id-的處理方法)
7. [靜態稽核方法論](#7-靜態稽核方法論)
8. [實機測試方法論](#8-實機測試方法論)
9. [新地圖遷移檢查清單](#9-新地圖遷移檢查清單)

---

## 1. 轉檔流程與第一個陷阱

### 流程

```
.scx (AoC)  --DE 遊戲內編輯器開啟並另存-->  .aoe2scenario (v1.58)
之後全部用 AoE2ScenarioParser (Python) 讀寫 .aoe2scenario
```

- **AoE2ScenarioParser 讀不了 AoC 的 `.scx`**，轉檔必須經過 DE 編輯器。
- 轉出後先跑一次讀回驗證（觸發器數、效果數、條件數、物件數）。

### 🔴 陷阱一：Big5 文字全滅

中文劇情的 `.scx` 內文字通常是 **Big5**。轉檔管線若解碼失敗，會產生一個
「結構完整、文字全空」的檔：觸發器/效果/條件/物件數量一個不少，
但**觸發器名稱、聊天訊息、改名效果、說明/提示/勝敗文全部變空字串**。

判定法（一分鐘）：

```python
chats = [e.message for t in tm.triggers for e in t.effects
         if e.effect_type == 3 and e.message]
# 一個 RPG 劇情通常有數千則；0 則 = 文字全滅，換一條轉檔路徑
```

檔案大小也看得出來：文字全滅的 `.scx` 會比原檔小數十 KB。
**先把 `.scx` 轉成 UTF-8 再進 DE 編輯器**是可靠解法。

### 陷阱二：DE 編輯器存檔會「正規化」數值

在 DE 編輯器裡開檔**再按存檔**，會改寫部分欄位（實測：`operation = -1`
被改成 `2`）。所以：

- 測試檔的母本放在遊戲資料夾**之外**，每次要測就複製進去
- 用編輯器**純瀏覽**沒問題，千萬別按儲存

### 建議設定

用 parser 產檔時設 `legacy_execution_order = True`，
讓觸發器執行順序與 AoC 一致（同 tick 內多觸發器的先後會影響三明治寫法）。

---

## 2. 引擎數值模型

這一節是整份文件的地基。AoC 時代的「特技」幾乎全是繞著這套模型寫的。

### 2.1 血量是兩個欄位

| 欄位 | 型別 | 誰改它 |
|---|---|---|
| **最大血量 max HP** | **int16**（改動量會 mod 65536 回繞） | `Change Object HP`（效果 27） |
| **當前血量 current HP** | **float32**，可到數十億 | `Damage Object`（效果 24），負值=回血 |

### 2.2 `Change Object HP` 的三個關鍵行為（實測）

1. **它是相加（ADD），不是設定** —— `operation = -1` 在 DE 語意等同 ADD。
2. 改最大血量時，**當前血量會等比例縮放**：
   `new_current = old_current × new_max ÷ old_max`。
   這就是為什麼舊劇情常在灌血前先把 max HP「正規化」到已知值。
3. 改動量走 **int16 回繞**：填 `65536 − N` 等於 `−N`。

### 2.3 `Damage Object` 的行為（實測）

- 負值 = 回血，**不回繞、不封頂**（float），單位可以被灌到 1.5 億血
- 死亡判定看**當前血量 ≤ 0**
- 遊戲的**傷害下限是 1**：護甲再高，每下至少扣 1（設計「不死物件」時別依賴護甲）

### 2.4 🔴 單位與建築物的決定性差異（DE 最大迴歸）

| | 舊版 AoC | DE |
|---|---|---|
| **單位** 當前血量超過上限 | 允許 | **允許**（155,204,672/70 實測成立） |
| **建築物** 當前血量超過上限 | **允許** | **夾在上限**（唯一例外見下） |

**這是遷移 RPG 劇情最常見的地雷**：舊劇情用「灌血的城堡/城牆」當
魔王、關卡閘門、進度鎖，DE 會把幾百萬血夾回幾千，戰鬥直接崩掉。

**唯一逃生門：max HP = 0 時夾制失效（「雙 0 血」）**

```
1. Change Object HP  ADD (65536 − 目前max)   -> max HP 變 0
2. Damage Object     大負值                   -> 當前血量灌到需要的量
```

代價：該建築的血條顯示 `❤ 0`，玩家看不到進度 → 需要自製血條（見 §5）。

**盤點時注意**：只有「會被灌血（負傷害）的建築」需要處理；
只吃 `Change HP` 與正傷害的建築完全不受影響。

### 2.5 `-1` 是「未設定」哨兵值 —— 但不是每個效果都認

實測結果不一致，必須分效果看：

| 效果 | `quantity = -1` 的行為 |
|---|---|
| `Tribute`（進貢） | **整個效果不執行**（被當成未設定） |
| `Damage Object` | **照常執行**（回血 1 點） |

- 進貢的邊界就只有 `-1` 這一個值：`-2` 起完全正常（不是「小額被夾」）。
- 自己寫觸發器要「+1 資源」時，**用負進貢別填 -1**，改 -2 以上或
  換 Gaia 正進貢。

### 2.6 AoC 的「不用負號」慣例

AoC 編輯器打不出負號，社群標準寫法是填補數：

```
int16 欄位（max HP 改動量）：想要 -N  ->  填 65536 − N
int32 欄位（傷害量）：      想要 -N  ->  填 4294967296 − N
```

**int32 那條有極限**：N > 2,147,483,648 時補數會落回正數區，
公式**靜默失效**（例：想回血 30 億 → 填 1,294,967,296 → 實際是
+12.9 億傷害，意圖治療、實際團滅）。稽核時看到 10 位數的傷害量，
先算一次補數還原，確認作者原意與實際值是否同號。

---

## 3. 轉檔造成的資料變形

### 3.1 攻擊力效果被重新切分（每張圖都要查）

`Change Object Attack` 的數值編碼是 `class × 基數 + amount`，
而**基數換了**：

| 版本 | 編碼 |
|---|---|
| AoC / 舊 trigger | `class × 256 + amount` |
| DE trigger v2.5 | `class × 65536 + amount` |

轉檔**不會**重算這個值。結果是舊檔的攻擊力效果在 DE 全部變形
（實例：一張圖 473 個效果全數損壞，買武器不加攻擊）。

**還原公式**：

```
old = 轉檔後讀到的 quantity
class  = old // 256
amount = old %  256          # amount > 127 者為負值補數：amount - 256
new = class * 65536 + amount
```

修復後用「成對結構」驗證：加成效果與解除效果應同 class、amount 互為正負。

### 3.2 `Change Object HP` 原值不變

血量效果的數值轉檔**不**變形（int16 慣例照舊）。變的是引擎行為（§2.4）。

---

## 4. AoC 慣用技巧在 DE 的存活對照表

全部實測。「用途」欄描述舊劇情常見的使用情境。

| 技巧 | 寫法 | DE 狀態 | 備註 |
|---|---|---|---|
| **負進貢給資源** | `Tribute qty<0, sp=玩家, tp=Gaia` | ✅ **完整保留** | 可累加、`-2` 起無下限、不彈提示訊息。RPG 的經驗值/貨幣系統可原樣搬 |
| **無座標 Task Object = 凍結** | 循環觸發器 + `Task Object` 不填座標 | ✅ **完整保留** | 每 tick 重下指派、蓋掉一切外來命令。**比 DE 的 `Freeze Object` 更牢**，別「改良」 |
| `Freeze Object`（DE 新效果） | — | ⚠️ 只擋玩家操作與單位 AI | **擋不住觸發器的 `Task Object`**。要真凍結，用上面那招 |
| **Kill Ratio 當擊殺計數** | `Accumulate Attribute attr=44` | ✅ 計數正常 | Kill Ratio = 擊殺 − 損失 |
| Kill Ratio 重置法（FAQ） | 領獎後 Create + Remove 自己的單位 | ❌ **失效** | DE 的 `Remove Object` 刪自己單位**不計損失** → 循環觸發器會無限發獎。改用觸發器開關（發完 Deactivate 自己） |
| **雙 0 血** | max HP 歸 0 + 負傷害 | ✅ 保留 | DE 建築唯一的超額血量途徑（§2.4） |
| **Object Visible（已棄用 type 15）** | 判定目標「看得見」 | ✅ 可用，語意查明 | = **我方視野 ∧ 在玩家畫面上**，兩者缺一不可。與新版 type 27 同 tick 成立，這是條件族本來的語意不是退化。DE 寬螢幕比 4:3 更容易成立 |
| Object Not Visible（type 16） | 反向判定 | ❌ **疑似已死**（實測從未成立） | 要「看不見才觸發」，改用 type 15 + 反向觸發器 |
| **無過濾破壞效果全圖生效** | `Damage`/`Remove` 只填玩家 | ✅ 保留 | 與距離、類型無關，且不跨玩家。結局清場、群體技可原樣搬 |
| `Send Chat` 重複訊息 | 循環發同一句 | ⚠️ **按內容去重** | 相同訊息合併成單行、頂到最新位置（非「相鄰才合併」）。功能沒壞、體感變差。要顯示次數就在訊息裡塞變動數字 |
| `Change View` | 鏡頭移動 | ✅ 可用 | `scroll=1` 平移。移到未探索區只見黑幕（鏡頭不揭圖）。AoC 的「長距離/貼邊會當機」問題未在 DE 重現 |
| `Own Fewer Objects` | 數量條件 | ✅ 可用，**語意是 ≤ N** | 不是「< N」。填 `quantity=0` = 「一個都沒有」。填 1 會把「正好 1 個」也算成立 |
| `operation = -1` | 舊檔常見 | ✅ 等同 ADD | DE 編輯器存檔會把它正規化成 2 |
| 英雄式 16777216 回血（float ULP） | hero regen 特技 | 未驗 | 本系列未遇到使用者，遇到再測 |
| Unload 滑行 / Patrol 隊形 / AI Signal 家族 / 駐紮技 / Place Foundation 夜景 | | 未驗 | 同上 |

> AoC 技巧總表來源：AoKH Scenario Design FAQ
> （aok.heavengames.com forum 4 thread 37500）。拿到新圖先對這份
> FAQ 做反向盤點：掃描觸發器，列出「實際用到的技巧×用量」，沒用到的不必測。

---

## 5. 顯示層

RPG 遷移常要自製 HUD（血條、進度）。DE 有四條顯示途徑，特性差很多：

### 5.1 四條途徑

| 途徑 | 位置 | 特性 |
|---|---|---|
| 觸發器顯示旗標 `display_on_screen=1` | 畫面右上 | 頂著「任務」標題；`display_as_objective=0` 可不進 F4 清單 |
| 同上 + `display_as_objective=1` | 右上 + F4 | 一般任務 |
| `Display Instructions`（效果 20） | 訊息面板 | 有顯示時限；`instruction_panel_position` 選位置 |
| **`Change Object Caption`（效果 88）** | **浮在物件頭上** | **不碰任務面板**、不需選取即顯示、可即時更新、物件死亡字自動消失 |

### 5.2 觸發器顯示旗標的細節（實測）

- 沒有條件的顯示觸發器**立刻達成** → 文字被劃線。掛一個永不成立的
  條件（`timer=30000`）保持未達成。
- `short_description` 支援 **`<變數名>`** 代入，變數值變動即時更新。
- 面板寬約 **20 個半形字**，超過會在任意字元處折行（數字會被折斷）。
- 用 `enabled=0` + `Activate/Deactivate Trigger` 控制何時顯示。

### 5.3 Caption 的細節（實測）

- **字型與任務面板不同**：全形符號（`％`）、幾何圖形（`▮ ■ ● ★ ❤ ▁`）
  全部變豆腐/問號。**只能用 ASCII + 中日韓文字**。
  （任務面板的字型反而有全形 `％` —— 同一份文字兩條途徑結果不同。）
- **不支援 `<變數名>` 代入**（原字照印）。要動態數字就分階梯逐次改 caption。
- ASCII 圖形血條可行：`亡魂之堡 [####______] 40%`。
- DE **沒有 Flash Objects 效果**（109 個效果 ID 全查過）。要標記位置，
  用會動的物件（營火）+ 旗子 + `Change View`。

### 5.4 自製血條的推薦做法

雙 0 血的目標看不到血量（§2.4），推薦 **caption 階梯**：

```
每 1%：1 個觸發器 = 條件 Object HP < 門檻 ×1 + 效果 Change Object Caption ×1
100 個觸發器/目標，非循環（達成即停，天然單調遞減）
```

- `Object HP` 條件在雙 0 血狀態下照常讀當前血量（instance 級，實測可用）
- caption 綁物件 → 玩家看不到目標就看不到字，**免距離開關**
- 門檻要用**開戰時的實際當前血量**換算（先套雙 0 血、實機讀一次再定刻度）

---

## 6. 未知物件 ID 的處理方法

舊劇情常用 parser 資料集查不到的物件 ID（被 DE 移除/改號的舊布景等）。

### 分類（全部可實測判定）

| 症狀 | 判定法 | 處置 |
|---|---|---|
| ID 完全無效 | `Create Object` 後 `Objects in Area` 查不到 | 找替代 ID |
| **有實體、無外觀** | 小地圖有點、地面看不到（實例：436 沉船） | 找同類布景替換（實例：DE 編輯器「其他→船」的 2352-2355/721/722） |
| **只認特定擁有者** | 逐一生給 P1/Gaia/P8 比對（實例：817 只能生給 Gaia） | 檢查原圖的 `source_player`，一致就不用改 |
| 正常 | — | 不動 |

### 快速全驗（一次 45 個 ID 的作法）

1. 每秒 `Create Object` 一個，**生成前先在聊天欄報 ID**（當機時最後一行就是兇手）
2. 排成網格（如 9×5、間距 5 格），跑完看地圖哪格是空的
3. 驗證用 `Own Objects`（≥1）+ 計數負進貢；失敗清單用
   `Own Fewer Objects quantity=0`（注意 §4 的 ≤N 語意）

### 查 ID 的土法（無 dat 工具時）

請使用者在 DE 編輯器空白圖上**按清單順序**擺放候選物件並存檔，
用 parser 讀回 `unit_const`，座標順序就是清單順序。

---

## 7. 靜態稽核方法論

### 7.1 🔴 效果的目標有三種指定方式 —— 只掃一種會漏

```
1. selected_object_ids（指名）
2. area (x1,y1,x2,y2) + source_player + object_list（區域/玩家/類型過濾）
3. 什麼都不填（無過濾 = 全圖）
```

實例教訓：只掃 `selected_object_ids` 時找到 1 個被灌血的建築；
把 235 個區域指定效果還原到實際物件後，是 **13 個**。
還原邏輯：

```python
def targets(e, all_objects):
    if e.selected_object_ids: return e.selected_object_ids
    return [o for o in all_objects
            if (e.source_player == -1 or o.player == e.source_player)
            and (e.object_list_unit_id == -1 or o.const == e.object_list_unit_id)
            and (無 area 或 o 在 area 內)]
```

### 7.2 🔴 「是不是建築」不能用 parser 資料集判定

parser 的 `BuildingInfo` 不含未知 ID —— 用它過濾會**跳過所有未知 ID**，
而那正是舊劇情最愛用的一批。實例：魔王城堡後面那座「要打倒的塔」
（const 208）就這樣漏掉，是使用者玩過才指出來的。

正解：未知 ID 是否具建築行為（血量會不會被夾）**實測判定**——
`Change HP +1000` → `Damage −50000` → `Object HP > 20000?`，
帶 CASTLE（必夾）與 KNIGHT（必不夾）雙對照。

### 7.3 盤點的固定清單

對新圖至少掃這些（全部可離線完成）：

1. 攻擊力效果（效果 28）的編碼形態（§3.1）—— `quantity ≥ 65536` 即已變形
2. 被**灌血**的建築（§7.1 的完整還原）
3. `Change HP` 絕對值 > 32767（int16 回繞是否符合作者原意）
4. 10 位數傷害量（int32 補數是否溢位，§2.6）
5. 負進貢全清單（`-1` 有沒有人用）
6. 無座標 `Task Object`（凍結用途，確認在循環觸發器內）
7. 已棄用條件（type 15/16）用量
8. 未知物件/科技 ID 清單與用途（實體 vs Create vs 條件）
9. `Change View` 座標是否貼圖邊（AoC 當機條件，DE 實測無妨但記錄）
10. 資源當計數器的映射（如 石=經驗、金=錢、木=技能點）—— 影響所有改動的判讀

---

## 8. 實機測試方法論

這一節的每一條都對應一次實際踩過的坑。

### 8.1 🔴 每個測試必須有陽性對照

沒有對照時，「全部失敗」分不出是被測目標壞了還是測試檔壞了。
實例：45 個 ID 全報無效，真相是測試檔自己兩個 bug
（`Tribute -1` 不執行 + `Own Fewer Objects` 語意搞錯）。
設計時先問「一切正常時哪一行**一定**會出現？」，把它印出來，
並在判讀表第一列寫明「這行沒出現 = 測試無效」。

### 8.2 測試環境

- **單人 → 劇情**進遊戲，不要用編輯器的測試模式（行為有差異）
- 測試檔母本放遊戲資料夾外（DE 編輯器會覆寫/正規化，§1）
- 說明文字用 `instruction_panel_position=2`，避免蓋住聊天欄
- 受害單位用 P3–P6，P1 留給人類（P1 全滅=落敗，回報來不及顯示）、
  P2 當不受碰的對照組；每個電腦玩家留一個「備援單位」以免滅亡判定觸發

### 8.3 讓結果「留得住」

聊天欄會捲走、錄影檔（`.aoe2record`）**只存玩家輸入**（觸發器結果不在裡面），
所以：

- 數值結果 → 用**資源數字**當輸出（負進貢 ±10 的倍數，畫面上方直接讀）
- 通過/失敗清單 → **只印失敗**（全過=零行），加時間標記行對時
- 空間結果 → 排網格，一張截圖讀完
- 每秒一步的長流程 → 每步前先報 ID/名稱（當機定位）

### 8.4 拆混淆變數

一個結果能被兩個機制解釋時，把兩個機制**在時間軸上錯開**。
實例：`Object Visible` 分不清看的是「單位視野」還是「玩家鏡頭」——
第 25 秒生單位（鏡頭在別處）、第 40 秒才把鏡頭移過去，
條件在哪個時點成立，答案就是哪個。鏡頭用 `Change View` 腳本控制，
不靠玩家自律。

### 8.5 常見的測試檔自傷

- 誘餌/刺激放在單位**視野外**（騎士視野 4 格）→ 對照組不動、全綠假象
- 計數用 `Tribute -1`（§2.5，不執行）
- `Own Fewer Objects` 填 1 想表達「沒有」（§4，會把 1 個也算成立）
- 沒宣告變數就用 `<變數名>`（原字照印）
- 全形 `％` 進 caption（豆腐）

---

## 9. 新地圖遷移檢查清單

按順序做，每步產出寫進該圖自己的規則書：

```
□ 1  轉檔：.scx --(必要時先轉 UTF-8)--> DE 編輯器 --> .aoe2scenario
□ 2  讀回驗證：觸發器/效果/條件/物件數；聊天訊息數 > 0（Big5 陷阱）
□ 3  反向盤點：對 AoKH FAQ 技巧清單掃描，列出「用到的技巧 × 用量」
□ 4  靜態稽核：§7.3 的 10 項
□ 5  修攻擊力編碼（幾乎必中）：§3.1 公式 + 成對驗證 → 存 _attackfix 新檔
□ 6  被灌血的建築清單 → 每個做雙 0 血；未知 ID 先過 §7.2 實測
□ 7  需要進度顯示的目標 → caption 階梯血條（§5.4）
□ 8  未知 ID 全驗（§6）；無效者找替代
□ 9  該圖獨有技巧若不在 §4 對照表 → 建最小重現檔實測，結果補回本文件
□ 10 全部驗完才動本體；改動一律另存新檔，永不覆寫原檔
```

**兩條鐵律**（來自使用者裁定，適用所有圖）：

1. **先驗完、再施工** —— 驗證通過不等於授權動本體，那是兩個決策。
2. **永不覆寫原始檔** —— 所有修復輸出到新檔名（`_attackfix`、`_fixed`）。

---

## 附錄：已實測的 DE 引擎雜項事實

- 條件 `Object HP` 是 instance 級、可在雙 0 血狀態讀當前血量；
  `Modify Variable by Attribute` 是 type 級（讀不到單一物件）
- `objects_in_area` 條件沒有 `comparison` 參數，反向邏輯用 `inverted=1`
- `Description`/`short_description` 的 `<變數名>` 語法有效；
  `<!stone storage>` 這類舊寫法無效
- DE trigger v2.5 的效果 ID 最大 109，**無 Flash Objects**
- `.aoe2spgame` 存檔是 raw deflate 可解壓（~5 MB），但物件記錄結構
  未逆向完成；`.aoe2record` 錄影檔 mgz 1.8.51 讀不了 101.103.x 的 body
- 傷害下限 1／護甲檔不住觸發器 `Damage Object`（直接改當前血量，無視護甲）
