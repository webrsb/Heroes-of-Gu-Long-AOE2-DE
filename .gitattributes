# 兩台機器（Windows F:\aoe2de／macOS ~/develop/…）交替建置，行尾必須有唯一裁決，
# 否則內容一模一樣的檔案會因行尾反覆進 diff。裁決：**索引與工作樹都用 LF**。
# 工作樹也發 LF 才有意義——管線的寫檔端已固定 newline='\n'（commit bd186e6），
# Windows 上不會再被 Python 的 open() 轉回 CRLF，所以不會出現「status 說改了、
# diff 卻是空的」那種假異動。
* text=auto eol=lf

# 建置產出的對帳報表（實例：34_任務欄文字校正.tsv，內文本身還含 CR 字元）。
# 上面那條已涵蓋，這裡明列是為了讓「這些是產物、行尾不得漂」這件事在檔案裡看得見。
src/合併/logs/*.tsv    text eol=lf
src/合併/logs/*.md     text eol=lf
src/合併/reports/*.tsv text eol=lf
src/合併/reports/*.md  text eol=lf

# 二進位：別讓 git 誤判成文字而做行尾轉換（會直接毀檔）
*.aoe2scenario binary
*.scx          binary
*.exe          binary
*.dll          binary
*.pyc          binary
