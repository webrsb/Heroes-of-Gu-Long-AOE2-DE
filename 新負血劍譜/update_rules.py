# -*- coding: utf-8 -*-
"""
update_rules.py —— 把 2026-08-29 這一輪的盤點結果與裁定寫進規則書

漏掉的東西：
  1. §4.2 的數字過期：無過濾效果 22 -> 18；建築物血量已由雙0血解決
  2. §4.3 缺 [5230] 修1 的 int32 回繞 bug
  3. 沒有條件型別盤點（OBJECT_HAS_TARGET 78 個從未記錄）
  4. §5.6d-3 / §5.6e 還寫著「待 01t / 01u 確認」，其實已通過
  5. 使用者已裁定「只在 DE 版遊玩，不用管相容性」，§5.6e 的相容性警告需結案
"""
import io
import sys

sys.stdout.reconfigure(encoding='utf-8')

P = r"F:\aoe2de\古龍921_DE遷移記錄.md"
NL = chr(10)
s = io.open(P, encoding='utf-8').read()
n = 0


def rep(old, new, label):
    global s, n
    if old not in s:
        print(f"  !! 未找到：{label}")
        return
    s = s.replace(old, new, 1)
    n += 1
    print(f"  ok  {label}")


# ---------------------------------------------------------------- §4.2
rep('''| 🔴🔴 2 | **建築物當前血量被夾在最大值（跨版本實機確認）** | **1 物件**：`亡魂之堡` 從 **6,000,300** 降為 **300** | 最嚴重迴歸。舊版實機 `6000300/300`，DE 為 `300/300`。見 1.2m / 5.5 |''',
    '''| 🟠 2 | **建築物當前血量被夾在最大值（跨版本實機確認）** | **1 物件**：`亡魂之堡` 從 **6,000,300** 降為 **300** | **修法已定案、尚未套用**。最嚴重迴歸（舊版實機 `6000300/300`，DE 為 `300/300`），解法為**雙0血**：見 1.2m / **5.6**。使用者指示：全部驗證完成後才動本體 |''',
    '§4.2 建築物血量列（修法已定案）')

rep('''| 🟡 2 | `Object Visible` 已棄用 | 7 條件 | `雪毒` `地裂` `沙天` `冰槍` `竹劍` `水刀` `賓周` |''',
    '''| 🟡 2 | `Object Visible` 已棄用 | 7 條件 | `雪毒` `地裂` `沙天` `冰槍` `竹劍` `水刀` `賓周`。**七個全部 `source_player = -1`**，`unit_object` 分別為 30320 / 30319 / 30318 / 30317 / 30316 / 30315 / 30304。待測：`ZZ_test04b`（見 4.5a） |''',
    '§4.2 ObjectVisible 列（補 sp=-1 與物件 id）')

rep('''| 🟢 4 | 無過濾破壞性效果（= 全地圖生效） | 22 效果（啟用中） | 部分疑似刻意防作弊，需逐個判斷 |''',
    '''| 🟢 4 | 無過濾破壞性效果（= 全地圖生效） | **18 效果**（原記錄 22 有誤，2026-08-29 重新盤點） | **作者是刻意依賴全圖生效的**（結局清場、群體傷害、全圖回血）。要驗的不是「有沒有壞」而是「DE 是否仍全圖生效」。明細見 4.5b。待測：`ZZ_test05b` |''',
    '§4.2 無過濾效果列（22 -> 18）')

rep('''| 🟢 5 | `Send Chat` 重複訊息被吞 | 400 字串重複、1029 在循環觸發器 | 體感退化，非功能損壞 |''',
    '''| 🟢 5 | `Send Chat` 重複訊息被吞 | 400 字串重複、1029 在循環觸發器 | 體感退化，非功能損壞。待測：`ZZ_test06b`（每 5 秒插唯一節拍訊息，才數得出重複次數） |
| 🟢 6 | 物件 `817` / 科技 `332` `317` 是否有效 | 817：**11 個實體（掛在 P0 Gaia）+ 12 處 `Create Object`**；科技：`[0] 科技` 研發給 **P7** | 待測：`ZZ_test07b`（生成 + 科技）與 `ZZ_test07c`（**實體**放置，單獨一檔以免載入失敗拖垮另一半） |''',
    '§4.2 SendChat 列 + 新增 817/科技 列')

# ---------------------------------------------------------------- §4.3
rep('''- `[4794]`：條件檢查 P4 黃金，效果卻扣 P5 → 複製貼上錯誤''',
    '''- 🔴 **`[5230] 修1`：`Damage Object` sp=1 `quantity = 1294967296`，無過濾（全圖生效）**
  - `1294967296 = 2³² − 3,000,000,000`
  - 作者想寫 **−30 億**（全圖回血），但超出 int32 範圍（min −2147483648）回繞成
    **正 12.9 億傷害**，會全圖秒殺 P1 所有東西
  - **AoC 也是同樣的 int32 欄位 → 原作者 bug，不是 DE 迴歸**
  - `ZZ_test05b` 用這個實際值打在 P6 上，單獨驗它的行為
- `[4794]`：條件檢查 P4 黃金，效果卻扣 P5 → 複製貼上錯誤''',
    '§4.3 新增 修1 的 int32 回繞 bug')

rep('''- 物件 ID `817`（11 個放置 + 12 個 `Create Object`）、科技 ID `332`/`317` 不在 parser 資料集內''',
    '''- 物件 ID `817`（**11 個實體全掛在 P0 Gaia** + 12 處 `Create Object`，觸發器 `1-80`~`6-80`）、
  科技 ID `332`/`317`（`[0] 科技`，**研發給 P7**）不在 parser 資料集內''',
    '§4.3 817/科技 的歸屬細節')

# ---------------------------------------------------------------- 新增 §4.5
rep('''### 4.4 無法靜態檢測的項目''',
    '''### 4.5 條件型別盤點（2026-08-29）

8032 個條件的型別分布：

| ID | 型別 | 數量 | 備註 |
|---|---|---|---|
| 5 | `OBJECTS_IN_AREA` | 3858 | 最大宗 |
| 10 | `TIMER` | 2463 | |
| 8 | `ACCUMULATE_ATTRIBUTE` | 1218 | 資源當計數器（石=經驗、木=技能點、金=銀兩） |
| 2 | `BRING_OBJECT_TO_OBJECT` | 187 | |
| 6 | `DESTROY_OBJECT` | 136 | |
| **14** | **`OBJECT_HAS_TARGET`** | **78** | **本次新發現的盤點項**，DE 仍支援，未列為問題 |
| 4 | `OWN_FEWER_OBJECTS` | 46 | |
| 1 | `BRING_OBJECT_TO_AREA` | 14 | |
| 3 | `OWN_OBJECTS` | 12 | |
| **15** | **`OBJECT_VISIBLE`** | **7** | 🟡 **已棄用**，見 4.5a |
| 13 | `PLAYER_DEFEATED` | 7 | |
| 7 | `CAPTURE_OBJECT` | 6 | |

**未使用**：`RESEARCH_TECHNOLOGY`(9)、`OBJECT_SELECTED`(11)、`AI_SIGNAL`(12)、
`OBJECT_NOT_VISIBLE`(16)、`OBJECT_HP`(3 在 DE 為 21) 等 —— 與 4.1 的「已棄用條件 0 使用」一致
（`Object Not Visible` 確實 0 個，`Object Selected` 也是 0 個）。

#### 4.5a `OBJECT_VISIBLE` 的 7 處明細

| 觸發器 | `unit_object` | `source_player` |
|---|---|---|
| `[4464] 雪毒` | 30320 | −1 |
| `[4485] 地裂` | 30319 | −1 |
| `[4566] 沙天` | 30318 | −1 |
| `[4591] 冰槍` | 30317 | −1 |
| `[4592] 竹劍` | 30316 | −1 |
| `[4593] 水刀` | 30315 | −1 |
| `[4599] 賓周` | 30304 | −1 |

`UGC` 明文（1.36 起棄用）：
> *"This condition should no longer be used. It does not work consistently (Even in Single Player).
> Use Object Visible (Multiplayer) instead."*

parser 的 `object_visible(unit_object)` 簽章與本體用法完全一致（無 `source_player`），
但 parser 產生條件時會把 `source_player` 自動填成 1 —— 做測試檔時必須手動壓回 **−1** 才忠實。

#### 4.5b 無過濾破壞型效果的 18 處明細

**判定條件**：無 `selected_object_ids`、無區域、無 `object_list_unit_id`、
無 `object_group`、無 `object_type`，只設了 `source_player`。

| 類型 | 數量 | 例子 | 看得出的用意 |
|---|---|---|---|
| `DAMAGE` 負值 | 11 | `[1205] 2松2` sp=2 qty=−300；`[2055] 棍2` sp=4 qty=−20；`[3835] 2馬3` sp=2 qty=−240；`[4391] 2++` sp=2 qty=−250 | **全圖回血**（回血術） |
| `DAMAGE` 正值 | 7 | `[4958]`~`[4963]` sp=1~6 qty=**+200**；`[5230] 修1` sp=1 qty=**1294967296** | **魔王全圖範圍傷害**（最後一個是回繞 bug，見 4.3） |
| `REMOVE` | 6 | `[4945]`~`[4950] 修端慕草1~6` sp=1~6 | **結局全圖清場** |
| `CHOWN` | 1 | `[4138] 4棍` sp=8 | — |

`[4945]`~`[4963]` 這一整串與 `[4926] 客棧`、`[4951] 馬` 相鄰 ——
**這個區段就是亡魂之堡最終戰**：全圖 +200 傷害是魔王的群體攻擊，
`修端慕草1~6` 是結局清場。動本體時要一併留意。

### 4.4 無法靜態檢測的項目''',
    '新增 §4.5 條件型別盤點與 4.5a/4.5b 明細')

# ---------------------------------------------------------------- §5.6d-3 確認
rep('''用**觸發器開關**控制顯示（`ZZ_test01t` 驗證）：''',
    '''✅ **已實機確認（`01t` / `01u` 2026-08-29）**：`enabled = 0` 的觸發器**不顯示目標**，
`Activate Trigger` 後才出現，`Deactivate Trigger` 後收掉。

用**觸發器開關**控制顯示：''',
    '§5.6d-2 標記 01t 已確認')

rep('''血條本身仍是 `enabled=0` + 永不成立條件（避免刪除線），
改由兩個**循環**觸發器依距離開關它：''',
    '''✅ **已實機確認（`01u` 2026-08-29，使用者回報通過）。**

血條本身仍是 `enabled=0` + 永不成立條件（避免刪除線），
改由兩個**循環**觸發器依距離開關它：''',
    '§5.6d-3 標記 01u 已確認')

rep('### 5.6e 最終修法（顯示旗標已由 `01s` 確認；開關待 `01t`、距離待 `01u` 確認）',
    '### 5.6e 最終修法（顯示旗標 `01s` ✅、開關 `01t` ✅、距離 `01u` ✅ 全部確認）',
    '§5.6e 標題更新')

# ---------------------------------------------------------------- 相容性結案
rep('''⚠️ **相容性**：血條用到**變數**（DE 新增功能，本體目前 0 個變數），
加入後檔案**無法回頭給 1.21z 遊玩**。
核心的雙0血 + `Kill Object` 不需要變數，那部分是雙版本相容的。''',
    '''### 相容性 —— **已結案（2026-08-29 使用者裁定）**

血條用到**變數**（DE 新增功能，本體目前 0 個變數），加入後檔案**無法回頭給 1.21z 遊玩**。

> 使用者裁定：**「我確定只在 DE 版遊玩不用管相容性」**

→ **不必為 1.21z 保留退路，變數、`Activate Trigger`、`objects_in_area` 等
　 DE 專屬功能可以自由使用。**
（僅供參考：核心的雙0血 + `Kill Object` 本身不需要變數，那部分原本是雙版本相容的。）''',
    '§5.6e 相容性結案')

# ---------------------------------------------------------------- 施工順序
rep('''*最後更新：2026-08-28（新增 5.6 `亡魂之堡` 最終定案、顯示層與排版限制）*''',
    '''## 第七部分：施工順序（使用者指示）

> **「先不要套本體，要先把全部驗完」** —— 2026-08-29 使用者指示

| 階段 | 內容 | 狀態 |
|---|---|---|
| 1 | 01 / 02 / 03 系列（血量、攻擊力、負傷害） | ✅ 完成，30 份文件全部回填實測結果 |
| 2 | 攻擊力修復 | ✅ 已套用 → `_attackfix` 檔 |
| 3 | **04 / 05 / 06 / 07 驗證** | 🔲 **進行中** —— `ZZ_test04b` `05b` `06b` `07b` `07c`，約 3 分鐘 |
| 4 | `亡魂之堡` 雙0血 + 百分比血條 | ⏸ **等階段 3 完成才動** |
| 5 | 全檔 `audit.py` 複驗 | ⏸ |

驗證文件在 `verify\\`，索引為 `verify\\00_INDEX.md`（30 份文件、狀態一覽）。

---

*最後更新：2026-08-29（新增 4.5 條件型別盤點、4.5a/4.5b 明細、修1 回繞 bug、
第七部分施工順序；相容性已結案為 DE-only）*''',
    '新增第七部分施工順序 + 更新日期')

io.open(P, 'w', encoding='utf-8', newline=NL).write(s)
print(f"{NL}共 {n} 處更新，總行數 {s.count(NL) + 1}")
