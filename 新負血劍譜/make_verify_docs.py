# -*- coding: utf-8 -*-
"""
make_verify_docs.py —— 補齊 verify/ 缺少的說明文件，並修掉重複的結果段

問題：
  1. 17 個已跑過的測試檔**完全沒有對應文件**
     （01b 01h 01i 01j 01k 01L 01n 01o 01p 01q 01r 01s 01t 01u 02c 02d 02e）
  2. 04~07 各有**兩個結果段**：舊測試的空表格 + 重測版新增的「（待填）」

所有結果都取自 `古龍921_DE遷移記錄.md` 的實測記錄或使用者當場的回報，
沒有記錄的一律標明「未實測」或「已被後續檔取代」，不臆造。
"""
import io
import os
import sys

sys.stdout.reconfigure(encoding='utf-8')

VDIR = r"F:\aoe2de\verify"
NL = chr(10)

DOCS = {}

# ----------------------------------------------------------------------
DOCS['01b_負血重測.md'] = '''# 01b — `operation` 三種值並排：−1 到底是 ADD 還是 SET  【P0】

**重現檔**：`ZZ_test01b_負血重測`　**狀態**：✅ **已完成**

## 問題

`01` 因為漏設 `operation`，parser 預設填了 ADD(2)，導致
`A + (65536 − A)` 互相抵銷回基礎血量 45，**沒有真正產生負血**，測試前提失效。

本體 **790 個** `Change Object HP` 效果全部使用 `operation = -1`。
AoC 沒有這個欄位，所以 −1 的語意是整個血量分析的地基：
若是 SET，最大血量會被真的設成回繞負值；若是 ADD，則只是累加。

## 測試設計

三條並排，唯一差異是 `operation`：

| lane | `operation` | 說明 |
|---|---|---|
| L1 | **−1** | 本體實際使用的值 |
| L2 | 1 = SET | 若 −1 等於 SET，L1 應與此相同 |
| L3 | 2 = ADD | 校準基準 |

## 結果（實機）

| lane | operation | 存活 | 血量 | 判定 |
|---|---|---|---|---|
| L1 | **−1** | 存活 | 45 | **與 ADD 相同** |
| L2 | 1 = SET | **死亡** | — | 真的設成回繞負值 → DE 殺死單位 |
| L3 | 2 = ADD | 存活 | 45 | 基準 |

**第二個佐證**：在同一檔手動追加一個 `Change Object HP` 參數 1000 的效果，
血量變成 **1045**（= 45 + 1000）而不是 1000。

**第三個佐證**：把測試檔在 DE 編輯器開啟再存檔，`operation = -1` 被寫回成 **2**。
DE 編輯器主動把 −1 正規化成 ADD。

## 結論

**`operation = -1` 在 DE 等同 ADD（相加）。**

AoC 同樣是 ADD，由 `NAKUZ` 文本自身反證：Q68 第三步 `A − 32767` 標註「還原」，
只有相加才能還原；Q61 的 `65536 − 單位上限值` 相加後回繞為 0，正是「雙0血」。

→ **本體 790 個效果沒有語意落差，這一項不需修改。**
→ 但這推翻了規則書早期所有以「設定」為前提的血量推論（見規則書第六部分）。

規則書：§1.2c
'''

# ----------------------------------------------------------------------
DOCS['01h_ChangeHP是否覆寫當前血量.md'] = '''# 01h — `Change Object HP` 對當前血量做什麼  【P0】

**重現檔**：`ZZ_test01h_ChangeHP是否覆寫當前血量`　**狀態**：✅ **已完成**

## 問題

已知 `Change Object HP` 會改最大血量。但它對**當前血量**做什麼？
三種可能：不動、覆寫成新的最大值、或等比例縮放。

這決定了「爆血」機制（當前血量遠超最大血量）能不能成立，
也決定 `亡魂之堡` 的修法方向。

## 測試設計

用**順序**當變數：治療與 `Change Object HP` 誰先誰後，結果會不同才能分辨模型。

## 結果（實機）

| lane | 序列 | 實測血量 | 模型計算 | 一致 |
|---|---|---|---|---|
| H2 | 治療 100000 | `100045/45` | 45 + 100000 | ✅ |
| H3 | ChangeHP+100 → 治療 | `100145/145` | 145 + 100000 | ✅ |
| **H1** | 治療 → ChangeHP+100 | **`322367/145`** | 100045 × 145/45 = 322367.2 | ✅ |
| H4 | 治療 → ChangeHP+10000 | `…332266/10045` | 100045 × 10045/45 = 22332267 | ✅ |
| H5 | 治療 155,204,605（Archbishop） | **`155204672/70`** | — | ✅ |
| H6 | 基準 Archbishop | `70/70` | 基礎血量 70 | ✅ |

## 結論

```
Change Object HP（相加 n）：
    新最大血量 = int16(舊最大血量 + n)                  ← 會 mod 65536 回繞
    新當前血量 = 舊當前血量 × 新最大血量 ÷ 舊最大血量     ← **等比例縮放**

Damage Object（負值 n）：
    新當前血量 = 舊當前血量 + |n|                       ← 不回繞、無上限

死亡判定：只看「當前血量 ≤ 0」，不看最大血量
```

**關鍵：`Change Object HP` 不是覆寫當前血量，而是等比例縮放（維持血量百分比）。**

H5 的 `155204672/70` 與本體 `毀僧聖僧` 在舊版與 DE 的顯示**位元級相同**，
這是「爆血」機制跨版本一致的直接證據。

規則書：§0.1、§0.2
'''

# ----------------------------------------------------------------------
DOCS['01i_建築物灌當前血量.md'] = '''# 01i — 建築物能不能像單位那樣灌當前血量  【P0】

**重現檔**：`ZZ_test01i_建築物灌當前血量`　**狀態**：✅ **已完成**

## 問題

單位可以「爆血」（`毀僧聖僧 155,204,672/70`）。
`亡魂之堡` 在舊版是 `6000300/300`，同一套手法用在建築物上。
DE 的建築物吃不吃這一套？

`01f` 當時的結論是「建築物完全不吃 `Damage Object` 負值」，
但那次城堡是**滿血**狀態，補不上去所以看不出差別 —— 測試設計有缺陷。
本檔先把城堡扣掉 1000 血（`3800/4800`）再施加各種做法。

## 結果（實機）

| lane | 做法 | 血量 | 解讀 |
|---|---|---|---|
| I1 | `Heal Object` 6,000,000 | **4800/4800** | 補到**滿**，不超過 |
| I2 | `Damage Object` −6,000,000（**區域** targeting） | **4800/4800** | **有生效**，補到滿 |
| I3 | `Damage Object` −6,000,000（**類型** targeting） | **4800/4800** | **有生效**，補到滿 |
| I4 | `Modify Attribute` → `REGENERATION_RATE` +10000 | 3800/4800 | 無變化 |
| I5 | `Change Object HP` +27,967 | **25940/32767** | 天花板 32767 確認 |
| I6 | 未施加（基準） | 3800/4800 | — |

## 結論

```
單位   ：當前血量可以遠超最大血量      （155,204,672 / 70；1045 / 45）
建築物 ：當前血量被夾在最大血量以內    （怎麼灌都只到 4800 / 4800）
```

**推翻 `01f` 的結論**：建築物**有**吃 `Damage Object` 負值，
真正的限制是它會把當前血量**夾在最大值**。

I5 再次驗證等比縮放模型：`3800 × 32767/4800 = 25940.7` → 實測 **25940**。

→ 當時的推論是「`亡魂之堡` 有效血量上限就是 32767」。
　 這個推論後來被 `01n` 的**雙0血**推翻（max = 0 時夾限不生效）。

規則書：§1.2k
'''

# ----------------------------------------------------------------------
DOCS['01j_無敵門檻.md'] = '''# 01j — `INVULNERABILITY_LEVEL` 等屬性能不能讓建築物無敵  【P0】

**重現檔**：`ZZ_test01j_無敵門檻`　**狀態**：✅ **已完成（全部失敗）**

## 問題

`01g` 曾對屬性 129（`INVULNERABILITY_LEVEL`）餵 **−6,000,000**，
但那個值餵錯了 —— 該屬性不是血量池，而是**門檻**：

> `UGC`：*"Sets an HP threshold after which a unit no longer receives damage when attacked"*
> `>0` = 基礎血量倍率、`<0` = 固定血量值

所以要用正確語意重測（−1 = 永不低於 1 血），並一併測回血類屬性。

## 結果（實機）

| lane | 做法 | 結果 |
|---|---|---|
| J1~J3 | `INVULNERABILITY_LEVEL` **−1 / −8000 / +1** | ❌ **六座城堡全被摧毀** |
| J4 | `Modify Attribute` → `REGENERATION_RATE` 60000 | ❌ 城堡仍被摧毀 |
| J5 | `Modify Attribute` → `REGENERATION_HP_PERCENT` 500 | ❌ 城堡仍被摧毀 |

## 結論

**用正確語意重測後仍然全部被摧毀 —— 屬性 129 對建築物無效。**

連同 `01g`（`HIT_POINTS` 對既有建築物無效）與 `01i`（血量被夾在最大值），
當時的結論是：

> **DE 沒有任何已測屬性能讓建築物的有效血量突破 32767。**

這個結論後來被 `01n` 的**雙0血**打破 —— 突破口不是屬性，而是把最大血量設成 0。

規則書：§5.5.1 B2 / E / F
'''

# ----------------------------------------------------------------------
DOCS['01k_建築換單位.md'] = '''# 01k — 把建築物換成單位以規避血量夾限  【P1】

**重現檔**：`ZZ_test01k_建築換單位`　**狀態**：✅ **已完成（使用者否決）**

## 問題

既然**單位**的當前血量可以遠超最大血量、**建築物**會被夾限，
那把 `亡魂之堡` 換成一個外觀像建築的**單位**，是否就能拿到 600 萬血？

`Replace Object` 效果可以做型別替換。

## 結果（實機・使用者回報）

| lane | 觀察 |
|---|---|
| K1 | 顯示的是**該單位**的血量 |
| K2 | 同上 |
| K3 | **被夾血** |
| K4 | **有效** |

## 結論

技術上 K4 可行，但**使用者判定「這不是好辦法，不採用」**。

理由（設計層面）：
- `亡魂之堡` 是勝利條件物件（`[4607] 嬴` 的 `DESTROY_OBJECT` 盯著它的 reference_id），
  替換後拿不到原本的 reference_id，勝利鏈需要改寫
- 外觀、選取框、佔地與建築物不同，玩家體驗會走味

→ 走 `01n` 的雙0血路線，本檔僅保留為排除紀錄。

規則書：§5.5.1
'''

# ----------------------------------------------------------------------
DOCS['01L_最大護甲.md'] = '''# 01L — 最大護甲 + 低血量，讓每下只造成 1 點傷害  【P1】

**重現檔**：`ZZ_test01L_最大護甲`　**狀態**：⚠️ **設計已被否決，未取得實測記錄**

## 問題

`UGC` 與官方更新說明：**傷害計算的最小值永遠是 1**
（*"the minimum total damage amount is still 1"*）。

於是有這個設計：

```
城堡護甲拉到極高（≥ 玩家最高攻擊 31267）
  -> 任何攻擊都只造成 1 點傷害
城堡最大血量設為「希望的命中次數」（例如 200）
  -> 恰好 200 下才會被摧毀，且與攻擊者的攻擊力無關
```

優點是只改兩個數值、不新增觸發器，而且各職業命中次數完全一致
（不會有 5.5.2 選項 3 的失衡問題）。

## 為何否決

**它讓神兵和木棍殺得一樣快。**

本體的核心進程就是買神兵變強（付出爆血代價換攻擊力），
若所有攻擊都固定造成 1 點傷害，**攻擊力成長完全失效**，
這摧毀的是遊戲設計本身，不是數值平衡問題。

使用者明確否決此方案。

## 結論

**不採用。** 未取得實測記錄（護甲數值上限與需覆蓋哪些護甲類型都未測），
但因設計層面已否決，不必補測。

改用 `01n` 的雙0血：保留 600 萬血池**且**攻擊力越高殺越快。

規則書：§5.5.3 設計一、§5.6
'''

# ----------------------------------------------------------------------
DOCS['01n_雙0血.md'] = '''# 01n — 雙0血：`NAKUZ` Q61 完整三步套在建築物上  【P0・關鍵突破】

**重現檔**：`ZZ_test01n_雙0血`　**狀態**：✅ **已完成 —— 這是整個血量問題的解答**

## 問題

`01g` / `01i` / `01j` 一路排除，當時的結論是
「DE 沒有任何方法能讓建築物的有效血量突破 32767」。

但 `NAKUZ` Q61 的完整結構從未測過：

```
效果：改變目標生命值｜參數：65536 − 單位上限值
效果：摧毀目標      ｜參數：2147483648
效果：摧毀目標      ｜參數：2147483648
```

解讀：
- 第一步 ADD(65536 − A) → 最大血量 = A + 65536 − A = 65536 → int16 = **0**
- 後兩步 `2147483648 = 2³¹`，當 int32 讀為 **−2147483648**
  → `Damage Object` 負值 = 治療 21 億，把當前血量灌爆

**先前為何漏掉**：`01c` 的 C2 只做了第一步（max 變 0），沒做後兩步灌血，
所以當前血量也跟著變 0 而死亡 —— 用不完整的結構「證明」了它不可行。

## 結果（實機）

| lane | 做法 | 挨打 | 結果 |
|---|---|---|---|
| **N1** | 城堡・完整三步 | ✅ | **挨打 30 秒存活** 🔑 |
| N2 | 城堡・只做第一步 | ✗ | **死亡**（重現 `01c` C2） |
| **N3** | 城堡・完整三步 | ✗ | **存活** |
| N4 | 步兵・完整三步 | ✅ | 存活（單位版本，符合 Q61） |
| N5 | 城堡・未處理 | ✅ | **被秒殺** |

## 結論

**雙0血（最大血量 0 + 當前血量極大）讓建築物在 DE 也能持有 600 萬血量並保持可受傷。**

關鍵在於：**建築物的當前血量夾限只在最大血量 > 0 時生效**。
最大血量為 0 時夾限不作用，當前血量可以任意大。

死亡判定只看當前血量 ≤ 0，所以它會持續掉血、可被偵測、最後可被擊殺 ——
正是 `亡魂之堡` 需要的行為。

→ 推翻 §5.5.1 的「無法突破 32767」與 §5.5.2 的全部選項。
→ 本檔的兩個缺陷（灌 42.9 億超出 `Object HP` 條件的 int32 上限、
　 30 秒存活標記覆寫 20 秒血量探測）由 `01o` / `01q` 改用 600 萬真實值解決。

規則書：§1.2n、§5.6
'''

# ----------------------------------------------------------------------
DOCS['01o_雙0血傷害偵測.md'] = '''# 01o — 雙0血後的傷害偵測（第一版）  【已被 01q 取代】

**重現檔**：`ZZ_test01o_雙0血傷害偵測`　**狀態**：⚠️ **已被 `01q` 取代**

## 本來要問的

`01n` 證實雙0血讓建築物無敵，剩下的問題是**傷害偵測**：
血量還在掉嗎？掉多少讀得出來嗎？（勝利鏈需要能判斷「打夠了」）

改用本體真實值 600 萬（而非 `01n` 的 21 億），讓血量落在
`Object HP` 條件的 int32 可偵測範圍內。

## 為何作廢

兩個載具缺陷，與 `01n` 同源：

1. **靠改名讀數會互相覆寫** —— 探測結果被存活標記蓋掉
2. 探測門檻是硬編的階梯，粒度粗

`01q` 改成**全部走聊天訊息**，順序不再互相干擾，並一次把
`Object HP` 條件、`<變數名>` 顯示、`Kill Object` 三件事一起驗完。

## 結論

無獨立結論，請看 `01q_傷害偵測與變數顯示.md`。
'''

# ----------------------------------------------------------------------
DOCS['01p_變數讀血量.md'] = '''# 01p — 能不能把建築物的當前血量讀進變數  【P0】

**重現檔**：`ZZ_test01p_變數讀血量`　**狀態**：✅ **已完成（結論：不能）**

## 問題

做血條需要一個「數字」。DE 新增了變數系統，看起來有現成的路：

```
MODIFY_VARIABLE_BY_ATTRIBUTE (87)   變數 := 物件屬性（屬性 0 = HIT_POINTS）
MODIFY_RESOURCE_BY_VARIABLE  (53)   資源 := 變數（倒進資源就能顯示在畫面上方）
VARIABLE_VALUE               (22)   條件，比對變數
```

讀數方式：P1 的「石頭」= 城堡當前血量（每秒更新），「木材」= 循環次數。

## 結果（實機・使用者回報）

**石頭一直是 4800，不會變。**（木材正常累加，證明循環有在跑）

4800 正好是**城堡型別的基礎血量**，不是那座城堡的當前血量。

## 結論

**`Modify Variable By Attribute` 是「型別層級」查詢**
（參數只有 `object_list_unit_id` + `source_player`，沒有 `unit_object`），
讀到的是**單位型別的屬性值**，不是某個實例的當前血量。

**DE 沒有「把實例當前血量讀進變數」的效果。**

→ 數字血條不能走這條路。
→ 改用 `Object HP` **條件**（它吃 `unit_object`，是實例層級）
　 搭配門檻階梯反推血量，見 `01q` / `01s`。

規則書：§5.6b
'''

# ----------------------------------------------------------------------
DOCS['01q_傷害偵測與變數顯示.md'] = '''# 01q — 雙0血下的傷害偵測與變數顯示  【P0】

**重現檔**：`ZZ_test01q_傷害偵測與變數顯示`　**狀態**：✅ **已完成**

## 三個問題

| # | 問題 | 為何要問 |
|---|---|---|
| Q1 | 雙0血狀態下 `Object HP` 條件讀不讀得到當前血量？ | `01p` 證實變數那條路封閉，這是唯一剩下的途徑 |
| Q2 | `<變數名>` 能不能長駐顯示在畫面上？ | 社群說可以，本專案未驗 |
| Q3 | `Kill Object` 能不能殺死雙0血建築？ | 勝利鏈需要 `DESTROY_OBJECT` 成立 |

讀數全部走**聊天訊息**，避免 `01n` / `01o` 改名互相覆寫的問題。

## 結果（實機）

**Q1 ✅** 聊天欄依序回報血量門檻：`5,900,000` → `5,500,000` → …
→ `Object HP` 條件吃 `unit_object`，是**實例層級**，
　 且在雙0血（最大血量 0）狀態下**讀得到**當前血量。

**Q2 部分成立**：
| 語法 | 結果 |
|---|---|
| `<變數名>` | ✅ **有效**（畫面顯示「階段=3」） |
| `<!stone storage>` | ❌ **無效**，顯示字面文字 |

**Q3 ✅** `Kill Object` 能殺死雙0血建築，`DESTROY_OBJECT` 成立。

## 結論

三件事湊齊，`亡魂之堡` 的完整修法成立：

```
雙0血               -> 建築物可持有 600 萬血且不死          01n
Object HP 條件      -> 實例層級，讀得到當前血量             01q Q1
<變數名>            -> 把數字長駐顯示在畫面上               01q Q2
Kill Object         -> 血量歸零時擊殺，勝利鏈照跑           01q Q3
```

→ 血條的具體做法（門檻階梯、百分比、排版、開關）見 `01r` / `01s` / `01t` / `01u`。

規則書：§5.6b
'''

# ----------------------------------------------------------------------
DOCS['01r_魔王血條.md'] = '''# 01r — 魔王血條第一版：原始血量階梯  【P1】

**重現檔**：`ZZ_test01r_魔王血條`　**狀態**：✅ **已完成（發現兩個問題，已在 01s 修正）**

## 設計

`01p` 證實無法把實例血量讀進變數，所以改用**門檻階梯**反推：
每跨過一個 `Object HP` 門檻，就把變數 `SET` 成該門檻值。

本檔用 100,000 間距、6,000,000 → 0 共 60 個門檻。

同時修掉前一版的刪除線問題：目標被劃掉是因為那個觸發器**沒有條件**，
開場立刻成立就被標記完成。改成給它一個**永不成立的條件**
（計時器 30000 秒 ≈ 8 小時），效果永不執行但目標文字照樣長駐。

## 結果（實機）

**刪除線 ✅ 解決** —— 永不成立條件有效，目標不再被劃掉。

**數字顯示 ✅ 成立但排版爆掉**：

```
任務
亡魂之堡　剩餘血量：10
0000
```

`100000` 被硬切成 `10` / `0000`，數字斷行就讀不出來了。

## 發現的限制

**任務面板寬度約 20 個半角字，超出就硬斷行，會把數字切成兩半。**

| 文字 | 半角字寬 | 結果 |
|---|---|---|
| `亡魂之堡　剩餘血量：100000` | 21 | ✗ 斷成 `10` / `0000` |
| `亡魂之堡 100％` | 14 | ✓ 有 6 格餘裕 |

原始血量最長 7 位數（`6000000`），標籤縮到 `血:` 也還是 19 個，貼著上限 ——
換字體或解析度就會再爆。

## 結論

- 永不成立條件是避開刪除線的正確做法 ✅
- **原始血量不適合放在任務面板**，改用**百分比**（最多 3 位數，永不斷行）
- 門檻階梯的做法本身可行，`01s` 沿用並改成百分比

規則書：§5.6b、§5.6c
'''

# ----------------------------------------------------------------------
DOCS['01s_魔王血條百分比.md'] = '''# 01s — 魔王血條第二版：百分比 + 不進任務清單  【P1】

**重現檔**：`ZZ_test01s_魔王血條百分比`　**狀態**：✅ **已完成**

## 改了什麼

**① 改用百分比**（`01r` 的原始血量會斷行）
1% = 60,000，共 100 個階梯 —— 比 `01r` 的 30,000 間距**少一半觸發器**，
而且每兩下更新一格，粒度足夠。

**② 不進 F4 任務清單**
使用者裁定：本體有自己的任務文字，血條塞進清單會**擾亂原本的任務呈現**，
而且沒人會點進去看。

| 欄位 | 值 | 理由 |
|---|---|---|
| `display_on_screen` | 1 | 右上角長駐 |
| `display_as_objective` | **0** | 不進 F4 清單 |
| `description` | `""` | 同上 |
| `description_order` | 99 | 排在本體任務下方 |
| `mute_objectives` | 1 | 每 1% 更新，避免提示音／閃爍 |

為了判斷右上角是靠哪個旗標顯示，同檔放了一個 `display_as_objective=1` 的對照組。

## 結果（實機）

**右上角兩行都出現**：`亡魂之堡 97％` 與 `[對照] 亡魂之堡 97％`
→ **`display_on_screen=1` 搭配 `display_as_objective=0` 仍然顯示** ✅

**F4 任務清單只有 `[對照]` 那行** → 不進清單的版本確實不污染任務呈現 ✅

**排版正常**，無斷行、無刪除線 ✅

## 附帶確認的一件事

雙0血狀態下**建築物自己看不到血量進度**：
滑鼠提示顯示 `❤ 0`，選取面板也沒有遞減數字。

這和舊版 `6000300/300`（玩家看得到數字往下跑）**不一樣**，是真實的體驗落差。
→ **右上角血條不是加分項，是補回舊版原有資訊的必要元件。**
→ 本體那 100 個階梯觸發器省不掉。

## 另一項確認

**本體 5232 個觸發器的顯示旗標全為 0 個**
（`display_on_screen` 與 `display_as_objective` 都沒人用，作者全靠聊天訊息與提示面板敘事）。

→ 加入血條後 F4 清單維持全空（與原版一致），右上角只有血條那一行，**零干擾**。

## 未定的小事

`description_order` 的排序語意未確認：`order=99` 出現在 `order=98` **上面**，
但觸發器建立順序也是前者在先，兩者無法區分。
本體只有一個顯示項，此欄位不影響結果，不深究。

規則書：§5.6c、§5.6d
'''

# ----------------------------------------------------------------------
DOCS['01t_血條開關控制.md'] = '''# 01t — 血條只在最終戰期間顯示  【已被 01u 涵蓋】

**重現檔**：`ZZ_test01t_血條開關控制`　**狀態**：⚠️ **內容已被 `01u` 完整涵蓋**

## 問題

血條的條件是**永不成立**（否則會被劃刪除線），所以它一旦啟用就**掛到遊戲結束**。
亡魂之堡是最終戰，開場第一秒就顯示「亡魂之堡 100％」是錯的。

| # | 問題 |
|---|---|
| Q1 | `enabled = 0` 的觸發器，其目標文字是否不顯示？ |
| Q2 | `Activate Trigger` 之後才顯示？ |
| Q3 | `Deactivate Trigger` 之後是否收掉？ |

## 設計

```
 0 秒  血條停用中   -> 右上角應空白
 8 秒  Activate     -> 出現「亡魂之堡 100％」
擊殺後 Deactivate   -> 消失
```

階梯觸發器也初始 `enabled=0`，一併 Activate，
最終戰之前省下 100 個觸發器的每 tick 條件檢查。

## 為何併入 01u

`01u` 在此之上再加**距離控制**（離開附近也要消失），
血條的顯示完全交給距離開關決定，因此 `01u` **包含 `01t` 的全部內容**。
使用者實測 `01u` 通過，本檔不需單獨執行。

## 結論

見 `01u_血條距離控制.md`。

規則書：§5.6d-2
'''

# ----------------------------------------------------------------------
DOCS['01u_血條距離控制.md'] = '''# 01u — 血條隨距離出現／消失  【P1】

**重現檔**：`ZZ_test01u_血條距離控制`　**狀態**：✅ **已完成（使用者確認通過）**

## 問題

使用者需求：不只「最終戰期間」，**離開亡魂之堡附近血條也要消失**。

AoE2 沒有鏡頭位置條件，只能用**單位距離**判定 —— 語意上也正確：
自己的角色在打魔王 → 血條在；角色撤退或全滅 → 血條收掉。

## 設計

血條本身仍是 `enabled=0` + 永不成立條件（避免刪除線），
顯示與否完全交給兩個**循環**觸發器：

| 觸發器 | 條件 | 效果 |
|---|---|---|
| 顯示控制（循環） | `objects_in_area(近框 ±12, quantity≥1, source_player=1)` | `Activate` 血條 |
| 隱藏控制（循環） | `objects_in_area(遠框 ±16, quantity≥1, **inverted=1**)` | `Deactivate` 血條 |

**API 事實**：`objects_in_area` **沒有 `comparison` 參數**，
反向判定靠 `inverted=1`（語意為「區域內少於 quantity 個」）。

同檔放兩條血條，因為「附近」的判定對象有兩種語意：
- **A**（`object_list=-1`，任何 P1 物件）—— 正式版採用。攻擊者貼著城堡打 → 持續顯示
- **B**（`object_list=83`，只看村民）—— 供手動走進／走出驗證開關真的有效

## 結果

使用者實測通過。

## 兩個必須記下的實作細節

**🔴 必須設緩衝區**：近框 ±12、遠框 ±16。

```
        ±12 ────────────────┐  進入 → 顯示
    ±16 ──────────────────────┐  離開 → 隱藏
        │  12~16 之間：兩個條件都不成立 → 維持現狀
```

兩框同大小的話，玩家站在邊界上血條會**每秒閃爍一次**。

**🔴 收尾順序**：魔王死亡時要**先 Deactivate 距離開關，再 Deactivate 血條**。
反過來的話血條會在下一個 tick 被還在跑的顯示控制重新打開。

## 附帶：階梯的自我校正性質

階梯依百分比**遞減**建立，故 trigger id 遞增 = 百分比遞減。
一個 tick 內若有多個門檻同時成立，**最後執行的正好是最低的成立門檻** —— 就是正確值。

→ 階梯即使中途停用再啟用，也不會顯示錯誤數字。
→ 所以階梯可以全程保持啟用（讓變數隨時是最新值），不必隨距離開關。

規則書：§5.6d-3
'''

# ----------------------------------------------------------------------
DOCS['02c_攻擊力正確編碼.md'] = '''# 02c — `Change Object Attack` 的 class 掃描  【P0】

**重現檔**：`ZZ_test02c_攻擊力正確編碼`　**狀態**：✅ **已完成（但 UI 數字不可信）**

## 問題

DE trigger v2.5 把攻擊力存成 `quantity = class × 65536 + amount`（v2.4 是 ×256）。
DE 轉檔把 AoC 的單純攻擊力數值在 ×256 邊界重新切分，
凡是 |值| ≥ 256 的都變成了假的 damage class。

要修，就得先確定**哪個 class 才是「單純加攻擊力」**。
本檔固定 `amount=100`，掃 class 0~6，看面板顯示。

## 結果（實機）

面板顯示 **`6+100`**（基礎 6、加成 100）。

但同一個效果（`class=0, amount=100`）在 `02b` 的**提示框**顯示
**「近戰 −94」** —— 兩個 UI 互相矛盾。

## 結論

**UI 數字不可信，兩個介面對同一份資料給出相反的數字。**

→ 改用**實際戰鬥傷害**當唯一判準，見 `02d`。
→ `02b` 依提示框寫成的「class=0 是減」結論（規則書舊 §3.3b）已作廢。

規則書：§3.3c（開頭段）
'''

# ----------------------------------------------------------------------
DOCS['02d_攻擊力實際傷害.md'] = '''# 02d — 以實際傷害量測攻擊力編碼  【P0・定論】

**重現檔**：`ZZ_test02d_攻擊力實際傷害`　**狀態**：✅ **已完成 —— 這是修復依據**

## 為何改用傷害量測

`02b` 的提示框說「近戰 −94」、`02c` 的面板說「6+100」，同一份資料兩個相反答案。
UI 不可信，只有**實際造成的傷害**是事實。

測法：靶最大血量 2045、已凍結不反擊、攻擊者原地防守、打約 7 下，量剩餘血量。

## 結果（實機）

| lane | 編碼 | 剩餘血量 | 造成傷害 | 推得攻擊力 |
|---|---|---|---|---|
| E1 基準 | 未加成 | 2003 | 42 | **6** |
| **E2** | **class=0, amount=100** | **1303** | **742** | **106** ✅ |
| **E3** | class=4, amount=100 | **1303** | **742** | **106** ✅ 與 class 0 相同 |
| **E4** | **class=78, amount=32** | **2003** | **42** | **6** 🔴 **完全無加成** |
| E5 | class=4, amount=30000 | 靶死亡 | — | 極高 ✅ |
| E6 | class=0, amount=30000 | 靶死亡 | — | 極高 ✅ |

## 結論

1. **`class=0, amount=N` 就是「攻擊力 +N」。** E2 與 E3 傷害完全相同
   → 提示框的 `−94` 是**顯示假象**。
2. **本體那 487 個 class=0 的效果本來就是好的**，不需修改。
3. **`class=78, amount=32` 完全無加成**（E4 傷害與基準一致）。
   該打包值 `5111840` 與本體 `火砲2` / `2刀3` / `周賓???` **位元組完全相同**
   → **確認 473 個效果在 DE 是死的。**
4. **沒有 255 上限**：class=0 帶 30000 一擊秒殺 2045 血的靶。
   `UGC` 指南的「quantity 上限 255」與實測不符。

## 修法（正值）

```
V = (DE現值 >> 16) × 256 + (DE現值 & 0xFFFF)      # 還原 AoC 原值
-> change_object_attack(armour_attack_class=0, armour_attack_quantity=V, operation=ADD)
```

驗算 `火砲2`：`5111840` → `78 × 256 + 32 = 20000` → `class=0, amount=20000` ✅

規則書：§3.3c
'''

# ----------------------------------------------------------------------
DOCS['02e_負值攻擊力寫法.md'] = '''# 02e — 負值攻擊力（解除加成）的正確寫法  【P0・定論】

**重現檔**：`ZZ_test02e_負值攻擊力寫法`　**狀態**：✅ **已完成**

## 問題

本體的攻擊力加成是**成對**的：買神兵時 `+N`、失效時 `−N`。
`02d` 定了正值的修法，負值那 134 個要怎麼寫？

三種候選：`operation=SUBTRACT` 搭正的 amount、`ADD` 搭負的 amount、
或直接沿用本體的 raw 負值。

測法同 `02d`：靶最大血量 20045、凍結不反擊、攻擊者原地防守、約 7 下。

## 結果（實機）

| lane | 序列 | 剩餘血量 | 傷害 | 攻擊力 |
|---|---|---|---|---|
| F1 | 未加成（基準） | 20003 | 42 | 6 |
| F2 | `class0 +500 ADD`（校準） | 16503 | 3542 | 506 |
| **F3** | `+500 ADD` → **`500 SUBTRACT`** | **20003**（＝F1） | 42 | **6** ✅ |
| F4 | `+500 ADD` → `200 SUBTRACT` | 17903 | 2142 | 306 ✅ |
| **F5** | `+500 ADD` → `ADD(−500)` | **16503**（＝F2） | 3542 | 506 🔴 |
| **F6** | `+500 ADD` → **raw −65652** | **16503**（＝F2） | 3542 | 506 🔴 |

## 結論

1. **`operation=SUBTRACT` 搭配正的 amount 是正解。**
   F3 精確回到基準、F4 精確落在 306。
2. **`ADD` 搭負值無效**（F5 沒有任何變化）。
3. **本體 134 個負值效果完全無作用。** F6 是 `1刀52`（raw −65652）的位元組重現，
   加成一點都沒被解除。
4. → DE 中**加成與解除加成兩邊都失效**，攻擊力系統整體是平的。
   **這正是神兵問題的成因**：付出每秒 6000 扣血的代價，攻擊力卻不變。

## 最終修復規則

```python
def restore(de_raw):
    sign = -1 if de_raw < 0 else 1
    v = abs(de_raw)
    V = (v >> 16) * 256 + (v & 0xFFFF)      # 還原 AoC 原值
    return (V, ADD) if sign > 0 else (V, SUBTRACT)
```

| 類別 | 數量 | 處理 |
|---|---|---|
| DE class = 0（原值 < 256） | 487 | ✅ 不動 |
| DE class ≥ 1（原值 ≥ 256） | 299 | 🔴 改寫為 `class=0, amount=V, ADD` |
| DE class < 0（原值為負） | 174 | 🔴 改寫為 `class=0, amount=V, SUBTRACT` |

驗算 `1刀52`：`−65652` → `1 × 256 + 116 = 372` → `class=0, amount=372, SUBTRACT` ✅
（其配對 `1刀5` = `+65652` → `ADD`，加成與解除正確成對）

**已套用**：`fix_attack.py` 修好 473 個效果，輸出
`src\\- 古龍 ９２１ 新負血劍譜５ -utf8_attackfix.aoe2scenario`，
報表 `attack_fix_report.tsv`，原檔未動。

規則書：§3.3d、§3.3e
'''


def write_docs():
    n = 0
    for name, body in DOCS.items():
        path = os.path.join(VDIR, name)
        if os.path.exists(path):
            print(f"  skip (已存在) {name}")
            continue
        io.open(path, 'w', encoding='utf-8', newline=NL).write(body)
        print(f"  new  {name}")
        n += 1
    return n


def fix_duplicate_result_sections():
    """04~07：移除舊測試留下的空結果表格，只留重測版那一個結果段。"""
    import re
    targets = ['04_ObjectVisible穩定性.md', '05_無過濾效果全圖生效.md',
               '06_SendChat重複訊息.md', '07_未知物件與科技ID.md']
    for name in targets:
        path = os.path.join(VDIR, name)
        s = io.open(path, encoding='utf-8').read()
        if '## 結果記錄' not in s:
            print(f"  skip (無舊結果段) {name}")
            continue
        # 舊的「## 結果記錄」整段（到下一個 ## 為止）換成一行指路
        pat = re.compile(r'## 結果記錄.*?(?=^## )', re.S | re.M)
        rep = ('## 結果記錄\n\n'
               '> 舊測試檔已作廢，其空白表格已移除。**請填寫本文件末尾的「結果」段。**\n\n')
        s2, cnt = pat.subn(rep, s, count=1)
        if cnt == 0:
            print(f"  !! 舊結果段比對失敗 {name}")
            continue
        io.open(path, 'w', encoding='utf-8', newline=NL).write(s2)
        print(f"  fixed {name}")


if __name__ == '__main__':
    print("補寫缺少的文件：")
    n = write_docs()
    print(f"\n新增 {n} 份\n")
    print("修掉 04~07 的重複結果段：")
    fix_duplicate_result_sections()
    print("\n完成。")
