# 01o — 雙0血後的傷害偵測（第一版）  【已被 01q 取代】

**重現檔**：`ZZ_test01o_雙0血傷害偵測`　**狀態**：⚠️ **已被 `01q` 取代**

## 本來要問的

`01n` 證實雙0血讓建築物無敵，剩下的問題是**傷害偵測**：
血量還在掉嗎？掉多少讀得出來嗎？（勝利鏈需要能判斷「打夠了」）

改用本體真實值 600 萬（而非 `01n` 的 21 億），讓血量落在
`Object HP` 條件的 int32 可偵測範圍內。

## 為何作廢

兩個載具缺陷，與 `01n` 同源：

1. **靠改名讀數會互相覆寫** —— 探測結果被存活標記蓋掉
2. 探測門檻是硬編的階梯，粒度粗

`01q` 改成**全部走聊天訊息**，順序不再互相干擾，並一次把
`Object HP` 條件、`<變數名>` 顯示、`Kill Object` 三件事一起驗完。

## 結論

無獨立結論，請看 `01q_傷害偵測與變數顯示.md`。
